種別: 調査所見（読み取り専用監査・原文保全）
日時: 2026-07-03 14時台
担当: 中間指揮官1（orca司令部ペイン・Opus 4.8）／采配・検収: 全体管理者A
出所: 同フォルダ plan.md（起票=kickoff手順経由・マルチ指揮官体制program子05実走1件目）
保全理由: 調査系の出力はチャットに流して終わりにせず、後から参照できる形でファイル保存する（2026-07-03ユーザー指示）

# skill-creator-custom 監査所見（2026-07-03・中間指揮官1報告原文）

**監査対象**: SKILL.md／references 3（create/review/absorb）／workflows 5（create-new/review-skill/migrate/global-scan/repo-index）／catalog（meta.md・applied.md・AGENTS.md）／logs/AGENTS.md／created 2件（kickoff・morning-routine）／deleted 2件（mokuteki-jisso・agent-task-orchestrator）。
**突き合わせ結果（前提）**: skills/19本 = meta 17 + applied 2 で完全整合。削除2本は skills/ と catalog 双方から除去済み✓。`logs/migrated/` は実在（空）＝create-new Step3.3 の参照は有効。

## 1. SKILL.mdの手順と実運用のズレ

1. **作成2件は概ね手順どおり**: create-new.md Step7（logs作成＋catalog追加）を遵守。両ログは作成テンプレ書式（日付時刻HH:mm JST・正本・概要・露出・備考）に準拠し meta.md にも登録済み。kickoff は「role-prompts組み込みはB締め後」を完了条件として備考に残置＝繰越が正しく追跡された（プロセスが機能した好例）。パス: `logs/created/2026-07/07-03-{kickoff,morning-routine}.md`。
2. **削除2件は日付時刻書式違反**: `logs/AGENTS.md` §2は `YYYY-MM-DD HH:mm JST` 必須（`date '+%Y-%m-%d %H:%M JST'`）だが、削除2件は「2026-07-03 JST」でHH:mm欠落（作成2件はHH:mm有り）。削除フロー側でタイムスタンプ取得が徹底されていない。パス: `logs/deleted/2026-07/07-03-*.md` 日付時刻行。
3. **削除ログのフィールドがテンプレと不一致（テンプレが実践に未追随）**: `logs/AGENTS.md` §6削除テンプレは「削除元/概要/露出削除/引き継ぎ履歴」。実ログは「削除正本/承認（決定ログ#8参照）/吸収部品の移動先/runtime露出撤去」でより有用だが、テンプレ側が未更新＝正本の自己不整合。パス: `logs/AGENTS.md` §6 vs 削除2件。

## 2. カバー漏れ

1. **段階runtime露出の手順・追跡機構が無い（最重要）**: 作成2件とも露出は `~/.claude` のみ、他4runtime（~/.agents/~/.codex/~/.gemini×2）は「実運用後に人間承認で追加予定」。だが create-new.md/migrate-skill.md は露出を一括前提（logsテンプレも5runtime前提）で、「一部だけ露出→後で残りを追加」の段階露出ステップと、未完了露出のバックログ/再チェック導線が無い。今日の2スキルが実際にこの中途状態＝後追い露出が抜ける穴。パス: `create-new.md` Step5-7 / `migrate-skill.md` Step5 / 両createdログ露出行。
2. **catalog↔skills/↔logs の整合照合ステップが無い**: 今回は手動突き合わせでOKだったが、global-scan.md は重複・矛盾の洗い出しが主眼で、「skills実体 vs catalog記載 vs deletedログ」の三点機械照合を明示ステップに持たない（孤児・記載漏れ・削除残骸の定期検知が非明示）。パス: `global-scan.md`。
3. **Codex向けSkill作成へのルーティングがWorkflowに無い**: create-new.md は対象がClaude系かCodex系かで分岐せず、`skill-creator-codex`（openai.yaml/quick_validate）へ渡すステップが無い（create-rules §10は openai.yaml に触れるのみ）。パス: SKILL.md §2 / `create-new.md` Step4-6。
4. **（軽微）改名の入口が弱い／自己doc style不整合**: 改名は description で謳うが SKILL.md §2.3 で migrate に同居し症状別分岐から見えにくい。また create-new.md Step3・review-skill.md Step3 が表形式を使用（personal-os の表を使わない方針・自身の create-rules §8「tableではなく…」と軽く不整合）。パス: SKILL.md §2.3 / `create-new.md` Step3 / `review-skill.md` Step3。

## 3. 他スキルとの重複・矛盾（境界）

1. **skill-delete: クリーン✓**。相互委譲が明文（custom §2.3→delete、delete→相談はcustom）。削除完了条件（deletedログ・catalog行除去・露出撤去・人間承認）は delete 側に集約。重複・矛盾なし。
2. **skill-creator-codex: 非対称な境界（要整理）**。meta.md は「ライフサイクル判断は custom」と言うが、(i) custom SKILL.md §2 Workflow に「Codex対象なら skill-creator-codex へ」の分岐が無く、(ii) codex 側 SKILL.md にも custom への委譲文言が無い。2つの作成スキルが相互ポインタ無しに並立＝入口次第で通る手順が変わる穴。パス: SKILL.md §2 ／ `skill-creator-codex/SKILL.md`。
3. **plan-ops: 概ねクリーン、軟い導線未接続**。責務は分離（plan-ops＝計画の機械手続き、custom＝Skill計画の配置判断）。ただし custom（create-new Step6.2/§11）は plan.md 手動配置を指示する一方、plan-ops は `new-plan.sh` で雛形生成を提供。custom から plan-ops の scaffold へのポインタが無い（矛盾ではないが二重の作り方が並立）。パス: `create-new.md` Step6 / `plan-ops`。

## 4. 改善候補の優先順位（上位3・各理由1行）

1. **段階runtime露出の手順化＋未完了バックログ**（最優先）— 今日作った2スキルが実際に「~/.claude のみ・残り露出は口約束」で追跡が無く、抜ける実害リスクが現存する。
2. **削除ログのタイムスタンプ徹底＋削除テンプレ更新** — 書式仕様（HH:mm必須）を実削除ログが破り、かつテンプレ自体が実践（承認/吸収先）に未追随＝正本の自己不整合を1手で解消できる。
3. **skill-creator-codex との境界を双方向に明文化** — 2つの作成スキルが相互ポインタ無しに並立し、Codex向け作成時に openai.yaml/quick_validate へ渡す導線が機械的に発火しない。

（制約遵守: 調査はファイル変更・コミット一切なしの読み取り専用で実施）
