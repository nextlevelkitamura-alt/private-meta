対象計画: plans/03-儀式の自動実行.md（program: 2026-07-09-デイリー運用刷新） ／ ラウンド: 01
diff範囲: コミット e0a1b50（直前 b888e77=計画改訂） ／ 規模: フル ／ 評価者: evaluator（Claude・異系統クロスチェック）

# 評価01: 3儀式（子03スコープ: Daily start 自動朝会）

## 項目別採点   ※ 子03「完了条件」と同順・全6項目

- [PASS] `board.py todo-add` で inbox todos に1行入り、今日ボードに表示される（対象: session-board・本番ボード）
  根拠: board.py:359-389 と store.py stmt_todo_insert（28列明示・inbox DB宛）を実物確認。test_todos.py 30件を再実行し全PASS（INSERT先=TURSO_INBOX_DB_URL・既定値・CHECK不正値の送信拒否・todo_id stdout）。読み取り専用の28列 `SELECT ... LIMIT 0` を実inbox DBへ実行し、stmt_todo_insert の列構成が本番スキーマと完全一致することを実測（書き込みなし）。実挿入→読み返しは指揮官実測PASS（テスト行dropped済み）。ボード表示は同一テーブルを読む既存機能で追加実装なし＝間接確認（残確認: 明朝の実運用で目視）。
- [保留] `daily-start --auto` が themes/todos を起票し、実行ログを書き、boardに成果logが残る（対象: skills/daily-start）
  根拠: フルE2E（実起票）は未実行で、初回は明朝10:03。部品は個別に実証済み: fetch-context.sh を本評価で実行し inbox_read=ok / board_read=ok / carried=1 / 前日logs=9件を実データで確認。SKILL.md 無人モード手順1-7 と prompt.md に起票→state/done→`board.py log`→finish の全手順が明記されている。結果状態は実測されていないため文書のみでPASSにしない。明朝の初回実走で判定を確定する。
- [PASS] 実行ログがある日の再実行（手動→10:03想定）は起票せずスキップする（対象: run.sh・スキル冪等ガード）
  根拠: tests/test_run.sh を再実行し7件全PASS。done マーカー存在時に exit 0・起動コマンド（cockpit/orca/claude）が一切呼ばれないことを実測（run.sh:72-76）。スキル側ガードも SKILL.md 手順1・対話モード・prompt.md に記載（手動実行日も同じ done マーカーを書く契約）。機械ゲートは run.sh 側にあり、マーカーがあればAI自体が起動されない。
- [保留] launchd `com.kitamura.daily-start` がloadされ、10:03 の `StartCalendarInterval` を持つ（対象: plist・launchctl list）
  根拠: launchd load は未実施（未充足）を実測で確認: `launchctl list` ヒットなし・`~/Library/LaunchAgents/` に symlink なし。設計物自体は検証済み: plutil -lint PASS・StartCalendarInterval Hour=10/Minute=3・RunAtLoad なし（test_run.sh ケース6-7で再実測）。launchd登録は人間ゲート（GLOBAL_AGENTS §7）であり、分類器ブロックによる環境制約で「朝の人間1コマンド」へ繰越し済み（loop.md・plistコメントに手順明記、初日はセッション内Cron代替と宣言済み）。load実施後に `launchctl print gui/$(id -u)/com.kitamura.daily-start` で再確認するまで本項目は閉じない。
- [PASS] 既存 session-board テストが全PASS・loops-registry verify.py が PASS（対象: 両テスト）
  根拠: 本評価で全て再実行。Pythonテスト11本=計264 PASS / 0 FAIL（builders14・common13・events48・events_sql26・output_layers7・reconcile16・spool20・subagents29・themes24・todo_steps37・todos30）、シェルテスト2本=80+60 PASS、verify.py=「PASS: 8 loops / 2 categories / plist / MD / HTML are consistent」。
- [PASS] routine の自称起票が存在しない（route='routine' は宣言照合経由のみ）（対象: daily-start 本文）
  根拠: SKILL.md に独立節「routine の自称禁止（重要）」（73-74行）＋手順4で禁止を明記、todo-add の usage 例示も `--route plan|single` に限定（50行）。prompt.md 8行目にも太字で禁止を明記。

## 総合判定

FAIL 0・PASS 4・保留 2。修正差し戻しは不要だが全PASSではないため done 移行は不可。保留2件はどちらも実装欠陥ではなく「実測タイミング待ち」:

1. 条件2 = 明朝10:03の初回フルE2E（実起票・実行ログ・成果log）を実測して確定。
2. 条件4 = 人間が loop.md 記載の1コマンド（symlink→bootstrap→enable）で load 実施後、`launchctl print` で実機確認して確定。あわせて条件1の「今日ボード表示」を明朝に目視。

保留解消後に評価02（ライトで可・上記3点の実測のみ）を回して閉じる。

## 修正指示ドラフト

FAILなしのため差し戻しなし。

## 追加指摘（FAIL外・参考）

- 中程度・文書整合: loop.md「permission と無人実行」節と実plist設定（bypassPermissions済み）の不整合 → **評価直後に指揮官が loop.md へ「現plistは人間承認により危険側を設定済み」の追記を実施済み（同日）**。
- 軽微: fetch-context.sh は todos 読取成功・themes 読取失敗の部分障害時に `inbox_read="ok"` のまま `active_themes=[]` を返す。テーマ重複作成のリスクは低く人間が対話で修正可能。次回改修時に themes 読取の可否を turso 節へ分離すれば足りる。
- secret混入なし（diff全域確認: token・認証値・DB auth部の出力なし。fetch-context.sh / run.sh とも Keychain 経由でコード上に値を持たない）。
