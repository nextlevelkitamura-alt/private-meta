対象計画: `plans/06-計画ミラー同期.md`（program「当日ボードSQL化」子06） ／ ラウンド: 評価01 ／ diff範囲: `~/Private` base=362c9adb → result=a2c730d9（子06パスに限定・子05コミット8df1279は除外） ／ 規模: フル ／ 評価者: reviewer（Opus・実装と別セッションのクロスチェック） ／ 評価日: 2026-07-18

## 項目別採点（計画と同順・全8項目）

**[保留] 1. active計画のprogram/子/共通/評価mdが plan_docs に生md本文とgit_commit付きで入り、md編集→コミットで自動差分同期される**
- 実測（DB非依存で確認できた範囲はPASS相当）: `plansync.py scan --json` を実activeへ実行し59文書抽出（program3/single2/child24/role2/eval28）。全59文書が `git_commit`（40桁hash）・`body`・`content_hash`（64桁）を保持。`stmt_doc_upsert`（plansync.py:375-384）が ON CONFLICT(path) でupsert。post-commitフック（plansync-post-commit.sh:22-36）はHEAD~1..HEAD差分をactive計画md正規表現で絞り→`sync --paths --apply`、非ブロッキングexit 0。
- 未検証（保留理由）: 実DB投入は inbox migration適用（人間ゲート・未実行）とpost-commitフック登録（人間ゲート）が前提。`.git/hooks/post-commit` は未登録を実確認（sampleのみ）。

**[保留] 2. plan_progress に _planops_map 算出の子N/M・完了条件x/y、parse失敗時 parse_ok=0、本文閲覧は生きる**
- 実測: `compute_progress`（plansync.py:188-212）が `_planops_map` 流用で算出（二重実装なし）。実scanで「当日ボードSQL化: 子1/7・完了条件0/8・parse_ok=1」「求人立案引き継ぎ基盤: parse_ok=0」を確認。parse失敗文書もbodyはsync_docsに入る（本文閲覧は生きる）。
- 保留理由: plan_progressへの実DB投入はmigration適用後に確認。

**[PASS] 3. secret疑い検知時は該当文書がDBに入らず人間に通知が届く**
- 実測: `scan_secrets`（:135-142）6パターンでヒット文書を blocked へ分離しsync_docsから除外（:443-449）。`notify`（:360-370）が notices-log＋stderr。common.py `_plansync_freshness_note` がSessionStart注入で通知検知。`test_plansync.sh` 再実行20 pass（AKIAフィクスチャ拒否・値非表示・通知ログ生成を実測）。DB非依存で完結。

**[保留] 4. activeから出た計画がミラーから消え孤児行が残らない**
- 実測: 差分モードのDELETE候補化（`build_plan`:453-460）をテストでPASS実測。full apply時は `_fetch_db_keys` →差集合DELETE（`apply_sync`:502-510）。
- 保留理由: フルreconcileの実DELETEはDB接続前提のため人間ゲート後に再検証。

**[保留] 5. 同一内容の再同期はhash一致でスキップ・送信失敗はspool再送で回復**
- 実測（機構はPASS）: `filter_unchanged`（:471-482）が一致skip・照会失敗時全送信フォールバック、テストPASS。専用spool名 `plansync-spool`＋inbox宛sender隔離（:531-541）、`test_spool.py` 再実行20 pass。
- 保留理由: 実DBとの送信往復（失敗→spool→再送成功）の末端はmigration適用後に再検証。

**[PASS] 6. DB側から計画本文を書き換える経路が存在しない**
- 実測: plansync.py はmd→DB一方向のみ（DB→md書き戻し関数は存在しない・コード全読）。migration（20260718_plan_docs_and_progress.sql:4-5）に「INSERT/UPDATEはplansyncのみ・書き戻し経路は作らない」明記、CREATE TABLE IF NOT EXISTS で再実行安全。

**[PASS] 7. areas規約に「計画本文のDB化は一方向の表示キャッシュに限る」の1行追記**
- 実測: `areas/AGENTS.md` §3先頭に追記をdiff実測。

**[PASS] 8. plan-ops・session-boardの既存テストが全通過**
- 実測（評価者が再実行）: plan-ops `__tests__/run.sh` = 209 pass / 0 fail。session-board py = 168 pass・shims 60 pass・main 80 pass、全0 fail。申告値と一致。

## 総合判定

**FAIL 0件 ／ PASS 4（項目3・6・7・8） ／ 保留 4（項目1・2・4・5）**。修正差し戻し不要。保留4項目は inbox migration適用・post-commitフック登録・初回一括投入という3つの人間ゲート待ちによるDB実landing部分のみ。この段階で子06をブロックする欠陥・回帰・secret混入は検出されなかった。

## 追加確認（レビュー/共通.mdの厳しく見る点・すべてクリア）

- 人間ゲートの先行実行なし: `.git/hooks/post-commit` 未登録を実確認。plansync-spool/notices/lock いずれの痕跡もなし（DB書込・spool退避の形跡ゼロ）。
- 既存契約の回帰なし: `spool.spoolable` の前方一致厳格化は既存 session_events/session_logs 文（store.py:105/107/114）と互換・既存テストpass。common.py注入は `repo != "Private"` で空文字＝既定挙動不変。board.py 3状態遷移は不変。
- 二重正本の発生なし: DBは読み取り専用表示キャッシュ・パーサは `_planops_map` 流用・第2の状態台帳なし。
- secret混入なし: migration・スクリプト・テストにtoken値/認証値/DB URL auth部なし。scan_secrets は値を返さず (label, line_no) のみ記録。

## 保留4項目の解除条件（人間ゲート実行後の再検証）

前提: (1) `turso db shell personal-os-inbox < migrations/20260718_plan_docs_and_progress.sql` 適用、(2) post-commitフックのsymlink登録、(3) `plansync.py sync --all --apply` 初回投入GO。

- 項目1: plan_docs SELECT で59文書相当が body・git_commit・content_hash 付きで存在。active計画md編集→commitでpost-commit経由の差分更新を確認。
- 項目2: plan_progress SELECT で子N/M・完了条件x/yが算出値と一致、parse_ok=0計画が0行落ちせず入り本文閲覧可能。
- 項目4: `git mv` でactive外へ移動→commit後に該当行DELETE、フルreconcileで孤児が残らない。
- 項目5: 連続 `sync --all --apply` の2回目がhash一致スキップ、送信失敗誘発→plansync-spool退避→replay回復。
