親計画: ../program.md ／ 対象計画: `plans/01-計画運用の正本入口を集約.md` ／ ラウンド: 01 ／ 規模: フル ／ 評価者: root（外部実装から独立したレビュー担当）
diff範囲: `plan-registry/`、`GLOBAL_AGENTS.md`、`my-brain/areas/AGENTS.md`、`my-brain/areas/ai運用/AGENTS.md` の作業ツリー差分

# 評価01: 計画運用の正本入口を集約

## 項目別採点   ※ 子計画の完了条件と同順

- [PASS] `plan-registry/AGENTS.md` に、正本/実行入口/機械手続き/手動判断/hookの責務が表または構造図で明示されている。
  根拠: §1の読む順番、§3の8者の責務表、§4の依頼→hookまでの構造図があり、個別plan本文・状態・履歴を所有しないことも明記している。
- [FAIL] Global、area、ai運用、registryのどれが規模・物理配置・実行手順・実行記録を持つかが一意で、同じ規約本文をコピーしていない。
  根拠: `plan-registry/AGENTS.md` §3は `plan-triage` が「規模を決めない」とする一方、現行 `skills/plan-triage/SKILL.md` §2と `references/route-contract.md` §4はサクッと判定を適用して `no_plan` routeを返す。規模基準の正本と、その基準を依頼に適用する役割を区別できていない。
- [PASS] 新規フォルダの `CLAUDE.md` が `AGENTS.md` への相対symlinkであり、本文コピーではない。
  根拠: `plan-registry/CLAUDE.md -> AGENTS.md` を `ls -l` で確認した。
- [PASS] `AGENTS.html` が白背景で、計画の置き場を動かさないことと責務の流れを人が1ページで確認できる。
  根拠: `AGENTS.html` に `color-scheme: light`、`background:#fff`、責務カード・フローがあり、暗色切替指定はない。ローカル `file://` はブラウザのURLポリシーで表示確認できないため、ここは静的確認で判定した。
- [PASS] `GLOBAL_AGENTS.md` と既存参照をgrepし、削った参照が未解決になっていない。
  根拠: registryの6つの相対参照（GLOBAL、areas、ai運用、plan-triage、plan-ops、hooks-registry）が全て解決し、対象4文書から旧session-board path・旧ai-jobs run-card参照は検出されなかった。
- [PASS] 既存計画・バケット・runtime設定を移動、削除、登録変更していない。
  根拠: 本子の新規 `plan-registry/` と4つの導線文書の差分は入口・参照の追加整理だけで、計画バケットやruntime露出を操作するscript・symlinkは含まない。並行中のhooks-registry再編の既存dirty差分は本評価の対象外として混同しない。

## 総合判定

FAILあり。`修正01.md` の責務表修正後に、現行 `plan-triage` の実装と再照合して評価02を行う。ほかのPASS項目は再確認対象として維持する。
