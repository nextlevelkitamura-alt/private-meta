親計画: ../program.md
並列: 子03表示側と同一レーン（モック承認後に一括実装）
人間ゲート: モック承認・push・本番反映

# 子06: 理想形モック駆動の仕上げ（ミラー掃除＋完成形の合意）

## 目的

2026-07-21 人間指示「サイドバーの現状が理想ではない。説明にある内容が理想。まず理想の見た目を
このデイリー部分に計画・実装・レビュー込みで作り込み、合意してから他へ展開する」を実行する。
進め方はモック先行（人間回答: Q1=A ミラー掃除即実行・Q2=A HTMLモック先行・Q3=A 子06として記録）。

## 実施済み（2026-07-21）

- [x] ミラー掃除: plansync sync --all --apply（242文）＋孤児31行のDELETE（計画立案実行完了基盤の旧activeパス一式ほか）。
      掃除後の bucket=active は本物の6本（ai運用5＋work「求人立案引き継ぎ基盤」1）に一致することをSQLで検証済み。
- [x] 理想形モックの提示: Artifact「当日ボード理想形モック」（explain想定・承認待ち）。
      設計判断5点=①カード=全エリアのactive計画 ②工程は実装→レビュー(まとめバッジ)→修正(出た時だけ)
      ③タスク見出し行なし ④AI行はセッション＋サブ展開(子03) ⑤動きなし計画も1行表示。

## 方針（モック承認後）

1. モックとの差分だけをfocusmapへ実装する（想定差分: 朝の意図ラベルの位置・「まとめ/出た時だけ」バッジ・
   サブ展開の枠）。子03表示側（sub-detail）も同じ規約でこのレーンで一括実装する。
2. 評価は「まとめ評価」1本: 修正02＋子05＋子03表示側＋本子の差分を 評価/まとめ評価01.md で一括採点する
   （2026-07-21人間指示「まとめられるレビューはまとめる」の初適用）。
3. 承認された見た目を正として、スマホboard/PCサイドバーは同一部品で自動展開（個別実装を作らない）。

## 完了条件

- [ ] モックが人間承認され、本番のサイドバー/ボードがモックの設計判断5点と一致している（対象: 本番実機）
- [ ] ボードのactive計画カードがフォルダ実体のactive集合と一致する（対象: plan_docs・実機）
- [ ] サブ展開でsub AIの種類・モデル・指示が見える（対象: 子03表示側・実機）
- [ ] まとめ評価01.mdが全PASS（対象: 評価/）

## 実装結果

実装後にplanctlが追記・更新する。実行前は記入しない。
