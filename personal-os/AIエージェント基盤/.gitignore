.DS_Store
__pycache__/
*.py[cod]
*.log
tmp/
output/
outputs/
.env
.env.*

# cockpit段階イベント（実行時状態・追記のみ・非git管理）
/skills/orca-cockpit/state/

# watch-keeper: 検知履歴(alerts.jsonl)・再通知防止リスト(seen.txt)の実行時状態（非git管理。loop.md参照）
/loops-registry/loops/watch-keeper/state/

# daily-notion-sync: DB id/parent page idキャッシュ・signature・last-count・mkdirロックの
# 実行時状態（secretではないが非git管理。loop.md参照）
/loops-registry/loops/daily-notion-sync/state/

# inbox-patrol: patrol有効化フラグ・pull済みid・出所マップ・DB idキャッシュ・スキーママーカーの
# 実行時状態（secretではないが非git管理。loop.md参照。2026-07-09 子06 tick統合で新設）
/loops-registry/loops/inbox-patrol/state/

# daily-start: 当日の冪等マーカー done-<日付>（起動された AI が書く実行時状態・非git管理。loop.md参照）
/loops-registry/loops/daily-start/state/
