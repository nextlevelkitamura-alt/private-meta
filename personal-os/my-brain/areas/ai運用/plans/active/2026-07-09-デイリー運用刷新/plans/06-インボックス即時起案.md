親計画: ../program.md ／ 分類: loop ／ 種別: 既存改善

# インボックス起案（手動指定のinbox-triage）

> 方針更新（2026-07-11）: 旧自動巡回loopとNotion自動pullは廃止・削除済み。再有効化・流用はしない。現行範囲は、人間がデイリーファイルと対象行を指定して `inbox-triage` を呼び、`plan-ops/scripts/new-plan.sh` でplanningへ起案する経路だけ。

## 目的

「やりたいこと」を伝えた瞬間（チャット／デイリーのインボックス節／外出先からNotion）に、plan-triage基準で判定して対象repoの `plans/planning/` に計画ドラフトを自動作成し、通知する。自動はドラフトまで・active化は人間承認（07-09裁定Q3）。

## 現状（調査5・2026-07-09）

- `skills/inbox-triage/` は人間指定で起案まで行う。自動巡回・行クレーム・headless起動は提供しない。
- Notion webhookは公開HTTPSエンドポイント必須で非現実的 → 既存 `notion-inbox-pull.sh` のポーリングを60秒tickに載せれば遅延≦60秒（`inbox-tick.sh` が pull→patrol の2段を実装済み・独立plist化可。稼働中の daily-notion-sync 30秒tickが同方式の安定実績）。
- 新裁定とのズレ: ①起案先が area `plans/active/` 直行（→planningへ変更）②領域4分類のみで対象repoの `plans/planning/` 非対応（plan-triage 経路判定の取り込みが必要）③見出しが `## 依頼インボックス`（現行デイリーには節自体が無い・子01で復活）。
- 最重要改善: 現行patrol.shは他者も書けるNotion行テキストを `--dangerously-skip-permissions` のheadlessに渡している → 役割別allowlist（planning配下と対象行のみ書込可・push/削除deny）への変更が再稼働の前提。
- plan-triage SKILL.md §6.2 が stale（new-plan.sh実在を「無い」と記載）・§7の「人間へ提示」はheadlessでは「planningに置いて通知＝提示」への読み替え明文化が必要。

## 方針

1. 人間が対象デイリーと対象行を指定する。
2. `inbox-triage` がplan-triageの経路・規模判定を使い、重複確認後にplanningへ起案する。
3. 雛形は現存する `skills/plan-ops/scripts/new-plan.sh` を直接使い、既存ファイルを上書きしない。
4. 自動巡回、Notion PATCH、macOS通知、launchd登録は本計画の現行範囲外。

## 完了条件（レビュー項目）

- [ ] 人間が指定した1行だけを処理し、他のインボックス行を変更しない
- [ ] 判定された対象repoの `plans/planning/<YYYY-MM-DD-名前>/plan.md` に原文・出所・対象日が残る
- [ ] 同一依頼の既存planning/active計画があれば新規フォルダを増やさない
- [ ] 起案完了時点で `plans/active/` を変更せず、git commit/pushを行わない
- [ ] 不存在helper・削除loop・Notion自動pull・launchdを実行導線から参照しない

## 関連

- 依存: 01（インボックス節の復活・見出し名の統一判断）
- 現行導線: `skills/inbox-triage/`・`skills/plan-ops/scripts/new-plan.sh`
- 判定基準の正本: `skills/plan-triage/SKILL.md`・`説明書/運用契約.md` §2
