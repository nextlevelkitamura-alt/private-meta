# 2026-07-16 E2E結果（子06前半: E2E 6系統 + 全lint）

- 担当: 子06前半（implementer・本ラウンドはE2E実行と全lintのみ。05一括レビュー・承認セット整形は別ラウンド）
- 作業dir: `/Users/kitamuranaohiro/.cache/private-worktrees/pf06`（branch `task/pf06`）
- base commit: `32a46d63c494235c928561d4f235ad0ede7a0eca`
- 対象子計画: `plans/06-E2Eと承認セット.md` の方針1（E2E 6系統）と方針3（全lint）

## 方法論

- 合成repo・合成plan・合成programはすべて `tempfile.mkdtemp()` で作った使い捨てディレクトリ配下に作成した。`~/Private` 本体・既存の実計画バケットには一切書き込んでおらず（各E2E内・本ドキュメント作成後の両方で`task/pf06`worktreeの`git status --porcelain`が無変更であることを確認済み）、実Turso・実Daily（`ゴール/デイリー/`）へは本E2E群のどのスクリプトもアクセスしていない（session-board連携はE2E(5)でも`board.py`/実GOAL_BASEを経由せず、hookのcommon.py層だけを直接呼んだ）。実secretは`install_fake_runtimes()`でSECRET/TOKEN/API_KEY/PASSWORD相当の環境変数をworker起動前に除去して混入を防いだ。
- 実runtime CLI（`codex` / `claude`）は起動していない。harnessの既存テスト（`agents-registry/harness/tests/test_program_run.py` の `RealGitProgramRunTest`）と同じ契約で、PATH上に fake `codex` / fake `claude` シェルスクリプト（実体はPython）を差し込み、`codex exec --json -C <cwd> [-s read-only] -o <final_path> <prompt>` と `claude --print --output-format json <prompt>` の入出力契約だけを満たす形で代替した。
- E2E駆動スクリプト本体（`e2e_lib.py` と `e2e1〜6_*.py`）はセッションのscratchpad配下に置いて実行した。本programの変更可能範囲は「E2E結果mdと一時ディレクトリ」のみのため、駆動スクリプト自体はworktreeにもrepoにもcommitしていない（再現に必要なコマンド・fixture本文は本ドキュメントに実文で残す）。
- 変更可能範囲（`変更可能範囲:` 契約フィールド）はすべてbacktickなしのbareなpathで書いた。理由は下記「発見した機構欠陥」の欠陥Bを参照（backtick付きpathは`planctl.py`側で正しく解釈されないため）。

## 全lint結果（suite別）

すべて `personal-os/AIエージェント基盤/` 配下、worktree内で実行。全緑。

- `skills/plan-ops/__tests__/run.sh`（plan-lint・program-lint・planctl・bucketctl・progctl・scaffoldの一括suite）: **178 pass / 0 fail**
  - `test_bucketctl.sh`: 39 pass / 0 fail
  - `test_plan_lint.sh`: 25 pass / 0 fail
  - `test_planctl.sh`: 44 pass / 0 fail
  - `test_progctl.sh`: 19 pass / 0 fail
  - `test_program_lint.sh`（既存fixture `__tests__/fixtures/lint-clean/` `lint-violations/` を使用）: 17 pass / 0 fail
  - `test_scaffold.sh`: 34 pass / 0 fail
- `agents-registry/harness/tests/`（`test_delegate.py` + `test_program_run.py`、`python3 -m unittest discover`）: **25 tests OK**（`HarnessTest` 10・`ProgramRunTest` 9・`RealGitProgramRunTest` 6）
- `agents-registry/roles/__tests__/test_role_definitions.sh`: **PASS**（explorer/implementer/reviewerの正本・Claude/Codex写像・sandbox_mode・固定値混入禁止を検査。fail-fast形式のため件数表示はなし）
- `hooks-registry/shared/plan-closeout/tests/test_plan_closeout.py`: **62 pass / 0 fail**
- `hooks-registry/shared/session-board/tests/`:
  - `test_common.py`: 13 pass / 0 fail
  - `test_builders.py`: 14 pass / 0 fail
  - `test_events.py`: 43 pass / 0 fail
  - `test_events_sql.py`: 26 pass / 0 fail
  - `test_output_layers.py`: 7 pass / 0 fail
  - `test_reconcile.py`: 16 pass / 0 fail
  - `test_spool.py`: 16 pass / 0 fail
  - `test-session-board.sh`: 80 pass / 0 fail
  - `test-shims.sh`: 59 pass / 0 fail
- `skills/plan-triage/scripts/validate-route-cases.mjs`・`validate-inbox-contract.mjs`: **exit 0**（各schema/fixture整合チェックがassertベースで全通過。`secret_values_emitted: 0` を確認）

**全lint合計: PASSチェック件数ベースで514件（plan-ops 178 + plan-closeout 62 + session-board python 135 + session-board shell 139）＋ harness unittest 25 tests OK ＋ roles 1 suite PASS ＋ plan-triage validator 2本 exit 0。FAILは0件、非ゼロexitも0件。** 実行後、`git status --porcelain` でworktreeが無変更であることを確認済み。

## E2E結果サマリ

- (1) 単発plan→Codex実装→Claudeレビュー→plan同期→done … **FAIL**（program-runが正本テンプレの`program.md`から子を検出できない。欠陥A）→ **2026-07-16追記: 修正後PASS**（欠陥A・B修正commit `3094758`／`34b7749`。詳細は本文末尾「修正後再検証（2026-07-16追記）」を参照）
- (2) Programの独立2子→2 worktree→統合→Program同期 … **FAIL**（同一欠陥Aを再現。見出しを回避すれば要求仕様どおり動作することは診断で確認済み）→ **2026-07-16追記: 修正後PASS**（同上。詳細は本文末尾「修正後再検証（2026-07-16追記）」を参照）
- (3) 並行計画との矛盾→conflict終了記録→archive … **PASS**（全項目確認）
- (4) completed→最終PASS→人間確認→archive … **PASS**（全項目確認。`done`バケット経由が前提という設計を確認）
- (5) Stop guardの動作（Stop guardとbucketctl双方） … **PASS**（全項目確認）
- (6) repo-local: Private以外の合成repoでの一巡 … **PASS**（全項目確認）

以下、各系統の詳細。

---

## E2E(1): 単発plan → Codex実装 → Claudeレビュー → plan同期 → done

### 設計判断

子計画06本文は「単発plan→…→done」と書くが、親program.mdの完了条件は「(1)(2)はprogram-run経由で完走」と明記しており、program-runはProgram（`program.md` + 子計画マップ）だけを対象にする（`plan.md`単体は対象外）。両方を満たすため、Wave1・子1体だけの最小programをprogram-run経由で完走させる構成にした。「→done」はProgramでは"全子完了・planctl同期済み"が実質的な完了シグナルであり（Programはbucket上`done/`を経由せず`active→archive`へ進む設計。`bucketctl_core.archive_errors`がprogram.md自身に「全子[x]」を要求するのもこの設計を裏づける）、単発planそのものの`active→done`実移動はE2E(6)で別途確認した。

### 実行内容・再現コマンド

合成repoを`mktemp`配下に作成し（`.gitignore`に`.planops-state/`、`plans/{planning,active,paused,done,archive}/`を用意、`main`作成後`integration`branchへ切替）、Wave1・子1体（`単一実装子`、変更可能範囲`src/e2e1.txt`）を持つ最小programを作成しcommit。fake codex/claudeをPATHへ差し込み、以下を実行した。

```
python3 <harness>/program_run.py \
  --program <repo>/plans/active/2026-07-16-e2e1-単発計画/program.md \
  --repo-root <repo> --state-dir <repo>/.planops-state \
  --run-id e2e1 --review-runtime claude
```

**Variant A（正本テンプレの見出しをそのまま使用・公式判定対象）**: 子計画マップの見出しを、`new-plan.sh --program`が実際に生成する形式（= 本program自身の見出しと同一）
```
## 子計画マップ   ※ 子の状態変更と同じコミットでここを更新
```
のまま使用。plan-lint / program-lintは違反なしで通過した（子01はProgramマップに正しく解決している）が、`program_run.py`は以下を返した。

```
{"status": "blocked", "events": ["blocked: 子計画マップに実行対象がありません"],
 "reason": "子計画マップに実行対象がありません"}
```

**Variant B（診断用: 見出しをbareな`## 子計画マップ`に変更）**: 同一fixtureで見出しだけをbareにすると、`program-run`は最後まで完走した（`status: completed`、子01完走、Programマップ「完了」化、`評価01.md`全PASS、workerファイルがintegration branchへ反映、no-ff merge記録、worktree cleanup、manifest phase=synced、作業treeクリーン、`main`は無傷、承認セットは人間ゲート無しのため未生成）。13項目すべてPASS。

### 判定: **FAIL**（Variant Aが公式判定。詳細原因は下記「発見した機構欠陥」欠陥Aを参照）

Variant Bにより、見出し一致さえ直れば実装→レビュー→同期→merge→cleanupの残りの経路は正しく動くことを確認済み（欠陥は`_section()`の見出しマッチ1箇所に局所化できる）。

**2026-07-16追記: 修正後PASS。** 欠陥A（commit `3094758`）・欠陥B（commit `34b7749`）修正後、Variant Aと同じ正本テンプレ見出しに加え、変更可能範囲もbacktick付き（`` `src/e2e1.txt` ``）にした最も厳しい構成で再実行し、13項目すべてPASSした。詳細は本文末尾「修正後再検証（2026-07-16追記）」を参照。

---

## E2E(2): Programの独立2子 → 2 worktree → 統合 → Program同期

### 実行内容・再現コマンド

E2E(1)と同じ合成repo構成で、依存関係の無い2子（`独立子A`=`src/e2e2-a.txt`、`独立子B`=`src/e2e2-b.txt`、いずれも`並列: 可 ／ レビュー: 都度`）を持つprogramを作成。fake codexに `FAKE_CODEX_LIVE_DIR` を渡し、implementer実行時に生存マーカーファイルを書いてから0.3秒待ち、その時点で存在する他taskの生存マーカーをsnapshotする仕組みを追加して「2 worktreeが実際に同時生存したか」を検証できるようにした。

```
python3 <harness>/program_run.py --program <repo>/.../program.md \
  --repo-root <repo> --state-dir <repo>/.planops-state \
  --run-id e2e2b --review-runtime claude
```
（環境変数 `FAKE_CODEX_LIVE_DIR=<tmp>/live-markers`）

**Variant A（正本テンプレの見出し）**: E2E(1)と同一の欠陥Aを再現（`子計画マップに実行対象がありません`）。子の数に依存しない欠陥であることを確認した。

**Variant B（診断用bare見出し）**: `status: completed`、`completed: ["01", "02"]`。events に `implement: 01,02`（同一Waveで並列委譲）を確認。live snapshotは両方存在し、`e2e2b-01.snapshot`・`e2e2b-02.snapshot`のいずれにも相手のtask_idが写っており、**2つのworktreeが実際に同時生存した瞬間があったこと**を直接確認できた。Programマップは01・02とも「完了」化、両子の`評価NN.md`が全PASS、workerファイル2件がintegration branchへ反映、`git log --merges`に2件のno-ff merge、子ごとに別branch（`plan-task/e2e2b-01`・`02`）、両worktreeともcleanup済み、manifest phase=synced（両方）、作業treeクリーン。全項目PASS。

### 判定: **FAIL**（Variant Aが公式判定。欠陥Aは子の数に関係なく再現するため）

Variant Bにより、「独立2子→2 worktree→統合→Programマップ同期」という要求仕様そのものは、見出し欠陥さえ回避すれば正しく動くことを確認済み。

**2026-07-16追記: 修正後PASS。** 欠陥A（commit `3094758`）・欠陥B（commit `34b7749`）修正後、Variant Aと同じ正本テンプレ見出しに加え、両子の変更可能範囲もbacktick付き（`` `src/e2e2-a.txt` ``／`` `src/e2e2-b.txt` ``）にした最も厳しい構成で再実行し、2 worktreeの同時生存確認を含む全項目がPASSした。詳細は本文末尾「修正後再検証（2026-07-16追記）」を参照。

---

## E2E(3): 並行計画との矛盾 → conflict終了記録 → archive

### 実行内容・再現コマンド

未完了（完了条件2項目のうち0項目チェック済み）の単発`plan.md`を2部用意し、`planctl.py close`（内部で`bucketctl_core.move`を呼ぶ）を実機構どおり使用した。

**(a) 対照実験**: 未完了のまま `--disposition completed --apply` を試行 → 拒否を確認。
```
python3 <plan-ops>/planctl.py close --plan <plan_a>/plan.md --plans-root <repo>/plans \
  --repo-root <repo> --disposition completed --apply ...
→ exit 1: "bucketctl: archive を拒否: completed には全完了条件の [x] が必要 / completed には最終評価md全PASSが必要"
```
plan.mdはactiveに留まった。

**運用上の注意点（挙動メモ・欠陥ではない）**: `cmd_close()`は`bucketctl_core.move()`呼び出し前に`終了記録.md`を無条件で書き出すため、archiveがrejectされても`終了記録.md`自体はplan.mdと同じディレクトリに残る。同一ディレクトリへ`--disposition conflict`で訂正再試行しても、「既存ファイルは上書きしない」ため stale な`終了記録.md`（completed）がそのまま使われ、再度同じ理由で拒否されることを実際に確認した。正しく訂正するには、別ディレクトリで再試行するか、人間が`終了記録.md`を確認・削除してから再試行する必要がある。

**(b) 本命**: 別ディレクトリで、後継・理由・人間確認・未完了事項を明記した`--disposition conflict --apply`を実行。
```
python3 <plan-ops>/planctl.py close --plan <plan_b>/plan.md --plans-root <repo>/plans \
  --repo-root <repo> --disposition conflict \
  --human-confirmation "指揮官(pf06 E2E script代理)2026-07-16 確認" \
  --reason "並行計画pf06-e2e3-conflict-sourceが同一対象を先行実装したため重複を避けて終了" \
  --successor pf06-e2e3-conflict-source \
  --implemented-scope "未着手(現状分析のみ)" --remaining "src/e2e3.txtの実装・レビュー" \
  --evidence "..." --apply
→ exit 0: "適用済み（未コミット）: <repo> (plans/archive/2026-07-16-e2e3-b-conflict実例)"
```

結果: `plan.md`がarchiveへ移動、activeには残らず、`終了記録.md`に終了区分=conflict・後継・理由・人間確認・未完了事項がすべて残存。**完了条件は`[ ]`のまま**（completedへの偽装なし）。archive後にplan-lintを再実行しても違反なし（`plan-lint.sh`はarchive配下では`bucketctl_core.archive_errors`も再検査するため、二重の経路で整合を確認）。

### 判定: **PASS**（全12チェック項目PASS）

---

## E2E(4): completed → 最終PASS → 人間確認 → archive

### 実行内容・再現コマンド

**(a) 対照実験1**: 完了条件は全チェック済みだが評価mdが無い状態で`--disposition completed --apply` → 拒否（`completed には最終評価md全PASSが必要`）。
**(a-2) 対照実験2**: 評価mdはあるが完了条件が未チェックのまま → 拒否（`completed には全完了条件の [x] が必要`）。

**(b) 本命**: 完了条件全チェック・評価01.md全PASSの計画に対し、まず

```
<plan-ops>/bucketctl.sh move <plan_b> --to done --commit
```
で`active→done`（評価全PASSが前提条件。`bucketctl_core.move`のALLOWED遷移表は`active→archive`も許すが、`disposition=completed`かつ現在バケットが`done`以外の場合は明示的に拒否する設計になっている。これは`my-brain/areas/AGENTS.md`§3の「`active→done`(完了・未評価)→`archive`(評価済みOK)」という2段階ライフサイクルと一致する意図的な仕様であり、欠陥ではない）。続けて

```
python3 <plan-ops>/planctl.py close --plan <done先>/plan.md --plans-root <repo>/plans \
  --repo-root <repo> --disposition completed \
  --human-confirmation "指揮官2026-07-16確認: 実装・レビューPASSを確認し完了と判断" \
  --reason "src/e2e4.txtの実装とレビューが完了したため" --apply
→ exit 0
```

結果: `plan.md`・`評価01.md`ともarchiveへ移動、active/doneいずれにも残らず、`終了記録.md`にcompleted・人間確認・理由が残存、完了条件は全`[x]`。archive後のplan-lint再検査も違反なし。

### 判定: **PASS**（全13チェック項目PASS）

---

## E2E(5): session finishだけではarchiveされない（Stop guardとbucketctlの双方で確認）

### 実行内容・再現コマンド

合成repoのactiveに未同期（`phase: review_passed`）の計画を1つ作成し、`hooks-registry/shared/plan-closeout/common.py`が使うrun manifest（`PLAN_RUN_MANIFEST`）を直接組み立てて、Stop hook本体を実行した。

```
PLAN_RUN_MANIFEST=<manifest.json> \
  python3 <hooks-registry>/events/session-end/guard-plan-closeout.py <<< '{"hook_event_name":"Stop","stop_hook_active":false}'
→ {"decision": "block", "reason": "plan-closeout: review_passedだが未同期。planctl apply-evaluation または planctl sync-check を実行してから終了する。"}
```
2回目（`stop_hook_active:true`）は同一Stopでの再blockを防止し`systemMessage`のみを返す。`plans/`配下の全ファイルのSHA256（相対path+内容）と`git status --porcelain`を、hook呼び出し前後で完全一致することを確認し、**Stop hook自体はバケットも終了記録も一切書き換えない**ことを直接確認した。`終了記録.md`も生成されない。

続けて、同じ状態（未同期・`終了記録.md`なし）のまま
```
<plan-ops>/bucketctl.sh move <plan_dir> --to archive --commit
→ exit 1: "bucketctl: archive を拒否: 終了記録.md が無い"
```
を実行し、**Stop hookとbucketctlという2つの独立した機構が、どちらも「セッション終了しただけ」ではarchiveを許さない**ことを確認した。

最後に`running`/`implemented`/`synced`/`blocked`の4phaseそれぞれでStop hookを呼び、いずれもblockしないこと（過剰ブロックしない）を確認した。

### 判定: **PASS**（全11チェック項目PASS）

---

## E2E(6): repo-local — Private以外の合成repoのplans/での起案→委譲→同期→doneの一巡

### 実行内容・再現コマンド

`~/Private`と無関係な合成repo（`acme-widgets`、mktemp配下）で、program.md/Programマップを一切介さず、直接プリミティブだけで一巡させた。

1. **起案**: `new-plan.sh --out <repo>/plans/planning/2026-07-16-acme-widget実装/plan.md --class 横断 --kind 新規作成` で雛形生成 → 目的・現状・方針・完了条件・実行契約（変更可能範囲: `src/widget.txt`、bareなpathで記載）を実文で埋め、`plan-lint.sh`（placeholderなし）で違反なしを確認 → `bucketctl.sh promote <dir> --to active --commit` で`planning→active`。
2. **委譲**:
   ```
   python3 <harness>/delegate.py --runtime codex --role implementer \
     --plan <plan.md> --repo-root <repo> --task-id acme-impl-01 \
     --base-commit <sha> --state-dir <repo>/.planops-state \
     --worktree-root <tmp>/worktrees --allowed-path src/widget.txt
   ```
   → `status: done`、task-scoped worktreeが`acme-widgets`本体とは別pathに作成された。続けて
   ```
   python3 <harness>/delegate.py --runtime claude --role reviewer \
     --plan <plan.md> --repo-root <repo> --task-id acme-review-01 \
     --state-dir <repo>/.planops-state --purpose "..."
   ```
   → `status: done`、`worktree_path: null`（reviewerはworktreeを作らない）、評価本文（全PASS）が`<task>-final.md`に生成された。
3. **同期**:
   ```
   python3 <plan-ops>/planctl.py apply-evaluation --plan <plan.md> --plans-root <repo>/plans \
     --evaluation <評価01.md> --result <result.json> --repo-root <repo> --manifest <impl-manifest.json>
   ```
   → 完了条件が`[x]`化、`## 実装結果`が追記、manifest phaseが`synced`へ進行。
4. **統合**: `git merge --no-ff <branch>` でworkerの実装ファイルを`acme-widgets`本体へ反映 → `git worktree remove`でcleanup。
5. **done**: `bucketctl.sh move <plan_dir> --to done --commit` → 評価全PASS済みのため成功。`plan.md`・`評価01.md`がdoneへ移動、activeには残らず、`main`は無傷、最終的に作業treeクリーン。

### 判定: **PASS**（合成repoが`~/Private`と無関係であること含め全24チェック項目PASS）

program-run（Programマップ）を経由しないため、E2E(1)(2)の欠陥Aの影響を受けない。

---

## 発見した機構欠陥

E2Eを「本物の`new-plan.sh`/`program-lint.sh`が生成・受理する正本テンプレ形式」で組み立てたところ、既存の単体テストが使う簡略化されたfixture形式では見えない、2件の機構欠陥が見つかった。いずれも**修正はせず、FAILのまま記録する**（修正判断は指揮官）。

### 欠陥A: `program_run.py::_section()` が正本テンプレの見出しを検出できない

- **対象ファイル**: `personal-os/AIエージェント基盤/agents-registry/harness/program_run.py`
- **該当箇所**: モジュール内に独自定義された `_section(lines, heading)` 関数（`_child_blocks()`経由で`子計画マップ`を、`_contract()`経由で`実行契約`を探すのに使われる）。
  ```python
  def _section(lines, heading):
      start = next((i + 1 for i, line in enumerate(lines) if line.rstrip() == f"## {heading}"), None)
  ```
  `line.rstrip() == f"## {heading}"` という**完全一致**でしか見出しを判定しない。
- **一方、`plan_lint_core.py`・`progctl_core.py`が共有する `_planops_map.find_section()` は前方一致**（`text.startswith(heading_prefix)`）であり、同モジュールの docstring 自身が「想定フォーマット」として
  ```
  ## 子計画マップ   ※ 子の状態変更と同じコミットでここを更新
  ```
  という、見出しの直後に注記が続く形を明記している。
- **実害の確認方法（最小repro）**: `new-plan.sh --program` で実際に生成した`program.md`（見出しは上記の注記付き形式そのもの）を`program_run.read_program()`に渡すと、`ProgramRunBlocked: 子計画マップに実行対象がありません`が発生する。この見出し文字列は、**本program自身（`2026-07-15-計画立案実行完了基盤/program.md`）を含め、この repository に存在する全ての`program.md`と完全に同一**である。
  ```python
  # 実行例（agents-registry/harness で python3 -c 相当）
  import program_run
  program_run.read_program(Path("<new-plan.sh --program の出力>"))
  # → ProgramRunBlocked: 子計画マップに実行対象がありません
  ```
- **影響範囲**: `program-run`（ゴールコマンド）は、現状のテンプレ・既存program.mdに対して**子を1件も検出できず、実質的に起動できない**。既存の単体テスト（`test_program_run.py`）がこの欠陥を検出しないのは、そのテストの`write_program()`ヘルパーが独自に**注記なしの`## 子計画マップ`（bare見出し）**を書いており、実際のテンプレ形式と異なるため。
- **切り分け**: E2E(1)(2)のVariant B（見出しをbareにした診断実行）では、実装→レビュー→同期→merge→cleanup→Programマップ更新のすべてが正しく完走した。欠陥は`_section()`の見出しマッチという1箇所に局所化できる。
- **推奨対応（実施はしていない）**: `program_run.py`が独自の`_section()`を再実装するのをやめ、`skills/plan-ops/scripts/_planops_map.py`の`find_section()`（前方一致）を共有利用する。
- **2026-07-16追記: 修正済み（commit `3094758`）。** `_section()`を前方一致（`line.rstrip().startswith(f"## {heading}")`）に修正し、bareな見出し（既存挙動）は維持したまま、注記付きの正本テンプレ見出しも検出できるようにした（共有ヘルパへの一本化は行わず、モジュール内で解釈だけを揃える最小修正）。`test_program_run.py`の`write_program()`ヘルパ（`ProgramRunTest`・`RealGitProgramRunTest`双方）が生成するfixtureの見出しも、正本テンプレと同じ注記付き形式へ更新した。修正前の状態へ一時的に戻すと、fixture更新後のテスト25本中11本が失敗することを確認済み（回帰検出力の検証）。

### 欠陥B: `planctl.py` の変更可能範囲パーサがbacktick付き・複数path・注記混在の実書式を解釈できない

- **対象ファイル**: `personal-os/AIエージェント基盤/skills/plan-ops/scripts/planctl.py`
- **該当箇所**: `range_items()` と `matches_range()`。
  ```python
  def range_items(value):
      return [item.strip().rstrip("/") for item in re.split(r"[,、]", value) if item.strip() and item.strip() not in {"なし", "該当なし"}]
  def matches_range(path, item):
      item = item.rstrip("/")
      return path == item or path.startswith(item + "/")
  ```
  backtickを剥がさず、区切り文字も半角/全角カンマ（`,`／`、`）だけを見る。
- **実際の`変更可能範囲:`書式**は、本program自身の既存子計画（例: `plans/02-遷移統制とplanctl同期.md`）で
  ```
  変更可能範囲: `skills/plan-ops/scripts/`（bucketctl拡張・planctl.py新設・共通core）、`skills/plan-ops/__tests__/`、`SKILL.md`・`references/script-map.md` の該当節、...
  ```
  のように、**backtick付きpath・`・`区切り・日本語注記混在**が標準形。
- **実害の確認方法（最小repro）**: 上記の実文をそのまま`planctl.range_items()`に渡すと
  ```python
  range_items("`skills/plan-ops/scripts/`（bucketctl拡張・planctl.py新設・共通core）、`skills/plan-ops/__tests__/`、...")
  # → ['`skills/plan-ops/scripts/`（bucketctl拡張・planctl.py新設・共通core）', '`skills/plan-ops/__tests__/`', ...]
  ```
  のように、backtickと日本語注記が付いたままの文字列がitemになり、`matches_range("skills/plan-ops/scripts/bucketctl.sh", item)`は**常にFalse**になる。E2E(1)の初回試行でも、子計画の`変更可能範囲: \`src/e2e1.txt\``（backtick付き・単一path）だけで`planctl apply-evaluation`が「変更可能範囲外の差分」として実装済みの正当な差分を拒否した。
- **一方 `program_run.py::_contract()` は自前でbacktickを正しく処理**しており（`re.findall(r"`([^`]+)`", scope)`で抽出してから、無ければbareパスへフォールバック）、同じ`変更可能範囲:`フィールドに対して**モジュールごとに解釈結果が食い違う**状態になっている。
- **既存テストが検出しない理由**: `test_program_run.py`の`RealGitProgramRunTest.write_program()`（実際に`planctl.py`を呼ぶテストクラス）は`変更可能範囲: src/{nn}.py`と**backticksなしで書かれており**、この欠陥を踏まない。同ファイル内のもう一方のテストクラス（`ProgramRunTest`）はbacktick付きで書いているが、Operationsを丸ごとfakeにしているため実際の`planctl.py`を経由せず、欠陥が表面化しない。
- **本E2Eでの回避**: E2E(1)以降、全ての合成fixtureで`変更可能範囲:`はbareなpath（backtickなし）で統一し、以降のE2E(2)(6)はこの回避のもとで実施した（本文冒頭「方法論」に明記）。
- **推奨対応（実施はしていない）**: `planctl.py`の`range_items()`にも`program_run._contract()`と同じbacktick抽出ロジックを持たせる（できれば両モジュールで共有関数化する）。
- **2026-07-16追記: 修正済み（commit `34b7749`）。** `range_items()`を「backtick付きpathがあれば区切り文字によらずbacktickの中身だけを項目にし、無ければ`,`／`、`／`・`区切り＋「なし」「該当なし」等の注記語除外へフォールバック」する規則へ修正し、`program_run.py::_contract()`と同じ解釈に揃えた（共有関数化はせず、両モジュールにそれぞれ実装したまま解釈だけを一致させる最小修正）。範囲検査自体は緩めておらず、範囲外パスは引き続き拒否する。`test_planctl.sh`にbacktick+`・`混在の変更可能範囲を許可するケースと、同じ範囲定義でも範囲外差分は引き続き拒否されるケースを追加した（修正前へ戻すと前者が失敗することを確認済み）。

## 運用上の注意点（欠陥ではないが記録に残す）

1. **`終了記録.md`は`close --apply`が失敗しても書き出される**（E2E(3)で確認）。既存ファイルは上書きされないため、rejectされた試行の後に正しいdispositionで再試行しても、stale recordがそのまま使われて再度拒否される。訂正時は別ディレクトリで試すか、`終了記録.md`を確認・削除してからにする必要がある。
2. **`completed`でのarchiveは`done`バケット経由が必須**（`active`から直接`completed`disposition ではarchiveできない。E2E(4)で確認）。これは`bucketctl_core.move()`の意図的な設計であり、`my-brain/areas/AGENTS.md`§3の2段階ライフサイクルと一致する。欠陥ではないが、単発planの「completed→archive」を試す際は`active→done→archive`の順で操作する必要がある。

## 次の一手

- 欠陥A・Bはいずれも子03（harness）・子02（planctl）の担当領域。修正要否・優先度の判断は指揮官へ委ねる。修正する場合は次点の直接再検証として、既存`test_program_run.py`のfixtureヘルパーを正本テンプレの見出し・変更可能範囲書式に合わせて更新すること自体もあわせて検討すると、以後この種の「テストは通るが実データでは動かない」乖離を防げる。**2026-07-16追記: 両方とも修正済み（`task/pf06`branch、commit `3094758`・`34b7749`）。詳細は本文末尾「修正後再検証（2026-07-16追記）」を参照。**
- 「単発plan→…→done」の直接プリミティブ経路（program-runを介さない`delegate.py`×2 + `planctl.py apply-evaluation`）はE2E(6)で実証済みであり、program-run側の欠陥Aとは独立に機能する。
- E2E(3)〜(6)、全lintは無条件でPASS。承認セットへ積む項目は本ラウンドでは未検出（05一括レビュー・承認セット整形は別ラウンド担当のためここでは扱わない）。

---

## 修正後再検証（2026-07-16追記）

担当: pf06（fixer・worktree `/Users/kitamuranaohiro/.cache/private-worktrees/pf06`、branch `task/pf06`）。上記で発見した欠陥A・Bを修正し、全既存テスト（新規ケース含む）とE2E系統1・2の再現手順を再実行した結果を記録する。**元のFAIL記録（上記各節）は履歴としてそのまま残し、本節は追記のみとする。**

### 修正内容

- **欠陥A**（commit `3094758`）: `agents-registry/harness/program_run.py`の`_section()`を完全一致から前方一致（`line.rstrip().startswith(f"## {heading}")`）へ修正。bareな見出し（既存挙動）は維持したまま、正本テンプレの注記付き見出しも検出できるようにした。同commitで`agents-registry/harness/tests/test_program_run.py`の`write_program()`ヘルパ（`ProgramRunTest`・`RealGitProgramRunTest`双方）が生成するfixtureの見出しも、正本テンプレと同じ注記付き形式へ更新した。
- **欠陥B**（commit `34b7749`）: `skills/plan-ops/scripts/planctl.py`の`range_items()`を、backtick付きpathがあれば区切り文字によらずbacktickの中身だけを項目にし、無ければ`,`／`、`／`・`区切り＋「なし」「該当なし」等の注記語除外へフォールバックする規則へ修正し、`program_run.py::_contract()`と同じ解釈に揃えた。範囲検査自体は緩めていない（範囲外パスは引き続き拒否）。同commitで`skills/plan-ops/__tests__/test_planctl.sh`にbacktick+`・`混在の変更可能範囲を許可するケースと、範囲外差分は引き続き拒否されるケースを追加した。
- 両修正とも、修正前の状態へ一時的に戻して該当テストが実際に失敗することを確認済み（回帰検出力の検証。欠陥Aは25本中11本、欠陥Bは新規4件中2件が失敗）。

### 全テスト再実行結果（suite別）

- `agents-registry/harness/tests/`（`python3 -m unittest discover`）: **25 tests OK**（構成は修正前と同一。fixtureが正本テンプレ形式に変わった状態で全PASS）
- `skills/plan-ops/__tests__/run.sh`: **182 pass / 0 fail**（内訳: `test_bucketctl.sh` 39・`test_plan_lint.sh` 25・`test_planctl.sh` **48**（既存44+新規4）・`test_progctl.sh` 19・`test_program_lint.sh` 17・`test_scaffold.sh` 34）

### E2E系統1・2の再現手順の再実行（合成repo）

方法論はE2E結果本文（冒頭「方法論」）と同一（`tempfile.mkdtemp()`の使い捨てディレクトリ、fake codex/claude CLI、実runtime・実secret・実Private本体・実Tursoには非接触）。今回は診断時のVariant A（正本テンプレの注記付き見出し）に加えて、**変更可能範囲もbacktick付きにした**（欠陥Bの実際の再現書式）。これは元のE2E(1)(2)がbacktick付きだと欠陥Bを踏むため意図的にbareパスへ回避していた箇所であり、欠陥A・Bの両方を同時に踏む最も厳しい構成になる。E2E駆動スクリプト本体はセッションのscratchpad配下（`pf06-e2e/e2e1_single_plan_postfix.py`・`e2e2_two_children_postfix.py`。既存の`e2e_lib.py`・fake CLI契約をそのまま再利用）に置いて実行し、worktree／repoにはcommitしていない（元のE2E(1)(2)スクリプトと同じ方針）。

**E2E(1)再実行**: `program.md`見出しを`## 子計画マップ   ※ 子の状態変更と同じコミットでここを更新`のまま、子計画の変更可能範囲を`` `src/e2e1.txt` ``（backtick付き）にして`program_run.py`を実行。
```
python3 <harness>/program_run.py --program <repo>/plans/active/2026-07-16-e2e1-単発計画-postfix/program.md \
  --repo-root <repo> --state-dir <repo>/.planops-state --run-id e2e1postfix --review-runtime claude
```
結果: `status: completed`。子01完走、Programマップ「完了」化、子計画の完了条件が`[x]`化（backtick変更可能範囲でも実差分が許可された＝欠陥B修正の直接証拠）、`評価01.md`全PASS、workerファイルが統合branchへ反映、no-ff merge記録、worktree cleanup、manifest phase=synced、作業treeクリーン、`main`は無傷。**13項目すべてPASS**（診断時のVariant Bと同じチェック項目を、今回は正本テンプレ見出し+backtick範囲という実書式そのもので確認）。

**E2E(2)再実行**: 同じ正本テンプレ見出しで、独立2子（変更可能範囲`` `src/e2e2-a.txt` ``／`` `src/e2e2-b.txt` ``、いずれもbacktick付き）を実行。
```
python3 <harness>/program_run.py --program <repo>/plans/active/2026-07-16-e2e2-独立2子計画-postfix/program.md \
  --repo-root <repo> --state-dir <repo>/.planops-state --run-id e2e2postfix --review-runtime claude
```
結果: `status: completed`、`completed: ["01", "02"]`。`implement: 01,02`（同一Waveで並列委譲）、live snapshotの相互写り込みにより2 worktreeの同時生存を直接確認、Programマップ01・02とも「完了」化、両子の完了条件`[x]`化・`評価NN.md`全PASS、workerファイル2件が統合branchへ反映、2件のno-ff merge、子ごとに別branch、両worktreeともcleanup済み、manifest phase=synced（両方）、作業treeクリーン。**全24項目PASS**。

### 判定

E2E系統1・2は、正本テンプレの見出し（欠陥A相当）と変更可能範囲のbacktick書式（欠陥B相当）を両方とも実書式のまま使う最も厳しい構成で、**修正後PASS**。診断時にFAILとして記録したVariant Aの条件は、今回の修正によって解消されたことを直接確認した。作業終了後、`task/pf06`worktreeの`git status --porcelain`が本節追記分の`2026-07-16-E2E結果.md`以外に無変更であることを確認済み（E2E駆動スクリプト自体はscratchpad配下のみに存在し、repoにはcommitしていない）。
