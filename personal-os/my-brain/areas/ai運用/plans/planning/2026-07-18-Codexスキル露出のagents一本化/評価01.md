# 評価01（ラウンド01）

- 対象計画: `2026-07-18-Codexスキル露出のagents一本化/plan.md`（R01〜R12）
- ラウンド: 01
- diff範囲: private-meta 未commit分（`global-skill-registry/scripts/link-global-skill.sh` 改修・`check-exposure.sh`/`exposure-manifest.tsv` 新規・`global-skill-registry/AGENTS.md`・`GLOBAL_AGENTS.md`）＋別repo `projects/active/仕事/AGENTS.md`・`projects/active/focusmap/AGENTS.md` 未commit分＋実symlink実測（`~/.agents` `~/.claude` `~/.codex`）＋`codex debug prompt-input`＋snapshot照合
- 規模: フル
- 評価者: impl-reviewer（Opus 4.8）

## 項目別採点

- **R01 [PASS]** `link-global-skill.sh` 差分で旧 `roots`（`$HOME/.codex/skills` 含む）が削除され、`default_windows="agents,claude,gemini-config,gemini-antigravity"` と `window_path_for_key`（codexキー無し）に置換。`~/.agents/skills` 残存・`~/.codex/skills` は露出配列から除去。
- **R02 [PASS]** `~/.codex/skills/*` 実測で正本 `AIエージェント基盤/skills/` を指すsymlinkは0件。残る5件は起業スキル（repo-local・スコープ外明記済み）、2件はscratch実dir。
- **R03 [PASS]** `codex debug prompt-input` 解析で available skill 重複ゼロ。正本Global Skillは `~/.codex/skills` から供給されず、二重登録解消。
- **R04 [PASS]** `~/.agents/skills` に kickoff/morning-routine/sns-post 不在、`~/.claude/skills` に存在（manifest claude限定が機能）。Codex漏れなし。
- **R05 [PASS]** `~/.claude/skills` の現状 readlink 一覧と snapshot が完全一致。Claude発見に影響なし。
- **R06 [PASS]** `~/.codex` 直下は config.toml・AGENTS.md・auth.json・hooks・rules・commands・sessions/state系に限定。Global Skillミラー無し。
- **R07 [PASS]** `global-skill-registry/AGENTS.md` に新露出モデル明記＋旧「OpenCodeは.agents経由」を「OpenCodeは`~/.claude/skills`と`~/.config/opencode`を読む」へ訂正。
- **R08 [PASS]** 仕事/focusmap の各AGENTS.md（別gitルート）＋GLOBAL_AGENTS.md §1 に境界（Codex skill=.agents／Codex固有=.codex／1 skill 1窓）を記載。
- **R09 [PASS]** `global-skill-registry/AGENTS.md` にCodex自動生成skillの容認方針（scratch扱い・手動移送・check-exposure警告）を明記。
- **R10 [PASS（重大な追加指摘あり）]** drift-checkは「.agentsに無いGlobal Skill」を検出（standup/inbox-triage欠落＝本変更起因でない既存未露出）。「.codex二重登録」ロジックも実装済（現在0件）。ただし追加指摘1（NFC/NFD比較バグ）により健全状態でも99件の誤検出＋exit1になり得るため、修正なしでは運用に耐えない。
- **R11 [保留]** snapshot（5窓ダンプ・`.codex`節に正本22件記録・全22件実在＝`ln -s`復元素材完備）はあるが、rollback手順を実際に検証した実行記録が計画フォルダに無いため「検証済み」を満たせず保留。
- **R12 [PASS（検証可能部分）]** 変更差分＋新規ファイルに secret/token/認証値なし。変更は未commit・push無し（計画どおり）。symlink削除の人間承認は本セッションで取得済み。

## 総合判定

**修正へ差し戻し。** ハードFAILは0だが、R11保留＋R10重大追加指摘（正規化バグ）で全PASS未達。doneゲート未達。

## 追加指摘

1. **【重大】`check-exposure.sh` L136 / `link-global-skill.sh` verify_link L113 のsymlink先比較がUnicode正規化に脆弱。** repoパスに「AIエージェント基盤」を含むため、`pwd`由来がNFDになると生文字列比較が誤判定し、既定cwd実行で正しいリンクを「symlink先不一致」と誤検出（99 issues/exit1）。修正: inode等価 `[ "$link" -ef "$source_dir" ]` または realpath/NFC正規化比較へ。
2. **【軽微】manifest配置と計画の齟齬。** plan §実行契約は `global-skill-registry/exposure-manifest.tsv` と記載だが実配置は `scripts/exposure-manifest.tsv`（AGENTS.md・両script内参照は scripts/ で内部整合）。計画本文を実配置へ揃える。

## 修正指示（→ 評価02で確認）

- R10: `check-exposure.sh`・`link-global-skill.sh` の正本一致判定を正規化非依存（`-ef`/realpath）へ。3経路（既定cwd/明示cd/絶対パス）で誤検出0・真のdriftのみ・二重登録0を再確認。
- R11: snapshotから復元する `rollback-codex-skills.sh`（既定--dry-run）を references に用意し、--dry-runで22件の復元コマンド生成と各target実在を確認。指揮官承認で「素材＋復元スクリプト完備」をもってR11可とする。
- 軽微: plan.md の manifest パス表記を `scripts/exposure-manifest.tsv` に修正。
