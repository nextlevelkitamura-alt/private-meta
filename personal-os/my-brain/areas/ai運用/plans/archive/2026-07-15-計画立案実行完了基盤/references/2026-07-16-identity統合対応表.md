親計画: ../../plans/05-既存計画とarea標準の適用.md ／ 分類: 横断 ／ 種別: 統合整理

# identity.md → AGENTS.md 統合対応表（子05-B ai運用pilot）

出所: `personal-os/my-brain/areas/ai運用/identity.md`（統合前の全文）
統合先: `personal-os/my-brain/areas/ai運用/AGENTS.md`（本対応表作成と同時にcommit済み）
参照した推奨構成: `../references/2026-07-15-計画実行基盤/01_計画実行基盤_現状調査と再設計.md` §4.2（identity.mdの内容は `AGENTS.md` の 目的/判断基準/置くもの/置かないもの/計画ルーティング へ統合する）

## 前提

- `identity.md` 自体は削除しない（削除は承認セット候補。本対応表と`AGENTS.md`§7-5・§8-6が「削除承認待ちの凍結状態」であることを明記）。
- 統合後、`identity.md` に書かれていた内容と同じ主張を`AGENTS.md`側に一本化した。今後の判断基準追記は`AGENTS.md`へ行い、`identity.md`は追記凍結する（`AGENTS.md` §7 書かないもの item 5）。

## 対応表（identity.md全項目 → AGENTS.md統合先）

| identity.md 項目（原文の節） | 統合先（AGENTS.md） | 統合方法 |
|---|---|---|
| `# ai運用 Identity`（表題） | `# ai運用 Area`（既存表題をそのまま正本に） | 表題は元々AGENTS.md側が正本。identity.md表題は吸収（新規追記なし） |
| `## 目的` 段落1「personal-os 基盤、Global Skill、repo、loop、CLI（Orca など）の運営に関する考えと計画を整理する。」 | `## 1. 目的` 段落1 | 一字一句そのまま移設 |
| `## 目的` 段落2「このareaは「考え・計画」を持つ。実装の正本は持たない。」 | `## 1. 目的` 段落2 | 一字一句そのまま移設 |
| `## 実装正本との違い（混同しない）` item1「このarea（`my-brain/areas/ai運用/`）: 基盤をどう運営するかの考えと計画。」 | `## 1. 目的` > `### 実装正本との違い（混同しない）` item1 | そのまま移設（見出しレベルのみ `##`→`###` に変更） |
| 同上 item2「実装正本（`personal-os/AIエージェント基盤/`）: Skill本文、registry、logs、runtime露出の正本。」 | 同上 item2 | そのまま移設 |
| 同上 item3「名前が似ているが別物。計画はこのarea、実装はAIエージェント基盤に置く。」 | 同上 item3 | そのまま移設 |
| `## 判断基準` item1「具体的な実行に進む前に、目的、前提、完了条件を明確にする。」 | `## 2. 判断基準` item1 | そのまま移設（AGENTS.mdに同名セクションが無かったため新設） |
| 同上 item2「人間が判断すること、AIに任せること、repoやSkillやloopに落とすことを分ける。」 | `## 2. 判断基準` item2 | そのまま移設 |
| 同上 item3「計画本文は `plans/<バケット>/<計画名>/plan.md` を正本にする（状態はバケットで持つ）。」 | `## 2. 判断基準` item3 | そのまま移設。`../AGENTS.md`（areas共通規約）と同旨だが、area固有の判断基準としても明記する価値があるため保持 |
| 同上 item4「実装正本（Skill本文、registry、logs）はこのarea内に増やさない。」 | `## 2. 判断基準` item4 | そのまま移設。既存AGENTS.md `## 4. 置かないもの` item1と同旨（重複だが「判断基準」と「置かないもの」は角度が異なるため両方に残す） |
| `## 置くもの` item1「personal-os 自体の構造・運用ルールの検討。」 | `## 3. 置くもの` item5 | 既存item5「Personal OS基盤・横断repo・Global Skill・repo・loopの計画は、このareaを正本にする。」に統合済みの内容と実質重複していたため、item5の文言へ明示的に織り込み、新規文としては複製しない |
| 同上 item2「Global Skill、repo、loop、CLI（Orca など）の企画・計画。」 | `## 3. 置くもの` item5 | 同上。item5の文言に統合済み |
| 同上 item3「旧計画ディレクトリから移行済みの基盤・横断計画。」 | `## 3. 置くもの` item5 ＋ `## 5. 計画ルーティング` item8 | item5の文言に統合し、移行状況の参照先（`plans/archive/2026-06-29-plans廃止とarea一本化/plan.md`）は既存の計画ルーティングitem8が既に持っていたため二重記載しない |
| `## 置かないもの` item1「secret、token、credential、環境変数の値。」 | `## 4. 置かないもの` item3 | 既存AGENTS.md item3と一字一句同一のため新規追記なし（既存のまま） |
| 同上 item2「Skill本文、registry、logs（`../../../AIエージェント基盤/` が正本）。」 | `## 4. 置かないもの` item1 | 既存AGENTS.md item1と同旨のため新規追記なし（既存のまま） |
| 同上 item3「実装repo本体（`/Users/kitamuranaohiro/Private/projects/` に置く）。」 | `## 4. 置かないもの` item2 | 既存AGENTS.md item2と同旨のため新規追記なし（既存のまま） |

## 網羅確認

- identity.md の全見出し（目的／実装正本との違い／判断基準／置くもの／置かないもの）と、配下の全13項目（目的2段落＋実装正本との違い3項目＋判断基準4項目＋置くもの3項目＋置かないもの3項目、表題を除く）が上表で統合先を持つ。
- 「計画ルーティング」に対応するidentity.md独自の節は無い（identity.mdは元々ルーティング規約を持たず、既存AGENTS.mdの旧§3「このarea固有の計画配置」がその役割を担っていた）。統合後は `## 5. 計画ルーティング（このarea固有の計画配置）` としてそのまま維持し、新規統合対象は無い。
- 統合後、AGENTS.md側に残る重複は無い（置くもの/置かないものは統合前から既存AGENTS.mdと内容が重なっていたため、新規複製ではなく既存文言への织り込み・確認に留めた）。

## identity.md自体の扱い

- 本pilotでは `identity.md` の本文を一切変更していない（変更可能範囲に含まれないため）。
- 統合が完了したことで `identity.md` は内容として`AGENTS.md`の重複コピーとなった。削除の是非は承認セット（`承認セット.md` 子05-B候補）で人間確認を得てから実行する。
- 削除実行時に合わせて更新が必要な既存参照（壊れるconsumerの一覧）は `2026-07-16-旧path参照grep結果.md` を参照。
