対象計画: plan.md ／ ラウンド: 01
diff範囲: `outputs/` 廃止、`知識/`・`explain/` への移設、規約・`.gitignore`・html Skill導線の変更 ／ 規模: フル ／ 評価者: /root/outputs_review

# 評価01: outputsを知識とexplainへ再編

## 項目別採点   ※ plan.md「完了条件（レビュー項目）」と同順・全項目を列挙

- [PASS] `../../AGENTS.md` §1 のarea標準に `知識/`(md恒久知識) と `plans/<計画>/explain/`(人間向けHTML) が明記され、`知識/` の「置くもの/置かないもの」(未確定の考えはidentity.md/方針へ)が書かれている。
  根拠: `areas/AGENTS.md` §1 と `areas/ai運用/AGENTS.md` §1。
- [PASS] `GLOBAL_AGENTS.md` §3 が計画内 `explain/` サブフォルダを許容し、§3(md隣接HTML)と §4(outputs成果物)の適用境界が矛盾なく1つに統合されている。
  根拠: `AIエージェント基盤/GLOBAL_AGENTS.md` §3–4。
- [PASS] `AIエージェント基盤/skills/html/` の create-html / meta-explain に、ディスク保存先を §4＋本方針へ委譲する導線が1ホップで足されている(規約本文の複製が無い)。
  根拠: `skills/html/workflows/create-html.md` と `meta-explain.md` の保存先導線。
- [PASS] ai運用 直下の可視エントリが area標準＋`知識/`＋`決定ログ.md` に収束し、`outputs/` が消えている。
  根拠: `areas/ai運用/outputs` が存在しない。
- [PASS] `outputs/2026-07/` の旧5枚が、対応計画の `explain/` か `知識/`(md化) へ移設され、現在の保存規約・実ファイル・運用導線に旧 `outputs/` パス参照が残っていない。
  根拠: 旧5枚は `知識/*.html` 2枚と各計画の `explain/` 3枚へ移設済み。知識MDはstage済みでHTMLとbasename一致。
- [PASS] `plans/active/*/` の散在HTML4枚が各計画の `explain/` に移設され、命名が統一されている。
  根拠: `plans/{active,planning,paused}` 配下で `explain/` 外のHTMLは0件。
- [PASS] `.gitignore` の ai運用 ブロックが `知識/**` と `plans/**`(explain含む)を許可し、旧 `outputs/**` 許可行が新構成に追従している。
  根拠: `.gitignore` のai運用ブロックと `git check-ignore` の結果。
- [PASS] work/money/health に同じ標準と `.gitignore` 許可が反映されている。
  根拠: 各areaの `AGENTS.md` と `.gitignore` の `知識/**` 許可。
- [PASS] 2026-07-13 決定ログに、規約変更(area標準・§3/§4・保存先)の1ブロックが追記されている。
  根拠: `areas/ai運用/決定ログ.md` の #20。
- [PASS] secret混入なし・正本本文の二重管理なし。
  根拠: 新規MD・規約差分に認証値はなく、知識MD/HTMLはbasename一致。

## 総合判定

全PASS。`知識/` の未追跡MDと旧規約を現在形で記したpaused計画HTMLの2点は、レビュー指摘後に修正し、再確認でPASSとなった。完了可。
