親計画: ../program.md ／ 分類: loop ／ 種別: 新規作成

# 実行レーン自動化とデイリー自動ログ

## 引き継ぎ（新セッションはここを最初に読む）
- 読む順: ① この plan.md → ② `/Users/kitamuranaohiro/Private/personal-os/AIエージェント基盤/loops-registry/ai-jobs/AGENTS.md`（実行レーン契約の正本＝6フォルダ・mv claim・レビューpair・worktree削除）。この2つで足りる。
- 現在地: **子04 実装7項目すべて完了・統合・push済**（基盤 `codex/align-agent-registries`＝③④`8cacc53`/⑥`ac275b3`/⑤`8a3ed7e`＋dispatcher AIJOBS_RUN・地図 `3ec0b03`／~/Private plan）。①捨て検証32/32・③dispatcher実機空打ち＋test18/18・④test・⑤hook sandbox＋claude実機end-to-end・⑥冪等実走、すべて独立検証済。残るは**有効化（人間ゲート）**のみ。
- 次の一手（人間ゲート）: (a) ⑤hook を `~/.claude/settings.json` に登録＝記録開始（README スニペット）／(b) dispatcher の launchd 有効化＋**実ワーカー1枚の headless 実走スモーク**（claude/codex CLI フラグ実機確認・費用）／(c) 夜loop⑥の 23:30 スケジュール登録。※(b) は実走スモーク後に有効化。**Orca 監督(program B)は別計画で本格化**（今回 Orca 経由の worker 起動・監視・枠切れ引き継ぎを実証済）。
- 別件の最優先（ハーネス全体）: 3年/年間ゴールが「未確定」。逆算が活きる前提なので、人間が的を埋めるのが先決（子01）。
- 進め方: 実装はコード作業なので Orca か新チャットで進める。

## 目的
ai-jobs 実行レーン（投入→実装→レビュー→完了）を実際に回し、その実績と Claude/Codex の対話作業を今日のデイリーへ自動集約する。手作業の記録と「次に何をするか」を、規約・スクリプト・loop へ移す。子01-03（縦ladder／ダッシュボード／朝夜ルーティン）に、実行と記録の自動化を足す。

## 現状
- ai-jobs の6フォルダ契約は確定し正本反映済（`/Users/kitamuranaohiro/Private/personal-os/AIエージェント基盤/loops-registry/ai-jobs/AGENTS.md`）。`reviewing/` 追加・レビュー claim・合格後のみ done・worktree 削除タイミングまで。`reviewing/` 実体も作成済。
- `mv` の掴み／二重実行防止はスモークテストで確認済（捨て検証・本物の ai-jobs は未使用）。
- **①完了**: `jobctl.sh` に `take`（review→reviewing・アトミック）を追加し、`done` 入口を `running|reviewing` に整合（review→done 禁止＝契約§3.3）、`back` に reviewing を追加。捨て検証 32/32 PASS。SKILL.md と ai-jobs/AGENTS.md の動詞一覧も同期済。
- **②完了**: worker プロンプト型 `loops-registry/references/worker-prompt.md` 新規（起動側 claim 前提・worktree・完了条件・review へ渡すの単一責務）。sandbox で spawn した claude worker が card 1枚を review まで実行、レビュアーが take→独立検証→done で1周を実測（実 repo/ai-jobs 未使用）。
- **③④⑤⑥⑦完了・統合済＝実装7項目コンプリート**（dispatcher/stale＝`loops/ai-jobs-dispatcher/`＋`AIJOBS_RUN`印付け、夜loop＝`loops/daily-digest/`、⑤hook＝`hooks/session-daily-log/`、template＝Phase0）。残りは有効化（人間ゲート）のみ。
- 3年/年間ゴールが「未確定」（子01の宿題）。逆算が活きる前提。

## 方針（確定した設計。詳細契約は ai-jobs/AGENTS.md が正本。ここは重複させず要点のみ）
- 状態＝フォルダ位置＝ロック。掴むのは `mv`（OSアトミック）。カードに `状態:` を書かない。
- 完了＝「完了条件（カードに先に明記）＋ 独立レビューが満たすと判定」。worker の自己申告では完了にしない。review は実装と別セッション。
- 記録の住み分け: dispatched（run-card 経由）はカードが記録／ad-hoc（対話セッション）は hook が記録。同じ行を二重に書かない。
- 起動2レーン: ① headless（dispatcher が自動起動・同時数 cap あり・夜loop 等の低リスク）／② 見える（Orca ターミナルを手で立ち上げ・人間が介入しうる計画実行・cap は監督の注意力）。両レーンが同じ ai-jobs を共有。
- hook ＝軽い記録係（事実1行のみ・secret 不記載・高速）。loop ＝重い処理係（要約・整合チェック）。
- worktree はレビュー合格＆反映後に削除（reviewing 中はレビュアー／差し戻しが使う）。

## 実行計画（並列・Phase 0/1/2）
- **Phase 0（完了）**: `## ログ(自動)` 形式契約を固定＋⑦template＋朝ルーティン反映＋フォルダ名予約。横断依存を除去。
- **Phase 1（完了・統合済）**: ③④(headless dispatcher/stale) ‖ ⑥(夜loop) を Sonnet5 で並列実装→独立レビュー→`codex/align-agent-registries` へマージ（`8cacc53`/`ac275b3`）。A:test 18/18・実機空打ちOK／B:冪等・人間行保護 実走OK。両loop停止。
  - ③④ → branch `feat/ai-jobs-dispatcher` ／ worktree `~/worktrees/ai-jobs-dispatcher`（基盤repo・base=`codex/align-agent-registries`）。コード置き場 `loops-registry/loops/ai-jobs-dispatcher/`。
  - ⑥ → branch `feat/ai-jobs-daily-digest` ／ worktree `~/worktrees/ai-jobs-daily-digest`（同base）。コード置き場 `loops-registry/loops/daily-digest/`。
- **Phase 2（単独）**: ⑤hook（env `AIJOBS_RUN` 継承は実測・~/.claude 設定は追記のみ）。branch `feat/ai-jobs-hook`。Phase1統合後。
- **共通ガード**: 共有正本（`jobctl.sh`/`worker-prompt.md`/`ai-jobs/AGENTS.md`）と 基盤`AGENTS.md`フォルダ地図・本plan は**ワーカー改変禁止**（統合時に私が1回更新）。launchd有効化＝人間ゲート。

## ログ(自動) 形式契約（⑤hook・⑥夜loop・⑦template 共通）
デイリー（`ゴール/デイリー/<年>/<月>/<日>.md`。雛形 `ゴール/templates/デイリー.md`）で AI が触ってよいのは**ペアのマーカー内側だけ**。人間の手書きは常にマーカー外・AIは消さない。
- `## ログ(自動)` `<!-- auto:log:begin/end -->` … **hook(⑤)** がセッション終了時に1行 upsert。形式 `YYYY-MM-DD HH:MM JST | cwd=<path> | repo=<名> | branch=<名> | dirty=<数> | commits=<short-sha,...> | session=<id> | transcript=<path>`（cwd が git 配下でなければ repo/branch/dirty/commits を省く＝後方互換）。**載せるのはポインタ／メタのみ**: commit は short-sha（＝git へのポインタ）で、本文・差分・secret は書かない。commit 件名などの本文は夜loop(⑥)が git から解決して `auto:done` に出す。commits はセッション開始（transcript 先頭 timestamp）以降に cwd の repo で積まれた分。同一 `session=<id>` は重複させず更新。`AIJOBS_RUN` では書かない。
- `## できた` `<!-- auto:done:begin/end -->` … **夜loop(⑥)** が `## ログ(自動)`（各行の repo/branch＋commit sha を git で**件名解決**）＋`ai-jobs/done` から実績要約を**再生成**（冪等・決定的）。transcript 本文の自然文要約は将来拡張。人間の `- ` 行は消さない。
- `## 逆算チェック` `<!-- auto:align:begin/end -->` … **夜loop(⑥)** の逆算補助（任意）。人間の回答は消さない。

## 実装手順（[担当program]を明記。重複させず backlink で束ねる）
1. [A] ✅ `jobctl.sh` に `take`（review→reviewing）追加＋`done` を契約整合（review→done 禁止）。plan-ops・基盤 `ce3893f`。捨て検証 32/32。
2. [B] ✅ worker プロンプト型 `references/worker-prompt.md`（起動側 claim 前提・worktree・完了条件・review へ渡す）。sandbox で1枚実走1周。基盤 `86174ab`。
3. [B] ✅ dispatcher（headless限定・cap・claim=jobctl経由・worker型を渡す）。`loops/ai-jobs-dispatcher/`・merge `8cacc53`・test 18/18＋実機空打ちOK。**有効化前に実ワーカー1枚の実走が必要**（未実施）。見えるレーン(Orca)は program B。
4. [B] ✅ stale 回収（running/reviewing の ctime>閾値を `jobctl back`。既定45分・env上書き）。dispatcher tick 内＋単独実行可・冪等。
5. [C] ✅ hook `hooks/session-daily-log/`（Stop・`AIJOBS_RUN`抑止=env継承を実機実測・auto:logへ session 単位 upsert・人間行保護・no-op安全）。dispatcher が `AIJOBS_RUN=1` を付与。sandbox＋claude実機で検証。~/.claude 登録は人間ゲート。Codex hook は今回不要。基盤 `8a3ed7e`/`3ec0b03`。
6. [C] ✅ 夜loop `loops/daily-digest/`（auto:log＋done card から auto:done/align を冪等生成・人間行保護・secret安全）。merge `ac275b3`。**現状は決定的集計のみ＝AI自然文要約は TODO**（transcript読解は未実装）。
7. [C] ✅ デイリー template に `## ログ(自動)`＋`auto:log/done/align` マーカーを追加、朝ルーティンに前日デイリー参照を1行追加（Phase 0 で完了）。

## 完了条件（レビュー項目）
- [ ] `jobctl.sh` で `review→reviewing` をアトミックに掴める（2回目の同 claim が失敗する）。
- [ ] 1枚の run-card が worker 起動→実装→`review`→`reviewing`→（合格）`done` を実走で1周し、`done` には reviewing を通った後だけ入る。
- [ ] dispatcher が ready 複数枚に対し 1card=1worker・同時数 cap を守って起動する（cap 超過分は ready に残る）。
- [ ] running/reviewing に stale が出たら一定時間で ready へ戻る。
- [ ] Claude 対話セッション終了後、今日のデイリー `## ログ(自動)` にポインタ1行（cwd/session_id/transcript・時刻）が upsert され、secret・本文が混入しない。`AIJOBS_RUN` のジョブセッションでは書かれない。
- [ ] 夜loop が `## ログ(自動)` と done カードから `## できた` ＋逆算チェックを生成し、再実行で二重生成しない（人間の行を消さない）。
- [ ] デイリー template に `## ログ(自動)` があり、朝ルーティンに前日参照 step がある。

## 関連（重複させず backlink）
- 実行レーン契約の正本: `/Users/kitamuranaohiro/Private/personal-os/AIエージェント基盤/loops-registry/ai-jobs/AGENTS.md`（6フォルダ・mv・レビュー pair・worktree 削除）。
- loop 起動標準: `/Users/kitamuranaohiro/Private/personal-os/AIエージェント基盤/loops-registry/references/loop-runbook.md`（launchd＋dispatcher＋runner 2系統・stale 閾値）。
- program A「計画親子層」= `../../2026-06-30-計画親子層の導入/program.md`（jobctl／run-card 共通契約＝A-03）。
- program B「OrcaCLI複数エージェント運用」= `../../2026-06-29-OrcaCLI複数エージェント運用/plan.md`（監督・worktree・worker_done・dispatcher・closeout）。
- 子01-03（ゴールladder／ダッシュボード／朝夜ルーティン）。夜loop は朝夜ルーティンの夜側を自動化する（03「skill化は子04で判断」への回答＝loop 化する）。

## リスク
- env 継承（`AIJOBS_RUN`）が公式に保証されない → hook 実装時に実測。NG なら marker／cwd 判定に fallback。
- 無人編集（夜loop）の冪等性。マーカーで囲み人間の行を保護する。
- secret／token をカード・ログ・要約に出さない。
- Codex は別 hook 系統。本計画は Claude 対話を主とし、Codex は要否調査から。
