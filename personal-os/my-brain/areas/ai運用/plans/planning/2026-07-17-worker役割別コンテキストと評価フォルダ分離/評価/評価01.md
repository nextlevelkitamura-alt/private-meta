対象計画: plan.md ／ ラウンド: 01
diff範囲: 6caf9b1..cb7e251 ／ 規模: ライト ／ 評価者: impl-reviewer(独立サブエージェント)
※ pilot: 本評価は新標準の置き場（<計画>/評価/）に保存された第1号。レビュアーは採点前に レビュー/共通.md を読了。

# 評価01: worker役割別コンテキストと評価フォルダ分離

前提確認: HEAD=cb7e251 と実測環境の一致を確認済み。

## 項目別採点

- [PASS] `new-plan.sh` のprogram生成を機械実行すると、出力先に program.md・実装/共通.md・レビュー/共通.md・評価/（.gitkeep）が生まれ、全てgit追跡可能
  根拠: mktempの合成git repoで実行→4点生成をfindで確認、`git add` で全て追跡可能（A）。

- [PASS] program配下の子への委譲で、implementerパケットが「AGENTS → program.md → 実装/共通.md → 子計画 → references」の順で出力され「レビュー/共通.md」の文字列を含まない。program無し（単発plan）では共通.md行が出ず従来のまま（両方テストで固定）
  根拠: 合成programで `delegate.py --role implementer --dry-run` の実パケットが宣言順（3=実装の共通コンテキスト絶対path）・`grep -c 'レビュー/共通.md'`=0。単発planは従来5行・共通.md 0件。test_delegate.py の新テストが順序・混入なし・単発非回帰をassert（11件OK）。

- [PASS] 同条件のreviewerパケットが「AGENTS → program.md → レビュー/共通.md → 対象計画の完了条件 → diff」で出力され「実装/共通.md」を含まない。explorerは従来どおり（テストで固定）
  根拠: 実パケットで宣言順（4=対象計画の「完了条件（レビュー項目）」・5=実装diff）・`grep -c '実装/共通.md'`=0。explorerは従来のまま。同テストで固定。

- [PASS] program_run の implement/review が program_path を渡し、パケットの「親program」が実pathになり、同一task_idのmanifestも最終的に実pathを保持する（None欠落の解消をテストで固定）
  根拠: implement/review が実在確認つきで program_path を渡す（diff確認）。real-gitテストが完走後manifestの `program_path == str(program.resolve())` をassert（15件OK）。

- [PASS] (a)評価・修正の 評価/ 保存と規約準拠名・commit_sync追従 (b)bucketctl の 評価/→直下 両対応（test_bucketctl.shで固定） (c)program-lint の旧配置除外維持＋役割別共通md存在チェック (d)実在検証: 2026-07-15-計画立案実行完了基盤 lint違反0・閉鎖ゲート判定不変
  根拠: (a) next_evaluation_path が `<program>/評価/{子stem}-評価{RR:02d}.md` を返し、テストが新配置生成＋旧path不生成＋差し戻し時の評価01/02連番をassert。commit_syncはchild_evaluations（新→旧→旧命名legacy）を反復し、real-gitテストで実行後worktreeクリーン。(b) python直実行の実測: pilot合成（評価/評価01.md全PASS＋子評価FAIL混在）→passes=True（混同なし）／旧配置→True／評価/空→直下フォールバック。実pilot（計画レビュー01.mdのみ）→False（レビュー前として正・自己評価に誤認しない）。test_bucketctl.sh (i) 追加・(h)不変・suite 189/0。(c) 検査7追加（フォルダ実在×共通.md欠落のみ違反）・旧配置除外は無変更・test_program_lint.sh (f) で両方向固定。(d) 実programでlint「違反なし」exit 0・evaluation判定は新旧ロジックとも同一。

- [PASS] manifest schema・manifest.py・planctl MANIFEST_TYPES・plan-closeout MANIFEST_REQUIRED に差分がない（新フィールド不追加の確認）
  根拠: `git diff 6caf9b1..cb7e251 --stat` が4対象とも空（対象の実在をlsで確認済み・空振りではない）。

- [PASS] hooks登録（~/.claude/settings.json・hooks-registry/codex/hooks.json）に差分がない
  根拠: 該当範囲のgit log/diff 0件。settings.jsonのmtimeは実装窓より前。

- [FAIL] 既存テスト（harness・plan-ops・plan-closeout・session-board）が全緑＋新分岐のテストが追加されている
  根拠: harness 26・plan-ops 189/0・plan-closeout 62/0・session-board python 7本・test-session-board.sh 80/0 まで緑。しかし **test-shims.sh が 56 PASS / 3 FAIL**（「ミラーは2行」「計画時は3行」「D9 記入後は2行」）。新分岐テストの追加は確認済みだが「全緑」が実測で満たされない。
  切り分け: 3件は本diff起因ではない。common.py の「レビュー宣言を確認…」行（e7bc9c5・2026-07-16、base 6caf9b1 の祖先）追加時にunit側test_common.pyだけ更新され、E2E側test-shims.shの行数期待（2/3/2行）が未追従。本diffのsession-board変更は0ファイル。実装者申告「shell 2本 緑」はtest-shims.shについて再現しない（実装者の実行がパイプでexit codeを潰した誤測定）。

- [PASS] areas/AGENTS.md の評価・修正文書節が新規約（programは評価/・単発は隣接・旧配置は読み取り互換）を宣言し、plan-registry側の委譲記述と矛盾がない
  根拠: 命名と置き場の改定＋program物理構成への追記を確認。plan-registry側は置き場をareasへ委譲するのみで競合なし。plan-create-review workflowも同一規約へ更新済み。

## 追加指摘（項目外・非ブロッキング）

- next_evaluation_path のRR採番は旧命名legacy `評価NN.md` を数えない。旧programの子を新フローで再レビューするとRRが01から再始動する（衝突なし・commit_syncは両方拾うため実害は限定的）。採番意図との整合は指揮官判断で明確化を推奨。
- 実装者申告「全テスト緑」は test-shims.sh について不正確。実装結果への記録時に訂正が必要。

## 総合判定

**FAILあり → 修正へ**（9項目中 8 PASS / 1 FAIL=項目8）。FAILの原因は本diffの実装不良ではなく基底以前からの test-shims.sh 期待値更新漏れ（既存不整合）だが、項目文言「全緑」は実測で満たされないため差し戻す。

## 修正指示ドラフト（項目8）

- 対象: hooks-registry/shared/session-board/tests/test-shims.sh 行48・57・116
- 今の状態: ミラー行数の期待が「レビュー宣言」行追加（e7bc9c5）前の 2/3/2 のままで、実出力 3/4/3 に対し3 assertionがFAIL。基底6caf9b1でも同結果。
- 期待する状態: HEADの正仕様（test_common.py (c) と同じ「+レビュー宣言行」）へ追従し、shell 2本を含む全suiteが全緑。
- 修正の方向: 期待値を 3/4/3 へ更新し、「レビュー宣言を確認」行の存在assertを追加（行数だけの盲目更新を避ける）。session-boardは本計画の変更対象外のため、指揮官が (i)修正01でスコープ拡張を明記して同計画内で直す か (ii)別サクッと修正へ切り出す かを裁定し、評価02で再実測する。
