# 実演例3: フル（合成例）

## 入力

- やりたいこと: 「orca-cockpitの権限モデル（役割別 `.claude/settings.json` 配布）を複数repo横断で見直したい」
- 対象repo見当: 不明（横断・複数repoにまたがる）

## 4判断

1. **規模**: 権限モデルの見直し＋各repoへの適用が独立に卒業する子計画2本以上（設計本体＋repo別適用ロールアウト）に割れる見込み → **フル**。
2. **経路**: 対象repo=横断のため「repo無し（ai運用領域）」扱い → 計画置き場=`~/Private/personal-os/my-brain/areas/ai運用/plans/active/`（ai運用/AGENTS.md §3）。担当指揮官=ai運用プロジェクトの指揮官（複数repoにまたがるが、決定ログ#3の原則どおり1プロジェクト1指揮官に一本化。repo別ロールアウトは子計画として同じ指揮官の下でレーン分割）。
3. **起動形**: 計画がまだ子計画文書として固まっていない（企画〜計画段階から始める）→ **3ペイン例外**（計画+監督ペインを追加、構成カードで明示宣言）。
4. **モデル**: 問い「このレーンが遅いと誰が待つか」→ 各repoへの適用ロールアウト（他レーン）がこの設計待ちでブロックされる直列ボトルネック → 実装をopus4.8 fastへ格上げ（構成カードで宣言）。レビューはxhighのまま。

## 出力

### 構成カード

```
── コックピット構成カード（起動前確認・未起動）──
レーン(title): cockpit権限モデル横断見直し
repo/branch  : AIエージェント基盤 / feature/cockpit-permission-model   (base: main)
規模/scope   : フル
ペイン構成:
  左   計画    claude sonnet5          権限: read-allowlist
  右上 実装    claude claude-opus-4-8 (fast)   権限: acceptEdits
  右下 レビュー codex gpt-5.5 xhigh    権限: read-allowlist
権限方式     : 役割別 .claude/settings.json を worktree に配布
起動コマンド : cockpit.sh up --repo AIエージェント基盤 --branch feature/cockpit-permission-model --pane "計画:claude:sonnet5" --pane "実装:claude:claude-opus-4-8" --pane "レビュー:codex:gpt-5.5:xhigh"
※これは提示のみ。人間のOK後に up を実行する。モデル格上げ（opus4.8 fast）はクリティカルパス例外として明示宣言。
```

### 計画スケルトン（`plans/active/2026-07-02-cockpit権限モデル横断見直し/plan.md`）

```
分類: skill ／ 種別: 既存改善 ／ 規模: フル

# cockpit権限モデル横断見直し

## 目的
## 現状
やりたいこと原文: 「orca-cockpitの権限モデルを複数repo横断で見直したい」
## 方針
未確定（企画〜計画段階から3ペイン運用で詰める）
## 完了条件（レビュー項目）
- [ ] 未確定
```

人間へ構成カード＋計画スケルトンを提示し、OKを得てから `up` を実行する（フルは事前ゲート）。
