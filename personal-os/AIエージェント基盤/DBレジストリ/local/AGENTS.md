# DBレジストリのローカル領域

このフォルダは、このMacだけで必要になる補助設定の隔離場所。任意の実体ファイルは`.gitignore`で追跡しない。

## 許可するもの

- 一時的なCLI補助設定
- 値を含むマシンローカルファイル

## 禁止するもの

- 値を含むファイルのcommit、共有、別repoへのコピー
- `.env.example`など追跡されるsampleへの値の追加
- providerの現在状態・接続URL・認証出力の保存

値が必要な作業は、作業後に対象サービス側でreadbackし、値そのものではなく成功・失敗だけを作業記録へ残す。
