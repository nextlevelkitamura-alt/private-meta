対象計画: `plans/05-タスク入れ子と2層チェック.md`（program「当日ボードSQL化」子05）／ラウンド: 評価01／diff範囲: focusmap 21c0a066→194e3b6e ＋ ~/Private 子05コミットのみ（8df1279・7654866・22b3563）／規模: フル／評価者: reviewer（Opus・実装と別セッション）／評価日: 2026-07-18

## 再実行した検証
- focusmap `npm run build`: 通過。session-board pyテスト全ファイル: test_todo_steps 37ほか全FAIL0。shell: 80＋60 PASS
- secret/token/DB-auth混入: 子05全diffにマッチなし
- 実skill/loop正本への `board_route: routine` 追記なし（人間ゲート未先行を実確認）。migration適用痕跡・push痕跡なし
- common.py: 子05の再開注入変更と子06変更は衝突・打ち消しなし

## 項目別採点

**1. ステップ入れ子＋タスク別%・0件=計画待ち・skipped分母除外・%はSQL集計のみ [PASS]**
todo-steps.ts getStepAggregatesForDate が NULLIF で skipped を分母除外・0件は null。進捗値カラムは存在せず保存なし＝SQL導出のみ。board-status.ts deriveBoardStatus で total0→計画待ち。※DB実挙動はmigration適用後に確認（保留扱いの解除条件参照）。

**2. 状態ラベル常時1個・レビュー待ちSQL導出・複数セッションでも実行中1個 [PASS]**
deriveBoardStatus は必ず1ラベル。`total>0 && pending===0` を集計から導出（保存値なし）。集計はtodo単位のため複数セッションでも1ラベル。

**3. ステップ✔=board.py経由AI・見出し完了=人間タップのみ・undo=DB正式遷移 [PASS]**
stmt_step_status は `WHERE status != 'done'`（過去done行UPDATE禁止の機械保証）。completeAiTodoHeading は人間タップ経路のみ・completed_by='human'。AIが見出しを完了させるコマンドは存在しない。undo は completed_by='human' 限定の正式遷移。

**4. 質問=選択肢＋自由入力・DB保存→再開時注入・人間ゲート質問は回答UIなし [PASS]**
stmt_ask（選択肢最大3＋free/gate）。QuestionAnswer が選択肢＋自由入力を描画。回答は question_gate=0 限定で保存、⏸→🟢再開時のみ注入。questionGate=1 は回答UI非描画で「セッションで明示承認」文のみ。

**5. 完了済みへのfix追記・見出し自動で戻らず手直し中・付け替え1タップ [FAIL]**
保持・自動reopen禁止・「手直し中」表示はPASS相当。ただし**付け替え1タップUI（FixReattach）が openタスクの AiTodoCard にのみ結線され、完了済みを描く DoneTodoRow に無い**。完了済みタスクに自動紐付いたfix行を付け替える経路がUIに存在しない（server action reattachFixStep 自体は存在・kind='fix'ガードも正しい。UIの結線漏れ）。→ 修正01.md

**6. board_route宣言の定型のみ⚙直行・宣言なしは自動完了しない [PASS]**
flow-done は正本frontmatter照合・未宣言は not-routine で不送信。第2台帳なし（キャッシュファイル無し）。※実挙動は宣言追記（人間ゲート）後に確認。

**7. 375px崩れなし・画面復帰時最新化 [保留]**
画面復帰の最新化はコードでPASS（visibilitychange＋focus で refresh・ポーリング10s）。375px目視は未実施・記録なしのため規約どおり保留（原則は親の最終一括に束ねる）。

## 総合判定
**FAIL 1（項目5）・PASS 5・保留 1（項目7目視）→ 修正01へ差し戻し（ラウンド1/上限2）**

## 保留の解除条件
- DB実挙動（%導出・NOT EXISTSガード・undo・回答消費・flow-done）: migration2本の人間ゲート適用後にローカル3001で実データ確認
- 項目6実挙動: 実正本への board_route 1本追記（人間ゲート）後に⚙直行確認
- 項目7: ローカル3001の375px目視を記録付きで実施（親の最終一括に束ねる）
