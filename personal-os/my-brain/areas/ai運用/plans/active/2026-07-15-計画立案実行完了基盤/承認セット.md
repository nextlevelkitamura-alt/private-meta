# 承認セット（計画立案・実行・完了基盤）

program-runと各子が蓄積し、子06（E2Eと承認セット）が最終的に1文書へ整形する（`program.md` 完走スキーム参照）。
このファイルは蓄積用のワーキング文書であり、各子は自分の節を追記する。**実行（適用・削除・移動）はしない。人間の最終一括確認後に対象限定で適用する。**

---

## 子05-B候補（既存計画とarea標準の適用・ai運用pilot）

記録日: 2026-07-16 ／ 記録者: 子05 Bレーン（ai運用area pilot実装担当） ／ worktree: `task/pf05b`（base commit 32a46d63c4）

### 候補件数サマリ

| 区分 | 件数 | 内訳 |
|---|---|---|
| 維持候補 | 2 | `知識/` の既存2ファイル（例外3条件を満たすarea共通参照と判定。移動しない） |
| 移動候補 | 0 | 本worktreeで読めた範囲では `知識/` の移動対象なし |
| 削除候補 | 1 | `identity.md`（AGENTS.mdへ統合済み。削除は人間承認後） |
| 要再確認 | 1 | `知識/2026-07-13-AI運用制限の実効性監査.md`（Aレーン監査は言及するが本worktreeに実在しない。メイン`~/Private`での現物有無を承認セット適用前に再確認する） |

### 1. identity.md 削除候補

- 対象: `personal-os/my-brain/areas/ai運用/identity.md`
- 状態: 全項目を `personal-os/my-brain/areas/ai運用/AGENTS.md`（目的/判断基準/置くもの/置かないもの）へ統合済み。対応表: `references/2026-07-16-identity統合対応表.md`（全項目の統合先・統合方法を記載、網羅確認済み）。
- 推奨処置: 人間確認後に削除。削除実行時は、`決定ログ.md:4` と `plans/done/2026-07-02-状態と記録の統合設計/program.md:125` にある「identity.md → …」の読む順記述を `AGENTS.md` 参照へ書き換える追従作業を同時に行う（詳細: `references/2026-07-16-旧path参照grep結果.md`）。
- 停止条件の判定: Skill・hook・registryからの参照は0件。他計画からの参照はすべて読み取り専用の既存文書（paused/done/archive）か、上記2件の「読む順」記述のみ。今回は削除していないため導線は切れていない（blockedに該当しない）。

### 2. 知識/ 分類・移動候補

詳細: `references/2026-07-16-知識分類候補.md`

- `知識/AppleのFilesでMacフォルダを外出先から見る.md`（+html） → 維持（例外3条件を満たすarea共通参照）
- `知識/外出先からMacフォルダを参照する選択肢.md`（+html） → 維持（例外3条件を満たすarea共通参照）
- 移動候補: 0件（本worktreeで読めた2ファイルは両方維持と判定したため）
- 要再確認: Aレーン監査（`references/2026-07-15-子05A-既存計画監査.md`）が言及する `知識/2026-07-13-AI運用制限の実効性監査.md` は本worktreeに実在しない（base commitとメイン作業ツリーの差分の可能性）。承認セット適用前に `~/Private` 本体で現物の有無を確認し、存在すればAレーンの推奨（歴史スナップショットとして計画references/へ移すか、恒久基準だけ抽出）に従って別途分類する。

### 3. 旧path参照grep結果

詳細: `references/2026-07-16-旧path参照grep結果.md`。要点:

- `identity.md` 参照20件中、Skill・hook・registryからの参照は0件。実運用上の「読む順」指示として生きているのは `決定ログ.md:4` と `plans/done/2026-07-02-状態と記録の統合設計/program.md:125` の2件（削除時に追従要）。
- `知識/` への直接参照は0件（Area標準構成の規約記述を除く）。

### 4. Aレーン監査の是正候補一覧への参照

- 既存計画のバケット是正候補（是正候補一覧・67計画の監査）は `references/2026-07-15-子05A-既存計画監査.md` を参照（本子05-Bでは複製しない・そのまま人間確認へ）。
- 主な論点（詳細は上記ファイル）: paused 23件（新上限3、20件超過）／done 18件（新上限8、10件超過）／archive 19件中18件が終了記録欠如／空フォルダ`planning/2026-07-12-loopレジストリ統治設計`の重複疑い／`2026-07-08-並列実装フロー`の終了記録内の後継path不整合。

### 5. 代表計画の新テンプレ移行（参考・承認不要の実施済み事項）

pilotの一環として、新テンプレ（実行契約付き）への移行を以下2件で実施し、`plan-lint.sh`／`program-lint.sh`をPASSさせた（既存の事実・数値は変更せず、構造のみ新テンプレへ移行）。承認セットとしての可否判断は不要（バケット移動・削除を伴わない構造更新のため）だが、記録として残す。

- 単発plan: `plans/planning/2026-07-08-セッション記録の定期削除loop/plan.md`（同一バケット内で構造のみ更新）
- program: `plans/active/2026-07-12-loopレジストリTurso移行/program.md`（同一バケット内で構造のみ更新。子計画マップに役割/対象repo/並列/レビュー/人間ゲートを追加。子ファイル本体は無編集）

---

*（子01・02・03・04・06の候補節は、各子が該当作業を完了した時点で追記する。）*
