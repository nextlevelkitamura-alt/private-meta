対象: 子05「タスク入れ子と2層チェック」評価01の FAIL 項目5 ／ ラウンド: 修正01 ／ 起票: 2026-07-18

## 修正指示（1件）

### 完了済みタスクのfix行に1タップ付け替えUIを結線する

- 対象ファイル: `/Users/kitamuranaohiro/Private/projects/active/focusmap/src/app/dashboard/board/page.tsx`（`DoneTodoRow`）
- 今の状態: `FixReattach` が `AiTodoCard`（open・working）にのみ描画され、完了済みtodoを描く `DoneTodoRow` のfix行には出ない。完了済みタスクに自動紐付いたfixを別タスクへ付け替える経路がUIに無い
- 期待する状態: 完了済みタスクの入れ子にあるfix行にも `FixReattach`（1タップ付け替え）が表示される。既存の server action `reattachFixStep` を流用（契約は現行のまま: kind='fix' のみ・todo_id/seq だけ移動・履歴書換えなし）
- 指揮官裁定（付け替え先候補）: **当日の open todo と完了済み todo の両方**を候補に含める（設計正本の「手直しは当日の関連タスクの入れ子へ・完了済みでも追記」に一致）。自分自身は候補から除外
- 完了の確認: `npm run build` 通過＋DoneTodoRow のfix行に FixReattach が描画されるコード確認。attention行（質問/レビュー待ち）への追加は不要（openのAiTodoCardで既にカバー）

## 制約
- 他ファイル・他機能に手を広げない（評価01でPASSした項目を壊さない）
- migration・push・正本追記は引き続き人間ゲート（実行しない）
