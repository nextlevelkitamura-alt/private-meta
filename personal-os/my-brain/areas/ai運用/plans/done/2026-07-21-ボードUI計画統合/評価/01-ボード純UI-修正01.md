対象計画: plans/01-ボード純UI.md ／ 対応する評価: 01-ボード純UI-評価01.md ／ ラウンド: 01

# 修正01: ボード純UI

## 必須修正（FAIL解消）

1. **条件1（ステップ表示）**: theme-card.tsx の StepFlow に done=✔＋取り消し線グレー、doing=明滅ドット＋「今ここ」ラベルを実装する（モックv2・計画方針39行目準拠。明滅は motion-safe 系・prefers-reduced-motion で停止）。
2. **条件7（サイドバー同一部品化・案aを採用=2026-07-21人間指示「一緒のものにする」に忠実な側）**:
   - `buildBoardV2Data` を page.tsx から `src/components/today/board-v2/build.ts`（新規・共有純関数）へ移し、page.tsx はそれをimportする。
   - `/api/board/summary` は完全な `BoardV2Data` を返す形へ変更し、導出はbuild.tsを呼ぶ（二重実装の解消。フェイルソフトsoft()は維持）。
   - `BoardSummaryPanel` は取得した BoardV2Data を **ThemeCardV2・AskLane・StrayBox の同一部品**で描画する（サイドバー幅向けの外枠だけ調整可。10秒ポーリング・フォーカス復帰再取得は維持）。旧 SummaryTheme 型・独自描画は削除。

## 回帰の解消（評価01の指摘3・4）

3. **FileAgentCheck の復旧**: SessionRow に FileAgentCheck（sessionKey・todoTitle・date・label）を搭載し、未使用だった selectedDate を配線する。孤児ファイル化を解消。
4. **未分類の完了AI todo**: `StrayData` に `finishedTodos: FinishedTodoItem[]` を追加（契約変更・指揮官承認済み）し、build.tsで振り分け、StrayBox の折りたたみで表示する。

## 検証

- 変更ファイル群の tsc --noEmit エラー0
- localhost:3001 実走（375px・1440px）で崩れなし
- 実データのテーマカード確認はトークン供給後または本番反映後（保留として評価02に記録）
