対象計画: plans/01-やること箱と今日ボード.md ／ ラウンド: 01
diff範囲: /Users/kitamuranaohiro/Private/projects/active/focusmap の e3980f72..ea0d46b7（8690dba3=実装本体・fb68dc8f=自動コミット・ea0d46b7=.local除外是正） ／ 規模: フル ／ 評価者: impl-reviewer（Claude Fable 5配下・実装Sonnet 5とのクロスチェック）

# 評価01: やること箱と今日ボード

## 項目別採点

- [実測不能・保留] 起票画面（FAB→遷移）で追加した行が今日ボードに即時表示され、リロード・日付切替後も正しく永続している（対象: 新ボード画面・todosテーブル）
  根拠: コードは整合（actions.ts addTodo→insertTodo→revalidatePath→`?date=doDate`へredirect、page.tsx getTodosForDateがdo_date一致・dropped除外で描画、永続はDB）。fb68dc8fに混入したローカル検証DBをダンプし、テスト起票2行が実在・永続していることを実測（created 08:55:32/08:56:05 UTC＝実装commit直前）。ただし行タイトルが「curlテスト」でUI経由の起票・ボード表示・リロード/日付切替の目視記録はペイン消失で喪失。build通過（.next/server/app/dashboard/board/page.js確認済み）は実測として採用。

- [PASS] 実行repoの選択肢が repo-registry 登録repo＋「なし（私用）」で表示され、reposマスタと `repo概要.md` の対応が確認できる（対象: 起票画面・reposマスタ）
  根拠: migration seed 5行（shigoto=仕事/focusmap/private=Private/ai-platform=AIエージェント基盤/none=なし（私用））が repo概要.md の登録4repo＋なし(私用)と1:1一致。検証DBのreposテーブルにも同5行が実在することをsqlite3で実測。起票画面はgetRepos()全件を無条件にPillRadio描画（add/page.tsx）でbuild通過済み。

- [実測不能・保留] ai_status の各状態がバッジ表示され、確認待ち行の承認タップで status=done / ai_status=完了 になる（対象: 今日ボード・承認API）
  根拠: コードは6状態全てにバッジ分岐（page.tsx getAiStatusPresentation・default=未検知）、approveTodoは `WHERE ai_status='確認待ち'` ガード付きで status=done/ai_status=完了 に更新（todos.ts）。検証DBのAI行が created 08:55:32→updated=completed 08:55:54 で done/完了 になっており遷移実行の痕跡は強い。ただし「承認タップ」（UI経由）の実施記録が喪失。語彙逸脱なし: migration CHECK・TS型とも定義6語彙のみ、書込は insert(未検知)/approve(確認待ち→完了) のみで承認を経ずdoneへ飛ぶAI経路なし。self行は assignee='self' ガードのtoggleでai_statusに触れない。

- [PASS] 既存 `/dashboard/workspace/sessions` 画面と board.py の挙動が無変更（対象: 既存画面・session-board tests）
  根拠: `git diff e3980f72..ea0d46b7 -- src/app/dashboard/workspace supabase/ src/lib/turso/personal-os-board.ts` は空（実測）。diff全8ファイル中、既存ファイルの変更はdocs/CONTEXT.md追記と.gitignoreのみ。session-board側（~/Private repo）はこの実装からの変更ゼロ。

- [実測不能・保留] スマホ幅375pxで `../references/mockup-a-today-v2.png` と同等の構成で崩れない（対象: 新ボード画面）
  根拠: モックA/Cと突合し、全区画（日付ナビ／今日のやること=チェック・バッジ・承認ボタン／動いているエージェント／本日サマリ3タイル／右下FAB、フォーム=何をやる?・いつ5択・実行repo・任せる・詳しく▾格納）が同順で実装されていることをコードで確認。min-h-11（44pxタップ）・break-words/truncate・固定幅なし等モバイルファーストで崩れ要因は不検出。ただし375px実レンダリングの目視記録が喪失（認証が要るため評価側での再実測も不可）。

- [PASS] secret・token・認証値がコード・migration・docsに含まれない（対象: 変更ファイル全部）
  根拠: diff全文をtoken/secret/bearer/eyJ等でスキャンし、該当はenv変数「名」の参照のみ（値なし）。migrationは適用手順コメントのみ。誤コミットされていた検証DB（fb68dc8fのblob）もダンプして実測し、中身はrepos/todosのテストデータのみでsecretなし。ea0d46b7で追跡除外済み（blobはローカル履歴に残るがpush前・無害）。

## 総合判定

FAIL 0・PASS 3・実測不能・保留 3。修正へは差し戻さない（コード欠陥による保留ではない）が、全PASSではないため完了（done）へは進めない。保留3件はすべて「実装ペイン消失によるローカルE2E目視記録の喪失」が原因で、コード・build・DB痕跡の範囲では欠陥ゼロ。ローカルで①FAB起票→ボード表示→リロード/日付切替 ②確認待ち行の承認タップ ③375px表示 を1回通しで目視し、結果を評価02に記録すれば確定できる。

## 修正指示ドラフト（保留解消＋追加指摘）

- 【保留解消・再実測】対象: focusmap（localhost:3001）。①②③の通し目視1回（AI行はsqlite3で ai_status='確認待ち' に手動セットして承認タップを再現）。コード変更なし・約5分→評価02で全PASS確定。
- 【要修正・文書とコードの不一致】対象: src/app/dashboard/board/actions.ts の「来週」計算。コード=来週日曜のdo_date／文書（子計画01方針追記・docs/CONTEXT.md）=「来週=来週月曜（JST）」で食い違い。どちらかへ統一（暫定マッピング・正式対応は子02）。
- 【軽微・記録のみ】(a) ボード見出し「今日のやること」が過去/未来日付表示時も固定（モック判定外）。(b) server actionsに認証チェックなし——既存addGoal（workspace/sessions/actions.ts）と同型の先例踏襲でこの子の責でなく、program全体のハードニング候補としてout_of_scope_findings行き推奨。(c) actions側にrepo値のマスタ照合なし（不正POSTで未登録repo文字列が通る余地）。(d) fb68dc8fのDBバイナリblobはローカル履歴に残存——secretなし確認済み・push人間ゲート前なので履歴書換えは不要と判断（実施するなら危険操作として別途人間承認）。

## 補足（採点表外）

方針節の設計判断4点と契約の整合を確認: DB置き先=inbox（boardは読取専用トークン・session-board正本=変更禁止範囲への抵触回避・「reposマスタは参照コピー」と一致）、ポーリング20秒（board-poller.tsx POLL_INTERVAL_MS=20_000・isToday時のみ）、画面配置（変更可能範囲内・src/components/ 無変更）。唯一「いつ」の「来週」だけが文書と不一致。二重正本の発生なし。
