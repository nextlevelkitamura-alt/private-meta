# Requirements Ledger

This is the canonical status table. Do not mark an item `done` without evidence.

| ID | Type | Requirement | Status | Priority | Evidence | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-001 | governance | `CLAUDE.md` and `AGENTS.md` route to the same requirements governance rules. | done | high | `bash scripts/agent-instructions/check-agent-instructions.sh` passed on 2026-05-28. | Router blocks match `docs/agent/root-agent-router.md`. |
| REQ-002 | governance | Requirements, specs, progress, contradictions, non-goals, and decisions are stored outside entry files. | done | high | Required docs passed in `bash scripts/agent-instructions/check-agent-instructions.sh` on 2026-05-28. | Entry files remain routers. |
| REQ-003 | governance | `requirements-governor` is available to both Claude Code and Codex. | done | high | `bash scripts/agent-instructions/check-skill-compatibility.sh` passed on 2026-06-26, including `claude --help` and `codex --help`; `docs/agent/compatibility-checklist.md` records the latest result. | `.agents/skills/requirements-governor` is a symlink to `.claude/skills/requirements-governor`; runtime detection now passes for the installed CLIs. |
| REQ-004 | governance | Entry files stay under the configured line limits. | done | high | `wc -l CLAUDE.md AGENTS.md` returned 96 lines each on 2026-05-28. | Below target and warning thresholds. |
| NFR-001 | compatibility | Router and skill checks expose safe command boundaries, and any command that writes result docs is explicitly labeled as mutating. | done | medium | `docs/agent/compatibility-checklist.md` now separates safe read-only commands from `check-skill-compatibility.sh`, which writes the checklist result block. | Scripts avoid implementation-code changes, but `check-skill-compatibility.sh` is not read-only. |
| REQ-005 | workflow | `note-create` should provide learning research material before asking for user commentary on technical topics. | done | high | `note-create` skill, workflow reference, spreadsheet schema, candidate board display, and note ops prompt were updated on 2026-05-28; TypeScript check passed. | Covers definitions, options, free/paid notes, strengths/weaknesses, combinations, and comment prompts. |
| REQ-006 | workflow | `note-create` should show a detailed article review packet before requesting Kitamura comments, then require structure approval before image planning or drafting. | done | high | `note-candidate-board show --markdown` added; skill/workflow/runner docs updated and TypeScript check passed on 2026-05-28. | Prevents asking for comments from a short topic summary only. |
| REQ-007 | workflow | `ai-news-short-video` should preserve the proven AI news short workflow as a reusable global skill. | done | high | Skill files created under `起業スキル/skills/ai-news-short-video` on 2026-05-29; `wc -l` returned 148 lines for `SKILL.md`; symlinks resolve in `.codex/skills`, `.claude/skills`, and `.agents/skills`; publishing safety, source-quality, preproduction, storyboard, and pronunciation references were added. | Covers research, preproduction wall review, detailed storyboard, quoted video clips, 720p/1080p source quality gates, Japanese TTS, captions, BGM approval, YouTube Audio Library preference for monetization, source risk classification, blurred-background layout, verification, and revision patterns. |
| REQ-007 | workflow | `note-create` should use ImageGen raster output for thumbnail-like or mascot-led final note visuals, and note login checks should not overwrite an active login tab. | needs_verification | high | `note-create` skill/workflow updated on 2026-05-29; `note-playwright.ts check-login` now opens a disposable check tab; `git diff --check` passed. | Full verification requires successful `note-nextlevel` reauthentication and draft creation. |
| REQ-008 | workflow | `note-create` should preserve the earlier real Chrome `北村 / next level北村` fallback learnings and release-summary articles should use a clear map-style structure with image placement guidance. | done | high | `note-create` SKILL, workflow, learning loop, note growth patterns, scripts README, note-ops prompt/config, and npm note scripts updated on 2026-05-29; `npm run help:note-ops`, `npm run help:note`, `npm run note-skill-feedback -- --help`, and `npm run note-ops -- --phase post-draft --dry-run` passed. | Superseded by REQ-009 for default note draft execution. Keep Computer Use and the user's visible Chrome profile as login/recovery fallback only. Publication remains prohibited unless explicitly approved. |
| REQ-009 | workflow | `note-create` should use a reusable dedicated Chrome CDP profile for note authentication, then run note body HTML injection, image upload, save, and verification through Playwright/CDP as the primary editor path. | done | high | On 2026-05-29, `npm run note-login-stable -- --url https://editor.note.com/` opened `.playwright/profiles/note-nextlevel`; `npm run note-nextlevel-check -- --draft-url https://editor.note.com/notes/n353659c7038d/edit/` returned `logged_in: true`, `editor_visible: true`, and `note_cookie_count: 7`; `npm run note-create-draft-cdp -- ... --save` created draft `https://editor.note.com/notes/n353659c7038d/edit/`; the later UI-only `note-draft-stable` path created `https://editor.note.com/notes/n477218781ba3/edit/` with save 201, title OK, `h2: 7`, `heroCount: 1`, `bodyImages: 2`, `figures: 2`, `markers: []`, and `savedText: true`; `note-draft-stable` gained CDP auto-start for `.playwright/profiles/note-nextlevel`; `npm run note-draft-stable -- --help`, TypeScript check for `note-playwright-ui.ts`, and `git diff --check` passed. | Uses a non-default Chrome user-data-dir for CDP stability instead of direct CDP against the normal Chrome `Profile 16`; `note-draft-stable` is now the default completion path and must return `ok: true`, `save.ok=true`, expected H2/image counts, and `markersLeft=[]`; Computer Use remains login/recovery fallback only; publication remains prohibited unless explicitly approved. |
| REQ-010 | workflow | `note-create` should preserve the Dynamic Workflows article learning as reusable low-calorie technical contrast and workflow-readiness article patterns, and should require concrete researched facts in model/tool release drafts. | done | high | On 2026-05-29, `note-growth-patterns.md` was updated with `workflow-readiness`; `note-create/SKILL.md` added concrete-number drafting rules; `npm run note-skill-feedback -- --type example/preference ...` appended durable learning; `git diff --check` passed. | Use chat as the review surface first; write to spreadsheet only after direction approval. |
| REQ-011 | workflow | `note-create` should start with chat-first topic-only scouting, avoid building `構成` before Kitamura reacts, and defer spreadsheet writes until the draft/direction is approved or explicitly requested. | done | high | On 2026-05-30, `npm run note-skill-feedback -- --type workflow ...` recorded the rule; `note-create/SKILL.md`, `references/workflow.md`, `references/spreadsheet-schema.md`, and `scripts/note-candidate-board.ts` were updated; `npm run note-candidate-board -- list --markdown` returned topic-only cards without `仮構成`; `git diff --check` passed in `起業スキル`. | Full repo `npx tsc --noEmit` still has unrelated pre-existing errors in `check-domain-availability.ts`, `onamae-domain.ts`, and `stripe-bootstrap.ts`. |
| REQ-012 | workflow | `note-create` should provide a fast Playwright/CDP recovery path for note image upload/crop handling on an existing draft, without repasting the whole article. | done | high | On 2026-05-30, `scripts/note-image-lite.ts` was added and exposed as `npm run note-image-lite`; `npm run help:note-image-lite` passed; `npx tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 note-image-lite.ts` passed; the command saved `https://editor.note.com/notes/n937fc2b1b6a0/edit/` with `save.status=201`, `bodyImages=3`, `allImages=4`, and `markersLeft=[]`; after speed tuning, non-mutating verification against the same draft returned `ok=true` with `timings.total=824ms`; the default save timing now waits about 3 seconds after the final image attaches. | Uses `marker=`, `afterText=`, or `afterHeading=`; accepts visible crop modals as-is when speed matters; final JSON includes `timings` to separate script overhead from note upload latency. Publication remains prohibited unless explicitly approved. |
| REQ-013 | workflow | `note-create` should create a new note draft with hero image, body images, and draft save in about 2 minutes or less, while recording per-step timing evidence. | done | high | On 2026-05-30, `note-draft-stable` was speed-tuned and created `https://editor.note.com/notes/nb28c64cec75e/edit/` with `ok=true`, `save.status=201`, expected `h2=6`, `heroImages=1`, `bodyImages=3`, `markersLeft=[]`, and `timings.total=21177ms`; evidence is stored at `起業スキル/out/note-assets/20260530-codex-mobile-workflow/note-draft-speed-run-3/final.json`; `npm run help:note-draft-stable`, file-level TypeScript check for `note-playwright-ui.ts` and `note-image-lite.ts`, and `git diff --check` passed. | Removes the previous long `alt=eyecatch` wait, uses DOM marker placement instead of keyboard marker deletion, short filechooser/file-input retries, configurable `--settle-ms`, `--crop-wait-ms`, `--upload-timeout-ms`, and `--save-delay-ms`, and treats `timings.total > 120000ms` as a workflow regression to inspect before completion. |
| REQ-014 | workflow | `note-create` should be able to recover or verify a missing note thumbnail on an existing draft through Playwright/CDP without repasting the article. | done | high | On 2026-05-30, the latest draft `https://editor.note.com/notes/nb28c64cec75e/edit/` was inspected via CDP and confirmed to have 3 body images but no thumbnail; the cover UI path was identified as `画像を追加` → `画像をアップロード 推奨サイズ：1280×670px` → crop dialog `画像のサイズの変更` → `保存`; the thumbnail was attached and persisted after reload with `alt=eyecatch`, `coverImages=1`, `bodyImages=3`; `note-image-lite --hero ... --save` then verified the same draft with `ok=true`, `save.status=201`, `heroImages=1`, `bodyImages=3`, `markersLeft=[]`, and `timings.total=4762ms`; help, file-level TypeScript checks, and `git diff --check` passed. | Hero detection must count only large `main img` elements outside `.ProseMirror`, not arbitrary `document.images`, because note's image menus/help popovers can add non-hero images. Crop handling must include `.CropModal__content` and `[role='dialog']:has-text('画像のサイズの変更')`. |
| REQ-015 | workflow | Local video/audio transcription should use `mlx-whisper` word timestamps to produce canonical `units` JSON, then generate `caption_blocks` from units without LLM-generated timestamps. | needs_verification | high | On 2026-06-01, `scripts/extract_audio.py`, `scripts/transcribe_local.py`, `scripts/build_caption_blocks.py`, `schemas/transcript.schema.json`, docs, feature spec, and `video-transcription` skill files were added; `python3 -m unittest tests/test_build_caption_blocks.py` and `python3 -m py_compile scripts/extract_audio.py scripts/transcribe_local.py scripts/build_caption_blocks.py` passed. | End-to-end `mlx-whisper` inference still needs local dependency/model installation and a sample media file. |
| REQ-016 | workflow | `ai-news-short-video` should automatically decide when to use local `video-transcription` for ordinary edit assets, quoted videos, existing MP4s, interviews, and caption timing work. | needs_verification | high | On 2026-06-01, `skills/ai-news-short-video/references/transcription-decision-gate.md` and `docs/plans/ai-news-short-video.md` were added in `起業スキル`; `skills/ai-news-short-video/SKILL.md` now routes source-material handling through the decision gate; global `video-transcription` symlinks were added for Claude, Codex, and agents. | Actual video production flow still needs a representative quoted video and ordinary TTS-based edit to verify the skip/use decision in practice. |
| REQ-016 | workflow | `仕事` Playwright auth recovery should not open Terminal or run fixed 10:00 auth prompts; it should open only the browser login flow when automation actually needs auth, retry until auth is restored, and keep success notifications. | done | high | On 2026-06-01, `com.nextlevel.auth-morning` and `com.nextlevel.keep-alive` were disabled; `job-update.schedule` was re-enabled at 900-second interval; `scripts/shared/auth-guard.ts` now starts `keep-alive.ts --allow-login --force` detached for Playwright auth instead of Terminal; `scripts/auth/setup-launchd.sh` no longer registers fixed auth jobs; file-level TypeScript check passed; `auth.json`, `auth-validated.lock`, and `auth-notified-2026-06-01.lock` updated at 10:20 JST. | GWS auth may still use the existing Terminal wrapper; this requirement covers Playwright/e-nextlevel browser auth. |
| REQ-017 | workflow | `仕事` GWS auth recovery should not open Terminal or the GWS scope picker; it should open only the Google browser OAuth window once per detected GWS auth failure, suppress repeats while the failure lock exists, and avoid terminal success messages. | done | high | On 2026-06-02, `scripts/shared/auth-guard.ts` switched GWS recovery from `gws auth login -s ...` in Terminal to detached `gws auth login --scopes <OAuth URLs>` with `/tmp/auth-guard-gws-recovery.lock`; `/tmp/auth-request-gws.lock` now suppresses repeat prompts until successful auth clears it; GWS browser auth starts only for `expired`, `missing_scope`, or `no_credentials`, not for status-check timeouts; `scripts/shared/AUTH-GUARD.md`, `scripts/shared/gws-login-with-browser.sh`, and `scripts/staff-status/MONTHLY-SCHEDULE.md` were updated; `tsx scripts/shared/auth-guard.ts`, `bash -n scripts/shared/gws-login-with-browser.sh`, `git diff --check` in `仕事`, `gws auth status --format json`, and a Sheets `values.get` health check passed. | Existing stuck `gws-login-with-browser.sh` / `gws auth login -s` processes were terminated and GWS locks were cleared after `token_valid=true` and Sheets API access were verified. |
| REQ-018 | workflow | `仕事` should automate circus-job.com job search from candidate conditions and output recommendation scores with reasons and concerns. | done | high | On 2026-06-04, `scripts/circus-job-search` was added; `npx tsc --noEmit` passed; `npm run search -- --pref 東京 --keyword 営業 --age 28 --education 高卒 --max 5` returned 5 jobs with ranked recommendations; `npm run search -- --candidate-file candidate.example.json --max 3` returned 3 jobs with S-ranked recommendations. | Uses the existing circus Chrome profile `~/.playwright-auth-temp`; credentials are read only from one-run env vars or manual login and are not written to repo files. |
| REQ-019 | workflow | `仕事` circus-job.com recommendations should treat must-have requirements as gates, not soft scoring signals, and should ask for candidate license/qualification/experience details before recommending jobs. | done | high | On 2026-06-04, `scripts/circus-job-search` added `--driver-license`, `--license`, `--experience`, and `--include-disqualified`; recommendation output now includes detected required requirements and disqualifying reasons; `npx tsc --noEmit` passed. | If a required license or experience is not present in candidate input, the job is excluded by default. Live rerun after this change needs circus login recovery because the session expired. |
| REQ-020 | workflow | `仕事` circus-job.com recommendations should scrape a larger candidate pool, suppress broad `その他事務` false positives, and output S/A/B recommendations split by ideal versus realistic lanes. | done | high | On 2026-06-04, `scripts/circus-job-search` added `--pool`, office-fit classification, lane output (`理想寄り` / `現実ライン` / `挑戦枠` / `対象外`), grouped Markdown, session-takeover login confirmation, idle-stop scraping, and stricter must-have gating; `npx tsc --noEmit` passed; `npm run search -- --from "博多駅" --time 30 --occupation "一般事務,営業事務,専門事務（IT・医療・金融・不動産・貿易等）,その他事務関連職" --age 30 --annual-min 360 --driver-license no --pool 300 --max 20` returned 2 eligible jobs from 25 collected jobs after strict filtering. | The current circus result set only exposed 25 cards for this condition before new cards stopped appearing; strict screening intentionally leaves fewer than 20 recommendations rather than padding with must-have mismatches. |
| REQ-021 | workflow | `仕事` circus-job.com recommendations should provide a fallback proposal scheme when strict matches are scarce, including adjacent occupations, condition-change examples, and qualification/experience routes. | done | high | On 2026-06-04, `scripts/circus-job-search` added proposal output with market verdict, candidate-info questions, adjacent office-related searches, unlock suggestions, and near-miss jobs that are separated from strict recommendations; `README.md` documents the scarce-result proposal scheme; `npx tsc --noEmit` passed; the 博多駅30分・事務・年収360万円・30歳・免許なし run generated `output/result-2026-06-04T14-38-14.md` with 2 strict recommendations plus adjacent/pivot guidance. | Near-miss jobs remain explicitly labeled as condition-change or information-addition examples, not normal recommendations. |
| REQ-022 | workflow | `仕事` circus-job.com scarce-result handling should compare multiple proposal vectors, such as commute expansion, salary relaxation, CS/BPO, HR/recruiting, and sales-adjacent pivots. | done | high | On 2026-06-04, `README.md` added vector-based additional-search guidance; `scripts/circus-job-search` tightened must-have detection for broad/pivot searches including generic driver-license wording, customer-facing experience, HR/recruiting experience, PT/OT/ST licenses, new-grad-only, and sales-leadership mismatches; `npx tsc --noEmit` passed; live vector searches produced `output/vector-current-final.md`, `output/vector-commute60-office.md`, `output/vector-salary330-office.md`, `output/vector-no-salary-office.md`, `output/vector-cs-bpo-final.md`, `output/vector-hr-assistant-final.md`, and `output/vector-sales-backoffice-final3.md`. | Results showed commute 60 minutes and salary 330万円 did not increase strict office recommendations; removing salary increases count but shifts to low-salary/non-office jobs; CS/BPO and HR/recruiting mostly require unconfirmed experience; sales-adjacent is a true pivot, not an office recommendation. |
| REQ-023 | workflow | `仕事` should preserve candidate-specific circus-job.com job search and proposal portfolios as a reusable Skill, with first-step direction setting and page-by-page collection evidence. | done | high | On 2026-06-05, `.claude/skills/circus-job-proposal/SKILL.md` was added; `方針/スキル一覧.md`, `.claude/skills/CATALOG.md`, and `work-skill-guide/SKILL.md` route candidate job proposal requests to it; `scripts/circus-job-search` now outputs `pageStats` and Markdown page logs; `npx tsc --noEmit` passed; `npm run search -- --pref 東京 --keyword 営業 --max 2 --pool 2 --json output/page-test.json --md output/page-test.md` produced page stats. | Current circus search appears infinite-scroll first; the script clicks a detected next-page control when present and otherwise records `stopReason=no-next-page`. |
| REQ-024 | workflow | `仕事` circus-job.com proposal Skill should deepen occupation-family guidance and attach job-detail PDFs for proposed jobs. | done | high | On 2026-06-05, `circus-job-proposal/references/occupation-strategy.md` was added; `SKILL.md` now routes ambiguous profile-to-vector decisions to it and requires `--pdf` for proposed jobs; `scripts/circus-job-search` added `--pdf`, `--pdf-dir`, `--pdf-max`, `jobs[].pdfPath`, and Markdown `求人票PDF`; `npx tsc --noEmit` passed; `npm run search -- --pref 東京 --keyword SaaS --max 1 --pool 3 --include-disqualified --pdf --pdf-max 1 --json output/pdf-test.json --md output/pdf-test.md` saved one PDF. | The site's visible `求人票を出力する` button emitted analytics but no PDF/download response in Playwright checks, so the script saves the authenticated detail page as an A4 print PDF. `転載NG` still requires shareability confirmation. |
| REQ-025 | workflow | `kimi-webbridge` should support logged-in Arc browser workflows with reliable daemon/extension recovery and Arc-safe task sessions when native WebBridge sessions hang. | done | high | On 2026-06-19, the Kimi WebBridge daemon was upgraded from v1.9.7 to v1.10.0 to match the Arc extension; `arc-session.sh` was added; `screenshot.sh` was updated for v1.10.0 path-based screenshots; `SKILL.md` and `references/operations.md` now document Arc recovery and wrapper usage; `quick_validate.py`, `bash -n`, health, two-session focus switching, click URL refresh, snapshot, evaluate, and screenshot smoke tests passed. | Native WebBridge `"session":"..."` commands were observed to time out in Arc, while no-session commands worked. Use serialized Arc wrapper sessions rather than concurrent native sessions for Arc. |
| REQ-026 | workflow | `short-video-create` should preserve a reusable, progressive-disclosure Skill for vertical short video creation, with the Japanese employment, recruiting, and AI labor-market workflow as the initial preset, and with research, source-safety, storyboard, production, revision, and skill-memory workflows split out of `SKILL.md`. | done | high | On 2026-06-19, the Skill was created; on 2026-06-22 it was renamed to `short-video-create`. The current `skills/short-video-create` has a hub `SKILL.md`, 15 total skill files, six workflow files, five reference files, three asset templates, and global symlinks resolving from `.codex/skills`, `.claude/skills`, and `.agents/skills`; trailing-whitespace check returned no matches; `bash scripts/agent-instructions/check-skill-compatibility.sh` passed after the rename. | Keeps existing `ai-news-short-video` focused on general AI news while making the new Skill usable for broader short-video creation. The initial preset still includes Japanese article priority, employment-specific source safety, selected VOICEVOX male voice candidates, subtitle readability, and generation-as-B-roll constraints. |
| REQ-027 | workflow | `coding-task-orchestrator` should preserve a confirmation-first development-task orchestration Skill that plans task size, execution surface, AI roles, branch/worktree/port, docs lifecycle, worker prompts, returned-report supervision, and closeout before implementation. | done | high | On 2026-06-26, the Skill was created and review-patched with worktree governance, UI comparison separation, Prompt Pack headings, closeout classification separation, static manual prompt test expectations, and supervisor evaluation patches covering Returned Report Type, Compared Against, Decision taxonomy, type-specific returned-report evaluation, worker recommendation as advisory only, Prompt To Use Next patterns, light Medium docs exception, short investigation phase, and explicit forbidden operation names. Current static counts: 29 Skill files and 133-line `SKILL.md`. A live manual prompt test on 2026-06-26 recorded Small, Medium, Large, UI Comparison, Worker Return, and Closeout transcripts in `docs/specs/coding-task-orchestrator-skill/live-manual-prompt-test-2026-06-26.md`; all six requested cases passed. An additional live prompt test recorded Short Investigation, Planning Return, Integrator Return, Review Return, Error / Blocker, and Realistic Worker Return + Closeout in `docs/specs/coding-task-orchestrator-skill/live-manual-prompt-test-additional-2026-06-26.md`; all six additional requested cases passed. Real operation evidence was recorded in `docs/specs/coding-task-orchestrator-skill/real-operation-evidence-2026-06-26.md` using branch `chore/cto-real-op-evidence-20260626`, worktree `/Users/kitamuranaohiro/Private/focusmap-wt-cto-real-op-evidence-20260626`, and commit `e938272e14ea93092455ab752fa8e4d6bfe27b9c`; it includes implementation-worker return supervision, `Prompt To Use Next`, closeout judgment, and prohibited-operation non-execution evidence. | Done because basic six cases passed, additional six cases passed, real branch/worktree representative operation exists, worker return supervision evidence exists, closeout judgment evidence exists, and no prohibited operations were executed. Strict destructive-operation Error / Blocker live coverage remains optional future hardening. `check-skill-compatibility.sh` is not used as formal `coding-task-orchestrator` evidence because it is `requirements-governor`-specific and rewrites `docs/agent/compatibility-checklist.md`. `.agents` remains the canonical source and is outside git; future recommendation is git-managed `skills/coding-task-orchestrator/` with `.agents`/`.codex`/`.claude` symlinks. |
| REQ-028 | governance | `/Users/kitamuranaohiro/Private` should be a local Git meta-repository for Orca/Codex/Claude governance work while independent child repositories stay outside the parent repo. | done | high | Commit `3dc31d3` initialized `/Users/kitamuranaohiro/Private` on `main`; `.gitignore` uses an allowlist for meta paths; `git ls-files | rg -v '^(\\.agents/|\\.claude/skills/|docs/|scripts/|schemas/|tests/|\\.gitignore$|AGENTS\\.md$|CLAUDE\\.md$)'` returned no output; `git status --ignored --short` shows child repos and local settings as ignored; `git remote -v` returned no configured remote. | This is a local-only meta repo. GitHub remote creation, pushing, child-repo submodules, and monorepo conversion remain out of scope until separately approved. |
