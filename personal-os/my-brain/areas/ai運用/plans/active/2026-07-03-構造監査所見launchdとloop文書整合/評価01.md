対象計画: plan.md ／ ラウンド: 01
diff範囲: 未コミット作業ツリーのloop構造簡素化差分 ／ 規模: フル ／ 評価者: sol-high-review

# 評価01: loop構造簡素化

## 項目別採点

- [PASS] `loops-registry/loops/` が現役4本だけになり、停止3本が存在しない
  根拠: 実体は `board-reconcile` / `board-sweep` / `daily-notion-sync` / `session-record-prune` の4本だけ。`ai-jobs-dispatcher` は別計画レーンで削除済み。

- [PASS] 削除5loopを指す LaunchAgents symlink が残らず、labelも未ロード
  根拠: `~/Library/LaunchAgents/` と退避場所のsymlink探索で0件。5labelは `launchctl print` で全てnot-loaded。

- [PASS] `references/` が存在せず、生きた共通規約が `loops-registry/AGENTS.md` に一意にある
  根拠: `references/` 不存在。loop境界、runner/scope、30日寿命、削除順、overview更新契約をAGENTSで確認。

- [PASS] `personal-os.md` が現役4本の必要情報を掲載する
  根拠: 4本それぞれに目的・発火・runner・label・正本・意図状態・実機確認がある。

- [PASS] `personal-os.html` がMDと同階層にあり、旧 `output/` が存在しない
  根拠: HTMLは4カードを表示し、MDとの正本/派生物関係を明示。旧 `実行一覧/output/` は不存在。

- [FAIL] `verify.py` がloop実体・plist・MD・HTMLの契約を過不足なく検査してPASSする
  根拠: 正常状態・HTML再現・SHA・labelはPASSするが、MDの発火値を999秒へ誤記してもPASSした。逆にAGENTSが許す30日以内の一時停止を常にFAILにする。

- [PASS] 現役4本の `loop.md` 状態表記と実機が一致する
  根拠: 4本とも「稼働中」で、4labelとも `launchctl print` でloaded。

- [FAIL] 削除loopへの現在形の実行依存が0件
  根拠: `inbox-triage/SKILL.md` が不存在の `inbox-patrol/scripts/new-plan-planning.sh` を直接実行する。`kickoff`・`cockpit-supervisor`・catalogも削除済みloopを現役正本/稼働中として説明し、active計画にも将来実行時に壊れる前提が残る。

- [PASS] 現役4本の関連テストとplist検証がPASSする
  根拠: shell構文PASS、board-sweep 31/31、session-record-prune 13/13、plist 4/4 PASS。daily-notion-sync 9/11の2失敗は対象ファイルに差分がなく今回差分外と確認。

- [PASS] secret・token・credentialが差分・overview・HTMLに含まれない
  根拠: 値を表示しない形式スキャンで該当0件。`git diff --check` もPASS。

- [FAIL] 異系統評価の全項目PASSが評価文書に記録されている
  根拠: 本評価で項目6・8がFAIL。全PASSではないため完了不可。

## 総合判定

FAILあり。構造削減、runtime残骸除去、現役4本、MD/HTMLの基本同期は正しいが、
将来の不整合を止める `verify.py` と削除loopへの現在形参照が未完了。
`修正01.md` の範囲だけを修正し、評価02を行う。

## 修正指示ドラフト

1. `verify.py` にplist発火設定とMDの機械可読値の一致検査を追加する。
2. 30日以内の一時停止をAGENTS契約どおり表現・検査できるよう、MD必須項目とverifyを揃える。
3. `inbox-triage` の不存在helper呼び出しを現存導線へ置換する。
4. `kickoff`・`cockpit-supervisor`・catalog・currentなAGENTS/active計画から削除loopの現役前提を除去する。

