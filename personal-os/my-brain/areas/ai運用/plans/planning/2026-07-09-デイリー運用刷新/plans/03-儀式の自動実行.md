親計画: ../program.md ／ 分類: loop ／ 種別: 新規作成
規模: フル
並列: 可（Wave 2） ／ レビュー: 一括（Checkpoint B / Final Gate）

# 3儀式（Daily start / Work end / Daily end）

## 目的

朝はwork/privateを一緒に開始し、従事仕事の終了と1日全体の終了を別々に締める3つの対話Skillを作る。09:50以降の最初のwake/loginではDaily startの入力済み対話を開くが、自動送信・自動closeはしない。

## 現状

- 実在するのは旧 `skills/standup/` 骨格と `skills/morning-routine/`。旧名称・旧計画pathが残り、Codex側への完全な導線はない。
- Work end / Daily endのSkillは未実装。
- Claude/Codexのdeep linkはprompt prefillが可能だが、自動送信はしない。
- 8節Daily、業務CLI、monthly-reset、goal-alignmentはCheckpoint Aで確定する前提。
- runtime Skill露出、旧Skill削除、launchd loadは人間ゲート。

## Terra実装パケット

### 担当とWave

- 担当: `Terra-03`（`gpt-5.6-terra`）
- Wave: 2。Checkpoint A PASS後にTerra-04と並列。
- worker完了時は自己テストとcommit hashだけをPlan 09へ渡す。

### 最初に読むもの

1. `/Users/kitamuranaohiro/Private/AGENTS.md`
2. この `plans/03-儀式の自動実行.md`
3. `personal-os/AIエージェント基盤/global-skill-registry/AGENTS.md`
4. `personal-os/AIエージェント基盤/loops-registry/AGENTS.md`
5. `personal-os/AIエージェント基盤/skills/skill-creator-custom/SKILL.md`
6. 現行 `skills/standup/SKILL.md` と `skills/morning-routine/SKILL.md`
7. Checkpoint A後にPlan 09が公開したsession-board CLI、monthly-reset、goal-alignmentの契約

`program.md` と他の子planは読まない。

### 触る場所

- worktree上の `personal-os/AIエージェント基盤/skills/daily-start/`
- worktree上の `personal-os/AIエージェント基盤/skills/work-end/`
- worktree上の `personal-os/AIエージェント基盤/skills/daily-end/`
- worktree上の `personal-os/AIエージェント基盤/loops-registry/loops/daily-start/` の未ロード部品とtests

### 触らない場所

- `session-board/board.py`、`md/store.py`、ゴール実ファイル
- `skills/standup/`、`skills/morning-routine/` の削除・改名・本文更新
- Global Skill catalog / logs / runtime symlink、loop overview、`~/Library/LaunchAgents/`
- `program.md`、他の子plan、実Daily、hook trust、launchctl、git push

### 実装すること

1. `daily-start`: 前日未封印の確認→3年/年/月/週/7日予定→work/private候補→人間確認→Daily反映。
2. `work-end`: `lane: work`だけの進捗・質問・明日へを確認し、CLI `work-close`を呼ぶ。privateは開いたまま。
3. `daily-end`: privateを含む全lane、明日へ、月/年の変更候補を確認し、条件成立時だけCLI `close`を呼ぶ。
4. Daily startのprompt builderとdeep link openerを分ける。openerは入力欄へprefillするだけで送信しない。
5. 09:50 `StartCalendarInterval`、`RunAtLoad`、低頻度retry、JST当日state、flockを持つplist/scriptをworktreeに作る。loadしない。
6. deep link失敗は成功stateにせず、retryまたは同じbuilderを使う手動コマンドを残す。

### 成果物と受け渡し

- 3Skillの入力→確認→作用器の契約、prompt builder、未ロードplist/script、tests。
- 旧Skillから何を移すか・残すか・削除候補かを一覧にし、実際の移行/削除はPlan 09と人間へ渡す。
- runtime露出・launchd loadの手順は示すが実行しない。

### 自己テスト

- prompt builderの固定fixtureで自動送信が発生しない。
- 時刻注入で09:49はno-op、09:50以降は当日1回、wake/login/retryでも重複しない。
- Work end後もprivate更新可、Daily end後だけ翌日回送になるCLI stub E2E。
- `plutil -lint`、Skill quick validation、`git diff --check`。

## 方針

- 自動化を「発火」「決定的集計」「対話」「作用器」に分け、launchdへ判断本文を埋め込まない。
- 旧 Standup / Wrap / Shutdown は移行元の名前とし、新しい人間向け名称は3儀式へ統一する。
- Work end / Daily endは時刻固定で強制closeしない。通知・対話候補まで。
- 未実施のDaily endは翌日のDaily startが先に拾う。

## 人間判断待ち

- `daily-start` / `work-end` / `daily-end` を正式Skill名にすること。
- 旧 `standup` / `morning-routine` の縮退・改名・削除。
- Work end / Daily endの通知時刻。時刻が未確定でも手動3儀式とDaily startの09:50 fixtureは実装可能。
- runtime露出、trust、launchd load。

## 完了条件（レビュー項目）

- [ ] 3Skillの手順が1組だけ存在し、launchd/deep link側に本文コピーがない
- [ ] Daily startはwork/private両laneを提示し、人間承認前にDaily・計画本文を変更しない
- [ ] Work end後はworkだけが締まり、Daily end後だけDaily全体が封印される
- [ ] 09:50 / wake / login / retryのfixtureで当日1回だけprefillし、自動送信しない
- [ ] deep link失敗は成功扱いにならず、手動fallbackが同じprompt builderを使う
- [ ] Checkpoint B / Final Gateで1日E2Eとplist/Skill検証がPASSする

## 完了報告

`3Skill / 未ロード自動化部品 / 自己テスト / 人間ゲート / commit hash` の5点だけをPlan 09へ渡す。
