# レビューの共通コンテキスト（この計画のレビュアーが気をつけること）

※ 本ファイルは新標準（役割別コンテキスト）のpilot第1号。レビュアーはこれを読んでから採点する。

## 読む順番

1. `plan.md` — 「完了条件（レビュー項目）」8項目がそのまま採点表
2. このファイル
3. 実装diff（base_commit → result_commit）

## 特に厳しく見る点

- **差分ゼロ系は実測で**: manifest 4バリデータ（`agents-registry/harness/schemas/run-manifest.schema.json`・`agents-registry/harness/manifest.py`・`skills/plan-ops/scripts/planctl.py` の MANIFEST_TYPES・`hooks-registry/shared/plan-closeout/common.py` の MANIFEST_REQUIRED）と hooks登録2ファイルに差分がないこと。
- **読み順のrole混入**: implementerパケットに レビュー/共通.md が、reviewerパケットに 実装/共通.md が漏れていないか。パケット出力の実物で確認する。
- **後方互換**: 既存program（実在検証対象: `plans/active/2026-07-15-計画立案実行完了基盤`）が新しいlint・閉鎖ゲート・評価探索で壊れないこと（旧配置 plans/ 隣接の評価mdを読めること）。
- **回帰防止**: program_path=None 欠落の解消がテストで固定されているか。「program無しでは共通.md行が出ない」「explorerは従来どおり」の非回帰2種も確認。
- **pilot自身の遷移実測**: 本計画の 評価/ 配置で bucketctl のdone判定（evaluation_passes）が評価を認識することをdry-run実測する（②裁定前でも判定ロジックの確認は可能）。

## 採点の流儀

- 項目ごとに [PASS]/[FAIL] ＋根拠（実測コマンドと結果）。推測でPASSにしない。
- 評価の置き場はpilot構成に従い、この計画の `評価/` へ（`評価01.md`。差し戻しは指揮官が `修正01.md` を同じ場所へ置く）。
- 検証コマンドは絶対pathで実行する。
