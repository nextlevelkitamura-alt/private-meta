# Progress Board

## Done

- REQ-001: Shared router for `CLAUDE.md` and `AGENTS.md`. Evidence: `bash scripts/agent-instructions/check-agent-instructions.sh` passed on 2026-05-28.
- REQ-002: Source-of-truth docs under `docs/requirements/`, `docs/specs/`, and `docs/adr/`. Evidence: required path checks passed on 2026-05-28.
- REQ-004: Entry file line policy and validation. Evidence: `wc -l CLAUDE.md AGENTS.md` returned 96 lines each on 2026-05-28.
- NFR-001: Safe compatibility and instruction check boundaries. Evidence: `docs/agent/compatibility-checklist.md` separates safe read-only commands from `check-skill-compatibility.sh`, which writes the checklist result block.
- REQ-005: `note-create` learning research pack before commentary for technical topics. Evidence: skill docs and note scripts updated, then TypeScript check passed on 2026-05-28.
- REQ-006: `note-create` detailed review packet before comments and approval gate before image/draft. Evidence: `note-candidate-board show --markdown` added and TypeScript check passed on 2026-05-28.
- REQ-007: `ai-news-short-video` reusable global skill. Evidence: `SKILL.md` plus workflow/style/publishing-safety/source-quality/preproduction/storyboard/pronunciation references created on 2026-05-29, and symlinks resolve in Codex, Claude, and agents skill directories.
- REQ-008: `note-create` real Chrome Profile 16 fallback learnings and release-summary structure pattern. Evidence: skill docs, script docs/config, and note pattern references updated on 2026-05-29; note script help commands and `post-draft` dry run passed. Default draft execution is superseded by REQ-009.
- REQ-009: Playwright/CDP primary note editor path with reusable dedicated Chrome auth profile. Evidence: on 2026-05-29, `npm run note-login-stable -- --url https://editor.note.com/` opened `.playwright/profiles/note-nextlevel`; `npm run note-nextlevel-check -- --draft-url https://editor.note.com/notes/n353659c7038d/edit/` returned `logged_in: true`, `editor_visible: true`, and `note_cookie_count: 7`; `npm run note-create-draft-cdp -- ... --save` created draft `https://editor.note.com/notes/n353659c7038d/edit/`; later, the UI-only `note-draft-stable` path created `https://editor.note.com/notes/n477218781ba3/edit/` with save 201, title OK, `h2: 7`, `heroCount: 1`, `bodyImages: 2`, `figures: 2`, `markers: []`, and `savedText: true`; `note-draft-stable` gained CDP auto-start for `.playwright/profiles/note-nextlevel`; `npm run note-draft-stable -- --help`, TypeScript check for `note-playwright-ui.ts`, and `git diff --check` passed.
- REQ-010: `note-create` low-calorie technical contrast, workflow-readiness, and concrete researched model/tool release drafting patterns. Evidence: `note-growth-patterns.md` gained the `workflow-readiness` pattern, `note-create/SKILL.md` added concrete-number drafting rules, `npm run note-skill-feedback -- --type example/preference ...` appended durable learning, and `git diff --check` passed on 2026-05-29.
- REQ-011: `note-create` chat-first topic-only scouting and spreadsheet-write deferral. Evidence: on 2026-05-30, `npm run note-skill-feedback -- --type workflow ...` recorded the workflow rule; skill/workflow/schema/script files were updated; `npm run note-candidate-board -- list --markdown` returned topic-only cards without `仮構成`; `git diff --check` passed in `起業スキル`.
- REQ-012: `note-create` fast note image upload/crop recovery path. Evidence: on 2026-05-30, `npm run note-image-lite -- --url https://editor.note.com/notes/n937fc2b1b6a0/edit/ ... --save` returned `ok: true`, `save.status: 201`, `bodyImages: 3`, `allImages: 4`, and `markersLeft: []`; after speed tuning, non-mutating verification returned `ok=true` with `timings.total=824ms`; default save timing now waits about 3 seconds after the final image attaches; help and file-level TypeScript checks passed.
- REQ-013: `note-create` full new note draft creation under about 2 minutes. Evidence: on 2026-05-30, `note-draft-stable` created `https://editor.note.com/notes/nb28c64cec75e/edit/` with hero image, 3 body images, draft save, `ok=true`, `save.status=201`, `markersLeft=[]`, and `timings.total=21177ms`; evidence is in `起業スキル/out/note-assets/20260530-codex-mobile-workflow/note-draft-speed-run-3/final.json`; help, file-level TypeScript checks, and `git diff --check` passed.
- REQ-014: `note-create` missing-thumbnail recovery on an existing note draft. Evidence: on 2026-05-30, CDP inspection found the latest draft `https://editor.note.com/notes/nb28c64cec75e/edit/` had 3 body images and no thumbnail; the thumbnail was attached through the top cover UI, persisted after reload with `alt=eyecatch`, and `npm run note-image-lite -- --url ... --hero ... --save` verified `ok=true`, `save.status=201`, `heroImages=1`, `bodyImages=3`, `markersLeft=[]`, and `timings.total=4762ms`; help, file-level TypeScript checks, and `git diff --check` passed.
- REQ-016: `仕事` Playwright auth recovery no longer opens Terminal or fixed 10:00 auth prompts. Evidence: on 2026-06-01, `com.nextlevel.auth-morning` and `com.nextlevel.keep-alive` were disabled; `job-update.schedule` was re-enabled at 900-second interval; `scripts/shared/auth-guard.ts` now starts detached browser auth via `keep-alive.ts --allow-login --force`; `scripts/auth/setup-launchd.sh` no longer registers fixed auth jobs; file-level TypeScript check passed; success notification lock `auth-notified-2026-06-01.lock` was present after auth at 10:20 JST.
- REQ-017: `仕事` GWS auth recovery no longer opens Terminal or the GWS scope picker, and suppresses repeat browser prompts while the auth-failure lock exists. Evidence: on 2026-06-02, `scripts/shared/auth-guard.ts` switched GWS recovery to detached `gws auth login --scopes <OAuth URLs>` with `/tmp/auth-guard-gws-recovery.lock`; browser auth now starts only for `expired`, `missing_scope`, or `no_credentials`, not status-check timeouts; docs and the old manual wrapper were updated; `tsx scripts/shared/auth-guard.ts`, `bash -n scripts/shared/gws-login-with-browser.sh`, `git diff --check` in `仕事`, `gws auth status --format json`, and Sheets `values.get` passed; stuck `gws-login-with-browser.sh` / `gws auth login -s` processes were terminated and GWS locks cleared after `token_valid=true`.
- REQ-018: `仕事` circus-job.com job search automation with recommendation scoring. Evidence: on 2026-06-04, `scripts/circus-job-search` was added; `npx tsc --noEmit` passed; `npm run search -- --pref 東京 --keyword 営業 --age 28 --education 高卒 --max 5` returned 5 jobs with ranked recommendations; `npm run search -- --candidate-file candidate.example.json --max 3` returned 3 jobs with S-ranked recommendations.
- REQ-019: `仕事` circus-job.com must-have requirement gating. Evidence: on 2026-06-04, `scripts/circus-job-search` added `--driver-license`, `--license`, `--experience`, and `--include-disqualified`; output now includes detected required requirements and disqualifying reasons; `npx tsc --noEmit` passed.
- REQ-020: `仕事` circus-job.com expanded-pool office-job ranking. Evidence: on 2026-06-04, `scripts/circus-job-search` added `--pool`, office-fit classification, lane output, grouped Markdown, session-takeover login confirmation, idle-stop scraping, and stricter must-have gating; `npx tsc --noEmit` passed; the 博多駅30分・事務・年収360万円・30歳・免許なし run returned 2 eligible jobs from 25 collected jobs after strict filtering.
- REQ-021: `仕事` circus-job.com scarce-result proposal scheme. Evidence: on 2026-06-04, `scripts/circus-job-search` added proposal output with market verdict, candidate-info questions, adjacent office-related searches, unlock suggestions, and near-miss jobs separated from strict recommendations; `README.md` documents the scheme; `npx tsc --noEmit` passed; `output/result-2026-06-04T14-38-14.md` contains 2 strict recommendations plus adjacent/pivot guidance.
- REQ-022: `仕事` circus-job.com vector-based scarce-result handling. Evidence: on 2026-06-04, `README.md` added vector-based additional-search guidance; must-have detection was tightened for driver-license wording, customer-facing experience, HR/recruiting experience, PT/OT/ST licenses, new-grad-only, and sales-leadership mismatches; `npx tsc --noEmit` passed; live vector searches produced current, commute60, salary330, no-salary, CS/BPO, HR/recruiting, and sales-adjacent outputs.
- REQ-023: `仕事` circus-job.com candidate job proposal Skill with first-step direction setting and page-by-page collection evidence. Evidence: on 2026-06-05, `.claude/skills/circus-job-proposal/SKILL.md` was added; `方針/スキル一覧.md`, `.claude/skills/CATALOG.md`, and `work-skill-guide/SKILL.md` route to it; `scripts/circus-job-search` outputs `pageStats` and Markdown page logs; `npx tsc --noEmit` passed; a small live search produced `output/page-test.json` with page stats.
- REQ-024: `仕事` circus-job.com occupation-family guidance and proposed-job PDF attachment. Evidence: on 2026-06-05, `circus-job-proposal/references/occupation-strategy.md` was added; `circus-job-search` added `--pdf`, `--pdf-dir`, `--pdf-max`, JSON `pdfPath`, and Markdown `求人票PDF`; `npx tsc --noEmit` passed; `output/pdf-test.json` and `output/pdf-test.md` show one saved PDF.
- REQ-025: `kimi-webbridge` logged-in Arc workflow support. Evidence: on 2026-06-19, daemon and Arc extension were aligned at v1.10.0; Arc-safe `arc-session.sh` was added; `screenshot.sh` was updated for path-return screenshots; skill docs were updated; `quick_validate.py`, `bash -n`, health, two-session switching, click URL refresh, snapshot/evaluate, and screenshot smoke tests passed.
- REQ-026: `short-video-create` reusable progressive-disclosure Skill. Evidence: on 2026-06-19, the Skill was added; on 2026-06-22, it was renamed to `short-video-create` with six workflows, five references, three asset templates, and global symlinks in `.codex/skills`, `.claude/skills`, and `.agents/skills`; trailing-whitespace check returned no matches; `bash scripts/agent-instructions/check-skill-compatibility.sh` passed after the rename.
- REQ-027: `coding-task-orchestrator` confirmation-first development-task orchestration Skill. Evidence: on 2026-06-26, static Skill docs and supervisor evaluation patches were in place; the basic six live prompt cases passed in `docs/specs/coding-task-orchestrator-skill/live-manual-prompt-test-2026-06-26.md`; the additional six live prompt cases passed in `docs/specs/coding-task-orchestrator-skill/live-manual-prompt-test-additional-2026-06-26.md`; real branch/worktree representative operation evidence was recorded in `docs/specs/coding-task-orchestrator-skill/real-operation-evidence-2026-06-26.md` with branch `chore/cto-real-op-evidence-20260626`, worktree `/Users/kitamuranaohiro/Private/focusmap-wt-cto-real-op-evidence-20260626`, commit `e938272e14ea93092455ab752fa8e4d6bfe27b9c`, worker return supervision, closeout judgment, and explicit non-execution of prohibited operations.

## In Progress

- None.

## Proposed

- None.

## Needs Verification

- REQ-003: `requirements-governor` skill file placement resolves for Claude Code and Codex, but Claude Code runtime detection is unverified because `claude --help` fails with a missing native binary.
- REQ-007: `note-create` ImageGen-first visual rule and non-destructive note login check were implemented on 2026-05-29, but end-to-end draft creation still requires `note-nextlevel` reauthentication.
- REQ-015: Local `mlx-whisper` transcription pipeline and units-based `caption_blocks` generator were added on 2026-06-01; `unittest` and `py_compile` passed, but real `mlx-whisper` inference still needs local dependency/model installation and sample media verification.
- REQ-016: `ai-news-short-video` now has a transcription decision gate for ordinary edit assets, quoted videos, existing MP4s, interviews, and caption timing work; global `video-transcription` symlinks were added, but one ordinary TTS edit and one quoted-video edit should still be run to verify the automatic use/skip decision.

## Blocked

- None.

## Deferred

- Full repository product audit. Run Audit Mode after the governance base is installed.

## Deprecated

- None.
