# 実装開始プロンプト（次セッションへ貼る用・2026-07-15作成）

計画レビュー完了後の実装開始時に、新しいセッションへそのまま貼る。本文とこのプロンプトが食い違う場合は program.md が正。

---

計画「計画立案・実行・完了基盤」の実装を開始してください。

## 前提（2026-07-15に人間承認済み）

- 計画正本: `personal-os/my-brain/areas/ai運用/plans/planning/2026-07-15-計画立案実行完了基盤/`（program.md＋plans/01〜06。全てcommit済み）
- 承認済み事項: 計画内容（6子・6Wave）／worktreeの自動削除と統合branchへのmerge（program.md「本programの計画承認に含まれる承認事項」）／実行開始ゲート＝記載→図解→合意（同「完走スキーム」0項）
- 実装は子計画単位で `codex exec` へ委譲し、あなたは設計・指揮・統合判断を担当する。reviewerは実装と異系統・read-only。

## 最初にやること（順番どおり）

1. `program.md` を読む（運転ルール・worktree 8段・レビュー運用・人間ゲートが正本）。
2. active枠を確認する。ai運用のactiveは上限問題があるため、人間の指示した枠の空け方（既定候補: 「計画運用ハーネス検証」programの完了確認を先に済ませる）を実行してから、`bucketctl` で本programをplanning→activeへ昇格する。5件目の昇格・他計画の自動退避は禁止。
3. Wave 1: 子01（計画テンプレート・lint・委譲入口）を実行契約どおりcodexへ委譲する（delegated-single）。完了したら**都度レビュー**（評価01.md・全PASSまで）。
4. 以降はprogram.mdのWave順: 02 → 03 → 04 →（02・03・04の3子一括レビュー）→ 05 →（06のE2E時に05のレビュー消化）→ 06 → 承認セット1枚を人間の最終一括確認へ。
5. hooksの実装（子04）は `references/2026-07-15-hooks設計調査.md` を設計正本として読む。資料02（`references/2026-07-15-計画実行基盤/`）の旧path・旧子番号は現構成へ読み替える（program.md正本境界に注記あり）。

## してはいけないこと

- hook登録・`~/.claude/settings.json`／`~/.codex/hooks.json` 変更・symlink・Codex再trust・注入文の有効化（→承認セットへ差分を積む）
- push・mainへのmerge・既存計画のバケット移動・identity.md削除・知識/移動（→承認セットへ候補を積む）
- 旧 `hooks-registry/hooks/` 構造の復活、`git add -A`、hooks-registryの他セッション未コミット差分の巻き込み

## 注記

- `plans/active/2026-07-14-計画運用ハーネス検証/program.md` に旧フォルダ名参照の追従2行が未コミットで残っている（同ファイルの他セッションhooks再編差分と同居のため分離不能だった）。そのセッションのcommitに任せるか、差分を確認のうえ単独でパス指定commitしてよい。
- 進捗はsession-boardへ（--plan "2026-07-15-計画立案実行完了基盤"）。子の状態変更はprogram.mdマップと同じコミットで更新する。
- 人間向けの視覚説明は `explain/program.html`（v5）と `explain/子計画02-04詳細とhooks設定.html`。計画を大幅更新したらHTMLも追従させ、日付renameの要否も確認する。
