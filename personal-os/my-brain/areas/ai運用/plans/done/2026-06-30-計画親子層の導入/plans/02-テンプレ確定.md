分類: 横断
種別: 新規作成
親計画: ../program.md

# テンプレ確定

## 目的
program.md / 子.md / run-card の3テンプレを正本化し、本programやrun-cardが沿う形にする。

## 方針
- program.md＝ `分類`/`種別`/`形態: program` ＋ 目的 / 全体像 / 子計画マップ / 完了条件。
- 子.md＝ `分類`/`種別`/`親計画:` ＋ 目的 / 方針 / 完了条件。
- run-card＝ `担当` / `出所`(絶対パスbacklink) / `依頼` / `許可` / `戻し方` / `差し戻し上限`。

## 完了条件
- 3テンプレが参照可能な場所に1つずつ存在し、本 program 自身がそれに沿っている。
