# 実装の共通コンテキスト（この計画の実装担当が全作業で守ること）

※ 本ファイルは新標準（役割別コンテキスト）のpilot第1号。単発planだが実験的に採用している。

## 対象と環境

- 対象repo: `~/Private`（personal-os配下のみ）。システムPython 3.9・bash。外部依存を増やさない。
- 変更対象は plan.md「変更対象（見込み）」の列挙内に収める。超えそうなら実装を止め、計画を修正してから続ける。

## 守る契約

- worker読み順の組み立ては `agents-registry/harness/delegate.py` の `render_task_packet` 内で完結させる。第2の組み立て場所を作らない。
- 実装/共通.md・レビュー/共通.md・評価/ のpathはprogramフォルダから機械導出する。manifestへ新フィールドを追加しない。
- 評価の置き場は「評価/ 優先 → plans/ 隣接フォールバック」の両対応。既存計画のファイルは移動しない。
- hooks登録（`~/.claude/settings.json`・`hooks-registry/codex/hooks.json`）に触らない。

## テスト（絶対pathで・全スイート）

- harness: `agents-registry/harness/tests/test_delegate.py`・`test_program_run.py`
- plan-ops: `skills/plan-ops/__tests__/run.sh`
- plan-closeout: `hooks-registry/shared/plan-closeout/tests/test_plan_closeout.py`
- session-board: `hooks-registry/shared/session-board/tests/`（test_common.py ほかpython直実行＋test-session-board.sh）

## スタイル

- 既存ファイルの流儀に合わせる。コメントはコードで表せない制約の説明だけ。
- 検証コマンドは絶対pathで打つ（cwd依存grepで自己検証が空振りした過去事故: 2026-07-16 計画入口一本化・評価01）。
