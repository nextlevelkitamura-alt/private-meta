---
name: <小文字とハイフン・64字以内・動名詞形推奨。helper/utils のような曖昧語は禁止>
description: <第三人称。①何をするか ②いつ使うか（ユーザーが打つ日本語トリガー語） ③〜には使わない（近接Skillとの否定トリガー）。主要ユースケースを先頭150字に。設計基準は references/create-rules.md §6>
# 副作用（削除・外部送信・デプロイ・課金・不可逆変更）があるSkillだけ、以下を検討（create-rules.md §7）:
# disable-model-invocation: true
# allowed-tools: <コマンド単位まで絞る。例 Bash(git add *)>
---

# <Skill名>

<このSkillが何をするか1行。判断基準は create-rules.md、実行手順は下記または workflows/。>

## 1. 役割

1. <このSkillが担当すること>
2. <対象にする作業>
3. <対象外にする作業>

<!--
構成の選び方（create-rules.md §4）:
- 単一の自然な流れで150行以内に収まるなら workflows/ を作らず、下の「手順」に直書きする（＝この節を使う）。
- 手順が長い・分岐する・別々の副作用があるなら router 型にし、「手順」の代わりに「Workflow振り分け」を置く（下のコメント参照）。
どちらか一方だけを残し、もう一方は削除する。
-->

## 2. 手順（単一フロー型の場合）

1. <Step 1>
2. <Step 2>
3. <Step 3>

<!--
## 2. Workflow振り分け（router 型の場合。単一フロー型なら削除）
1. `workflows/<workflow>.md`: <どんな発話で使うか>で使う。<何を判断または実行するか>。
2. `workflows/<workflow>.md`: <どんな発話で使うか>で使う。<何を判断または実行するか>。

## 委譲（他Skillへ渡す条件があれば）
1. <条件> → <委譲先Skill>。

## 迷ったら
1. 対象pathや目的が曖昧なら、推測せず短く確認する。
-->

## 3. 安全方針

1. 副作用: <読み取りのみ / 書き込み / 削除・外部送信・不可逆>
2. 確認・承認: <不要 / 差分提示 / 明示承認が必要な操作>
3. 禁止操作: <あれば>

## 4. 出力（applied系＝成果物を作るSkillだけ）

1. 出力先: 所属repoの outputs 規約に従う（`outputs/<用途>/YYYY-MM/`。正本は create-rules.md §8）。
2. 出力するもの / 報告する確認結果。
