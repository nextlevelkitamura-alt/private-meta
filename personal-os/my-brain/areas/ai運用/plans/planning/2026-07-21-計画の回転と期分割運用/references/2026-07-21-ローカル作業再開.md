# ローカル作業再開（2026-07-21 リモートセッションからの引き継ぎ）

## 現在地（リモートで完了済み）

- AI討論（3案の立案×反対尋問）を実施。結論: md上位層の新設・programの格上げは不採用。**テーマ=傘（参照で束ねる・DB正本）／program=期（所有して束ねる・有期）／閉じる運用の改善**で「programが終わらないと次ができない」を解消する。
- 企画 **「計画の回転と期分割運用」** を planning へ起案（このフォルダ）。done上限は **無制限＋定期促し** で人間裁定済み（2026-07-21）。
- 人間向け図解: `explain/plan.html`。当日の実行手順: `references/2026-07-21-初回クローズ儀式チェックリスト.md`。
- ブランチ `claude/program-structure-naming-m4cxur` ／ PR #3（draft）。**mainには未反映**（ローカルmerge＝承認）。

## 次にやること（ローカル）

チェックリストの手順0→1→2（余力があれば3→4）。
規約改定・bucketctl改修・stale検知などの**実装系は今日やらない**（「2026-07-19-計画とカスタムエージェント整理」のLane A再編完了後に、この企画をactive昇格してから）。

## 作業再開プロンプト（ローカルの Claude Code / Codex に貼り付け）

```text
~/Private で作業する。目的は、企画「計画の回転と期分割運用」の最初の一歩（クローズ儀式）の実行。

## 手順0: リモート成果の取り込み
1. git fetch origin claude/program-structure-naming-m4cxur
2. git diff main...origin/claude/program-structure-naming-m4cxur --stat を私に見せる（追加5ファイルのみのはず: plan.md／explain/plan.html／references/チェックリスト・ローカル作業再開・実装ドラフト集）
3. 私のOK後に git merge --no-ff origin/claude/program-structure-naming-m4cxur

## 手順0.5: 文脈の読み込み
1. personal-os/my-brain/areas/ai運用/plans/planning/2026-07-21-計画の回転と期分割運用/plan.md（企画本文）
2. 同 references/2026-07-21-初回クローズ儀式チェックリスト.md（今日の実行手順の正本）
3. 必要時のみ 同 explain/plan.html（図解）

## 手順1: done 8本の一括クローズ
- done/ の8計画それぞれについて、完了条件と最終評価mdを確認し、終了区分 completed の終了記録を起草して一覧で私に提示する。
- 私の承認後に bucketctl（既定dry-run → --apply明示）で done→archive を1本ずつ実行する。
- 全件完了後、「done残数・active空き枠」を1行で報告する。

## 手順2: 「2026-07-15-計画立案実行完了基盤」を閉じる
- active/2026-07-15-計画立案実行完了基盤/承認セット.md を要約して私に提示する。
- 私の承認後に active→done→archive（終了記録つき）を bucketctl で実行する。

## 手順3（余力があれば）: 当日ボードSQL化の5子の人間ゲート
- active/2026-07-17-当日ボードSQL化/program.md の「人間ゲート」節を正本に、migration適用・focusmap push・Cloud Run本番反映の実行計画（順序・対象・確認方法）を提示する。実行はすべて私の個別承認後。実機目視の結果は該当評価mdへ記録する。

## 手順4（子09の本番反映後）: テーマ登録の実践
- テーマ「計画の回転改善」をチェックリスト手順4の文面（目的・完了条件）で登録し、plan_refs に本企画を登録する。名前を変えたい場合は先に私へ確認。

## 守ること
- バケット移動は必ず bucketctl（既定dry-runで提示 → 私の承認後に --apply）。--force・自動退避は使わない。
- git add -A 禁止。自分が変更したファイルだけをパス指定でコミット。push は私が明示した時だけ。
- 既存の未コミット変更は私の作業として扱い、巻き戻さない。
- 各手順の完了ごとに1〜2行で状態を報告し、判断が要る点はまとめて聞く（1件ずつ細切れにしない）。
```

## 補足

- merge後、リモートブランチ `claude/program-structure-naming-m4cxur` は片付け対象（削除はあなたの承認後）。mainをpushした時点でPR #3は自動的にmergedになる。
- この引き継ぎ文書は初回実行が終わったら役目を終える（企画の実装評価には使わない）。
