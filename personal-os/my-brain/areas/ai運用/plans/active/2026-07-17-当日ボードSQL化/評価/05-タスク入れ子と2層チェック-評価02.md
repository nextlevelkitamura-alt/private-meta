対象計画: `plans/05-タスク入れ子と2層チェック.md`（program「当日ボードSQL化」子05）／ラウンド: 02（修正01確認）／diff範囲: focusmap 194e3b6e→b8bbf058（1コミット）／評価者: reviewer（Opus・実装と別セッション）／評価日: 2026-07-18

## 項目5（評価01 FAIL）の再採点 → [PASS]

- DoneTodoRow が fix行（kind='fix'）ごとに FixReattach を描画。呼び出し側で selectedDate・fixTargets を結線（修正指示どおり）
- 付け替え先候補は `aiTodos.filter(todo => todo.id !== currentId)` ＝当日のopen＋完了済み両方・自分自身除外（指揮官裁定と一致）
- reattachFixStep の契約は無変更（kind='fix'のみ・todo_id/seq移動・履歴書換えなし）
- 「手直し中」表示・自動reopen禁止は維持

## 回帰確認

- `git diff --stat 194e3b6e b8bbf058` 実測: page.tsx 1ファイルのみ（+31/-6）。%導出・状態ラベル・完了/undo・質問回答の各経路は非接触
- `npm run build` 再実行: Compiled successfully

## 人間ゲートの先行実行なし（実測）

- push なし（main...origin/main ahead 4 のまま）・migration適用痕跡なし・board_route正本追記なし（grep再実行でマッチ0）・secret混入なし

## 総合判定

**FAIL 0（項目5がPASSに転じ、静的検証で全PASS）。修正ラウンド完了。**

保留は評価01の解除条件を引き継ぐ:
- DB実挙動: migration2本（inbox: todo_steps/質問カラム・board: session_logs.todo_id）の適用後にローカル3001で実データ確認
- board_route宣言の実挙動: 正本への1本追記（人間ゲート）後に⚙直行確認
- 375px目視: 記録付きで実施（原則は親の最終一括に束ねる）。完了済み行の付け替えUIも目視対象に含める
