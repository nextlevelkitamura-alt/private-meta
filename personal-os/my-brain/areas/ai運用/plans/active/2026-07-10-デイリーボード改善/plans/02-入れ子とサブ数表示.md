親計画: ../program.md ／ 分類: 横断 ／ 種別: 既存改善

# 02 入れ子とサブ数表示

## 目的

「動いているエージェント」節を、目標→セッション→サブエージェントの入れ子で表示し、どのセッションがどの目標に属し、サブが何体走っているかを1画面で読めるようにする。

## 現状

セッション行はフラットで、`sort_agents()` が目標グループ順に隣接させるだけ。サブエージェントは行状態🔵になるのみで、体数・役割は見えない。

## 方針

1. 表示イメージ（program.md 全体像・UI設計Artifact参照）:
   - `### セッション` 相当の下に `- <絵文字> <目標名>` の親行、その下にセッション行をインデント、さらに下に `↳ 🔵 サブN体: <ラベル>` 行。
2. サブ体数はAIに書かせず機械で増減: Claude=SubagentStart/SubagentStopフック、Codex=既存 `subagent.py` の拡張。
3. 時間・所要はこの画面に出さない（時間は03のTurso集計で見る）。
4. 確定（2026-07-11・設計レポートの推奨を採用しユーザーGO）:
   - 互換戦略: 3regexの先頭を `^\s*- ` に緩和し物理2字インデント。旧v1/v2/v2.2は読み取り互換・書き込みは常にv3（自然移行）。
   - 見出し: `### セッション` は置かず `### 本日の目標` に統合（目標親行が索引を兼ねる・（N件）=セッション数）。
   - サブ保持: 行末尾コメント `<!-- s:KEY sub:N -->`（欠落=0の後方互換）。表示はラベル無し `↳ 🔵 サブN体`（B案）。reconcileの🔵→⏸降格でsub 0クリア。

## 完了条件（レビュー項目）

- [x] 未確定3点が確定し、本mdに追記されている（人間確認済み）。
- [x] 目標→セッションの入れ子が当日デイリーで機械生成される。
- [x] サブエージェントの開始・終了で体数表示が自動で増減する（AIの手書きなし）。
- [x] 既存3世代フォーマットの読み取り互換が維持され、テストが全緑。
- [x] ボード区画に時間・所要の情報が出ていない。
