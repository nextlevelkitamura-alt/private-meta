# 評価02（ラウンド02・修正確認）

- 対象計画: `2026-07-18-Codexスキル露出のagents一本化/plan.md`（評価01の差し戻し2点の解消確認）
- ラウンド: 02
- 評価者: 指揮官（Fable 5・impl-reviewer評価01を受けた修正の独立再検証）
- 前提: 評価01でR01〜R09・R12=PASS。差し戻しはR10（check-exposureのNFC/NFD比較バグ）とR11（rollback未検証）の2点。

## 修正の再検証（実測）

- **R10 [PASS]** `check-exposure.sh`・`link-global-skill.sh` の正本一致判定を inode等価テスト `-ef` へ置換（生文字列比較を廃止）。既存の欠落／非symlink／broken link判定と順序は不変。
  - 再検証: `check-exposure.sh` を **/tmp（絶対パス直呼び）と ~ の2cwd**で実行し、出力が**完全一致**。「symlink先不一致」の誤検出=**0**、「Codex二重登録」=**0**。残drift=standup/inbox-triage の欠落8件のみ（元々未露出・スコープ外＝正しい検出）。評価01で報告された99件誤検出は再現せず解消。
  - `link-global-skill.sh --dry-run html/kickoff` を /tmp から実行しても4窓／claude限定を正しく「Already linked」判定。
- **R11 [PASS]** `references/rollback-codex-skills.sh`（既定--dry-run・snapshotを動的パースし正本22件のみ対象化・起業スキル5件は自動除外）を新規整備。
  - 再検証: `--dry-run` で **22件**の `ln -s <target> ~/.codex/skills/<name>` を生成、全targetが実在。`bash -n` OK。`--apply` は未実行（実HOME不変）。
  - 指揮官判定: 評価01が提示した選択肢のうち「snapshot＋復元スクリプト＋--dry-run検証をもってrollback可とする」を採用・承認する（実際の削除→復元の往復は削除ゲートを再発火させるため実施せず、復元素材の完備と復元コマンドの妥当性確認で代替）。

## 軽微指摘の解消

- plan.md §実行契約のmanifestパス表記を実配置 `scripts/exposure-manifest.tsv` に修正済み。

## 総合判定

**全PASS。** R01〜R12すべて満たし、評価01の差し戻し2点（R10バグ・R11保留）を解消。doneゲート達成。

## 残す申し送り（スコープ外・別タスク）

- `standup` 孤立（catalog/logs/全窓未登録）と `inbox-triage` 意図的未露出は check-exposure が「欠落」で正しく検出。本計画スコープ外。将来 manifest に `none`（意図的未露出）を足すと drift から除ける。
- `起業スキル/skills/*` 5件が repo-local ながら `.codex`/`.agents` 両窓ミラーで二重登録中（link-global-skill.sh管理外）。repo-local Skill露出の別計画で扱う。
