親計画: ../program.md ／ 分類: 横断 ／ 種別: 統合整理
規模: フル
形態判定: Program子 ／ 理由: 子02・03の成果を統合し二重書きを畳む最終子のため親で束ねる
並列: 不可 ／ レビュー: 都度
人間ゲート: md→DBミラー送信の廃止（不可逆）・origin/main push・本番反映

# 方向修正ビューとミラー廃止

## 目的

的（slug）×当日実績の照合ビューをfocusmapに作って「目標の方向修正」をできるようにし、md→DBミラー・二重書きを廃止して一本化を完了する。

## 非対象

- 的の文面のDB化（md正本のまま。DBは参照のみ）
- 新しい目標管理手法の導入（既存ゴールladderのslug規約に従う）

## 現状

- 子01〜03完了後の前提: todos箱・両ビュー・起票キュー・DB先書きが揃い、ミラー系コード（turso/store.pyの送信・spool）だけが旧経路として残る
- 的のslug規約と進捗行（機械専用）の正本は my-brain/ゴール/AGENTS.md

## 実行契約

- 対象repo: /Users/kitamuranaohiro/Private/projects/active/focusmap（方向修正ビュー）＋/Users/kitamuranaohiro/Private/personal-os/AIエージェント基盤（ミラー廃止）
- 実行形: delegated-single（repoごとに順番に実施）
- 最初に読む順番:
  1. focusmap/AGENTS.md → hooks-registry/shared/session-board/AGENTS.md
  2. ../program.md
  3. 実装=../実装/共通.md ／ レビュー=../レビュー/共通.md
  4. この計画
  5. my-brain/ゴール/AGENTS.md（slug規約・進捗行）
- 依存成果: 子02（ビュー・検知キュー）・子03（DB先書き）
- 変更可能範囲: focusmapの方向修正ビュー・session-boardのミラー送信部（削除）・関連AGENTS.mdの追従更新
- 変更禁止範囲: 的の文面・過去デイリー・todosスキーマの破壊的変更
- ファイル担当マップ: 不要
- worktree方針: 不要
- 維持する契約: goal_ref は的slugへの参照のみ（本文コピーなし）・進捗行は機械専用のルール・削除は人間承認後のみ
- 検証: 全画面（今日・2週間/月・起票・方向修正）の本番スマホ動作＋ミラー関連コード0件のgrep＋spool残ゼロ
- 停止・エスカレーション条件: ミラー削除の承認前・削除後に記録欠損が見つかった時（即報告・復旧判断へ）
- 完了時に返す情報: result packet（status / base_commit / result_commit / changed_paths / tests / assumptions / blockers / remaining_risks / out_of_scope_findings）

## 方針

- 方向修正ビュー: 月間/年間の的（slug）ごとに、紐づくやること（goal_ref）と実行時間（sessions集計）を並べ、「的に対して今日・今週が向いているか」を1画面で見せる。的の文面表示はmdから読むかslug参照のみとし、方式は実装時に決めてこの計画へ追記
- ミラー廃止: 子03の新経路が安定運用（目安2週間、または人間の前倒し判断）に達した後、md→DB送信コードとspool処理を削除する

## 完了条件（レビュー項目）

- [ ] 方向修正ビューで的slug×やること×実行時間の照合が表示され、的の本文コピーがDBに無い（対象: 新ビュー・DBスキーマ）
- [ ] md→DBミラー送信コード・spool処理が削除され、grepで参照が残らない（対象: session-board turso/・state/）
- [ ] 今日ボード・2週間/月・起票・方向修正の全画面が本番URLでスマホ動作する（対象: focusmap本番）
- [ ] 関連AGENTS.md（shared/session-board・focusmap docs/CONTEXT.md）が新構成へ追従している（対象: 両文書）
- [ ] 統合評価 `../評価/評価01.md` が全PASS（対象: 評価/）

## 実装結果

実装後にplanctlが追記・更新する。実行前は記入しない。

## 終了記録

archive時に必須。実行中は記入しない。
