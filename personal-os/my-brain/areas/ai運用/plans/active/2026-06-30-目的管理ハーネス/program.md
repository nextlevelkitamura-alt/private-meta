分類: 横断
種別: 新規作成
形態: program

# 目的管理ハーネス

## 引き継ぎ（会話を圧縮/新セッションでも続けられる・最初に読む）
- 読む順: ① この program.md → ② 子04 `plans/04-実行レーン自動化とデイリー自動ログ.md` → ③ 基盤 `loops-registry/references/loop-types.md`（実行方式）→ ④ program B `../2026-06-29-OrcaCLI複数エージェント運用/plan.md`。
- 現在地: **子04＝実装7/7完了・基盤へ統合・push済**（配管は全通・大半は"停止"＝蛇口が閉じてる）。**ゴール(子01)の中身は空**。実行〜統合は今も人手。
- ゴール（本programの成功条件）: 下「ロードマップ」の**8割自動**＝1ジョブが `ready→archive` を「**的から選ぶ＋push承認**」の2点だけで完走する定常運用（残り2割＝的決め・重要承認は意図的に人間）。
- 次の一手: **Step①（記録の輪）** ＝ ①ゴールを粗く記入(人) → ②今日のデイリー作成 → ③hook登録(`~/.claude`・人承認) → ④毎朝のデイリー自動作成loop(新規) → ⑤夜digest有効化(23:30) → ⑥この2 loopを基盤 `loops-registry` に登録。
- 体制: **このセッション＝計画・監督・レビュー本部**（戻り報告をここで評価）。実行＝Orca(キュー既定)か委譲セッション。並行「評価やく」セッションが基盤 `AGENTS.md`/`loop-types.md` を編集中＝**基盤ファイルは同時編集しない**（plan編集はこの Private repo 側に限定）。
- 決定事項: 状態の正本=ai-jobs 6フォルダ／Orca=見える実行面／監督skillは「振り分け」と「統合closeout」の2本／レビューは折り畳み可／一気通し(ready→done,push前だけgate)は後付けモード／実行方式は迷ったらキュー(Orca)・安定でheadless卒業。

## 目的
「目標(3年/年間) → デイリー → 計画 → 実行 → 完了 → ダッシュボード」を、状態がズレず・人がいなくても回るハーネスにする。今は人の記憶と手作業が接着剤になっている部分を、スクリプト・規約・整合した地図へ移す。

## 全体像
- 役割分担: A＝計画の構造、B＝どう実行するか、C(本program)＝何を目指し今どこか(目標・デイリー・ダッシュボード)。
- 本Cが新設するのは「縦のladder(目標→デイリー)」と「状態の1枚(ダッシュボード)」。計画構造(テンプレ/ready/レビュー/plan-ops/run-card)はA、実行(Orca監督/worktree/closeout)はBに足す。重複させずbacklinkで束ねる。
- 不変条件: 状態の正本＝ダッシュボード(上書き)、履歴の正本＝デイリー(追記)、実行レーン＝ai-jobs一本、loop＝時計(計画は実行しない)。
- 今回スコープは設計(枠)まで。ROIとOrca細部はパーク(下記「後続」)。

## ロードマップ（8割自動への時系列）
下に行くほど難しく効く。①は安く即効なので土台に先行、②で技術の未確認を潰し、③(本丸)で8割到達。④は更に上。
- **① 記録の輪（今）**: ゴール記入 → デイリー自動作成 → hook登録 → 夜digest有効化 → loop基盤登録。輪の中心(的)と記録が入る。 ▸ 子01・子03・子04(⑤⑥⑦)
- **② 実行の自動化**: 実ワーカー1枚の実走スモーク(唯一の技術未知・要承認) ＋ Orca監督=振り分けskill化(PoC→skill)。ready に置けばAIが実行。 ▸ 子04(③④)・program B
- **③ 統合の自動化（本丸）★8割ライン**: 統合closeout skill＝独立レビュー→競合/merge→**push前gate**→archive。1ジョブが端まで通る。 ▸ program B
- **④ 投入の自動化（更に上）**: 的→今日のジョブ→run-card 生成。勝手に積まれて回る。 ▸ 新規(子01×ai-jobs)

## 子計画マップ   ※ 子の状態変更と同じコミットでここを更新

01  ゴールladder構造 … done
    次: 置き場+テンプレ実装済(my-brain/ゴール/)。(人)実目標を記入／フォルダ名`ゴール`確認
    場所: plans/01 ／ 依存: ―

02  ダッシュボード昇格 … active
    次: A:昇格済(my-brain/ダッシュボード.md)。B:dashboard.sh自動生成は子03・実job発生後
    場所: plans/02 ／ 依存: 01・A-03

03  朝夜ルーティン … done
    次: 手順書化済(ゴール/朝夜ルーティン.md)。skill化は子04で判断
    場所: plans/03 ／ 依存: 01・02

04  実行レーン自動化とデイリー自動ログ … active（実装7/7完了・基盤へ統合push済／残＝有効化）
    次: Step①(記録の輪)＝hook登録＋デイリー自動作成loop(新規)＋夜digest有効化。dispatcher有効化は実走スモーク後
    場所: plans/04 ／ 依存: 03・A-03・B

## 完了条件
- my-brainにゴールladder(3年/年間/デイリー)とダッシュボードが存在し、dashboard.shがplans/とai-jobs/の状態から自動生成する(子01,02)。
- 朝夜ルーティンが目標・ダッシュボード・デイリーを突き合わせ、逆算Alignmentとボード更新を回す(子03)。
- A/Bへの追補(レビューゲート/ready統一/run-card共通契約/監督振り分け/dashboard再生成)が反映され、設計のドリフトが0。
- ROI/評価基準は本ハーネスを一定運用後に別途設計(現状未確定・後続)。

## 関連 (重複させずbacklink)
- program A「計画親子層」= `../2026-06-30-計画親子層の導入/program.md`。本Cの設計を次の子へ足す: テンプレ＋レビュー項目(A子02)、plan-ops＋run-card=repo-aware共通契約＋cross-repo2コミット戻し(A子03)、計画立案パイプライン統合(A子04)、ready統一(A子01規約)。
- program B「OrcaCLI複数エージェント運用」= `../2026-06-29-OrcaCLI複数エージェント運用/plan.md`。監督＝振り分けスキル(最初に作る)、worktree実行、worker_done、closeout、ダッシュボード再生成(単一writer)。
- 接点は `run-card`(repo-aware共通契約) と `ダッシュボード.md`。順序は A→C→B で「何を→今どこか→どう実行」。

## 後続 (今回パーク)
1. ROI・評価基準: 本ハーネスを一定運用後にテストして設計(別途)。
2. Orcaの細かなskill中身・誘導ルール・skillをloopフォルダのどこへ置くか: program B内で後続。
