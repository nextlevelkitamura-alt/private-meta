分類: 横断 ／ 種別: 統合整理 ／ 優先: ◎

# 指揮官体制の縮小と安定化（マルチ指揮官 → まず最小の安定形）

## 目的
拡大方向で設計した「マルチ指揮官体制（連邦制）」を、一度**最小の安定形まで縮小**し、
実行に確実に乗る土台を作る。安定を確認してから、必要な分だけ積み増す。
「立派な設計」より「動く小ささ」を優先する。

## 現状
- 体制がごちゃごちゃで安定していない。設計は進んだが、**実行にまだ十分に映せていない**。
- 既存の拡大側 program（本計画はその縮小・安定化にあたる）:
  `../2026-07-03-マルチ指揮官体制/program.md`（子01〜10）。
- 2026-07-03 時点で移行途中。未完・ズレが残る（下記ナレッジで可視化済み）:
  - GM標準権限セットが未完（子10の完了条件が全未チェック）。
  - ドキュメントと実装のズレ（コックピット既定モデル／受け渡し自動化v1）。
- **ナレッジ（本フォルダ同梱）**: 現状構造を1枚に可視化した
  `Orcaコックピットと指揮系統の構造.html`。
  Orcaコックピット2スキル（orca-cockpit／cockpit-supervisor）・5層の指揮系統・
  正本の場所・参照3層をまとめたもの。**正本ではなく現状把握の資料**（表示専用HTML）。

## 方針
2026-07-04、ユーザーが縮小の方向を提示し、Fable 5 が以下へ具体化した。
第1弾＝**計画運用（my-brain/areas）の縮小と安定化**。指揮官体制本体（ロースター・司令部・レーン数）の
縮小は、この土台の安定後に第2弾として詰める（未確定のまま保持）。
調査結果（サブエージェント3体）は同梱『areas運用の現状調査と縮小設計.html』に集約。
裁定3点は確定済み（下記に反映）。**実装GOは壁打ち後**（2026-07-04時点 未GO）。

### 1. AGENTS.md ダイエット（読ませすぎない）
- 原則: AGENTS.md は上位チェーン（global → Private → personal-os → my-brain）を読んだ後に読まれる。
  上位で定義済みのルールを再掲せず、そのフォルダ固有の説明だけを書く。
- areas/AGENTS.md: 190行 → 上位再掲（約40行）削除＋冗長説明（約35行）圧縮で110行以下へ。
- work/money/health/AGENTS.md（各23行・ほぼ同型）: 固有部分のみ各12行以下へ。ai運用（51行）は30行以下へ。
- 廃止経緯の説明（thinking/・ops/・ダッシュボード残置）は正本から外す（履歴は決定ログとgitが持つ）。

### 2. 人間向けHTML（AGENTS.html・計画HTML）
- areas/AGENTS.md の対として areas/AGENTS.html を新設（正本は常にmd。AI導線からHTMLを参照しない＝グローバル§3）。
- 同時更新ルール: 「対の .html を持つ md を更新したら、同じ作業で .html も更新する」をグローバルCLAUDE.md §3へ1行追加。
  本文の二重記載はしない（契約§8）。areas側には対（AGENTS.html）が存在する事実だけ書く（裁定済み 2026-07-04: グローバル§3のみ）。
- 対を2段に絞る（裁定済み 2026-07-04 壁打ち#1）:
  常設の対（同期必須）＝ areas/AGENTS.html の1枚だけ（下位4本はスリム化後に短くなるためHTML不要）。
  計画HTML＝人間確認ゲートに入る節目だけ生成する使い捨て（同期義務なし・古くなったら作り直す・既定quick）。
  全active計画への一律義務にはしない。

### 3. 計画の過剰分割の抑制
- program化の条件を強化: 子＝独立に卒業/実行される単位のみ。子は最大3〜4本。超えるなら計画自体を分ける。
- 30行未満で書ける内容は子計画ファイルにしない（program.mdマップの「次:」か plan.md の方針に書く）。
- program.md は索引に徹する（目的・全体像・子マップ・完了条件）。時系列の裁定ログはデイリー/決定ログへ、資料は references/ へ。
- 計画フォルダ直下は plan.md（program.md）＋対HTMLのみ。draft.md 等の平置きをやめ、資料は references/ に一本化。
- 既存2 program（状態と記録の統合設計＝子8・マルチ指揮官体制＝子10）は作り直さず legacy として完走させる（裁定済み 2026-07-04）。

### 4. レビュー項目の必須化と評価サブエージェント
- 実行ゲート強化: レビュー項目未確定の計画は実行に出さない（plan-triage／構成カードで存在を確認）。
- 実装完了後は、実装者と別のサブエージェント（レビュー役）が「計画のレビュー項目」に対して判定する。
  項目自体に欠陥があれば修正案ごと差し戻す。契約§2のレビューゲートへ1行で接続（別レーンは新設しない）。

### 5. ナレッジと卒業の受け皿
- 計画付随の調査・資料は計画フォルダの references/ に一本化。領域の恒常的な判断軸は identity.md（現行どおり）。
- 卒業時に受け皿（<repo>/plans/・基盤の卒業先バケット）が無ければ、卒業手順の中で生成する
  （現状: 卒業実績0・移行済みログ0・卒業先バケット未生成を2026-07-04調査で確認）。
- repo-create の最小構成へ plans/ は含めず、卒業時に生やす（裁定済み 2026-07-04）。

## 完了条件（レビュー項目）

- [ ] areas/AGENTS.md が110行以下で、上位チェーン定義済みルール（secret・削除承認・二重管理禁止・push規約・symlink規約）の再掲が無い
- [ ] work/money/health/AGENTS.md が各12行以下、ai運用/AGENTS.md が30行以下で、領域固有の内容だけになっている
- [ ] areas/AGENTS.html が存在し、スリム化後の areas/AGENTS.md と内容が同期している
- [ ] ~/.claude/CLAUDE.md §3 に「対の .html を持つ md の更新時は同じ作業で .html も更新する」旨の1行があり、同じ本文の二重記載が無い
- [ ] areas/AGENTS.md の program 節に「子＝卒業/実行単位・最大3〜4本・program.md は索引に徹する（裁定ログを溜めない）」が明記されている
- [ ] areas/AGENTS.md に計画フォルダ規約「直下＝plan.md（program.md）＋対HTMLのみ・資料は references/」が明記され、active計画の平置き資料が references/ へ移動済み（legacy program の既存構造は除く）
- [ ] plan-triage（構成カード）にレビュー項目の存在確認が入り、運用契約§2に「評価＝計画のレビュー項目に対する判定・実装者と別のエージェントが行う」接続が1行ある
- [ ] areas/AGENTS.md §5 卒業手順に「受け皿が無ければ生成する」が明記されている
