対象計画: 01-計画テンプレートと委譲入口.md ／ ラウンド: 01
diff範囲: base 23e9791 → head task/pf01（commits: a38ab7d, c4809c8） ／ 規模: フル ／ 評価者: sonnet5 impl-reviewer

# 評価01: 計画テンプレート・lint・委譲入口（Wave 1）

## 項目別採点   ※ 子計画「完了条件（レビュー項目）」と同順・全項目を列挙

- [PASS] `templates/` に拡張版3枚と新規3枚（実行指示.md・実行結果.json・終了記録.md）が存在し、実行結果.jsonが有効なJSONである。どのテンプレにもモデルID・worktree・branch・session IDのフィールドが無い。
  根拠: `skills/plan-ops/templates/実行指示.md`（新規58行）・`実行結果.json`（新規13行）・`終了記録.md`（新規11行）を確認。`plan.md`・`program.md`・`子計画.md`は拡張のみでファイル名維持。`python3` でJSONの有効性を確認済み。grep `-niE "model.?id|worktree|branch|session.?id"` で全テンプレを検査し、ヒットは「worktree方針: <不要/task-scoped>」（ポリシー欄・実インスタンス値ではない）と「実行場所・作業branchはハーネスのmanifest割当を確認し、自分で作成・切替しない」（地の文）のみ。実runtime値を書き込むフィールドは無い。

- [PASS] plan-lintが 正常plan通過／必須節欠落／placeholder残存／実行契約欠落／子backlink不正／マップ必須行欠落／delegated-parallel宣言でレーン別担当・worktree方針が未記載 をfixtureで検出し、既存program-lintの全fixtureとplan-opsの全テストが通る。
  根拠: `__tests__/test_plan_lint.sh` の(a)〜(k)が該当7シナリオ全てをカバー。worktree内で再実行した `bash skills/plan-ops/__tests__/run.sh` は **120 pass / 0 fail**（test_bucketctl 25、test_plan_lint 25、test_progctl 19、test_program_lint 17、test_scaffold 34。program-lint系fixtureは今回diffで未変更のまま全通過）。

- [PASS] new-plan.sh／new-child.sh が新テンプレで雛形を生成し、生成直後の雛形が（placeholder検査を除き）plan-lintを通る。plan-lint・テンプレは明示pathを受け、area計画とrepo-local `plans/` のどちらにも同じに適用できる。
  根拠: `test_scaffold.sh`(a)(b)(c)と`test_plan_lint.sh`(j)(k)で生成直後の雛形が`--allow-placeholders`でlint通過することを確認。`plan_lint_core.py`・`new-plan.sh`・`new-child.sh`に`Private|my-brain|areas/ai運用|AIエージェント基盤`のハードコードが無いことを確認。留保: 「合成repoで確認」は汎用tmpディレクトリでの生成・lint通過であり、repo-local構造（独自AGENTS.mdを持つ`projects/<repo>/plans/`相当）を模した専用fixtureは無い（path非依存はコードレベルで確認済み。子06のE2E系統(6)で実構造検証がある前提でPASS）。

- [PASS] `areas/AGENTS.md`・`plan-registry/AGENTS.md` に「レビュー: 一括」の束ね方（依存なし3子程度・契約変更時は即差し戻し）と、計画合意ゲート（explain図解HTMLの提示→相違なしの明示→実行開始。並列はレーン担当の記載後）が1-2行で追記され、既存の都度レビュー原則と矛盾しない。
  根拠: `plan-registry/AGENTS.md`「レビューと人間ゲート」に一括の既定と計画合意ゲートの2行を確認。`my-brain/areas/AGENTS.md`にも実行ゲートと子計画マップ`レビュー:`規約の追記を確認。一括は「直接依存のない子」に限定され都度原則と矛盾しない。

- [PASS] plan-triageの構成カードに 実行形・必要役割・write lane数・worktree・レビュー・人間ゲート・判定理由 が含まれ、Orcaが既定出力に無く、route契約fixtureが通る。
  根拠: `workflows/triage.md` Step 5のカードテンプレで全7項目を確認。Step 4.5とStep 5末尾でOrcaの既定出力からの除外を確認。worktree内で`validate-route-cases.mjs`（exit 0, case_count 7）・`validate-inbox-contract.mjs`（exit 0）の通過を確認。

- [FAIL] handoff-plan-supervisorが `実行指示.md` を正本参照し、独自の指示テンプレ本文を持たない。記入例1件で必須情報（読む順番・変更可能/禁止・検証・停止条件・result packet）が揃う。
  根拠: 正本参照への一本化と旧独自テンプレの削除は満たす。しかし新設「## Task Packet記入例」に Task ID／役割／対象repo／対象計画／base commit／目的／変更可能／変更禁止／依存成果／検証／停止条件／完了時 はあるが、**「読む順番」に相当する行が無い**。完了条件が明示する5情報のうち1つが記入例に欠けているためFAIL。

## 追加指摘（項目に無いが見つけた問題・非ブロッキング）

1. `my-brain/areas/AGENTS.md` §4.2の旧注記「段階語彙、programの最終一括確認、危険操作の個別承認は plan-registry/AGENTS.md に従う。§4.1の英語バケット語彙はフォルダ用で、マップには使わない（plan-ops program-lint.sh が機械チェックする。2026-07-08 決定ログ#12）。」が追記と同時に削除されている。方針2は追記のみを指示しており、意図しないドキュメント後退（program-lint.sh自体は無変更でテストは通る）。
2. `plan-triage/SKILL.md` のfrontmatter `description:` と「## 1. 必ず読む」に旧語彙「起動形」が残存（本文とworkflowは「実行形」へ更新済み）。

## 総合判定

FAILあり（6項目中5 PASS・1 FAIL）→ 修正01.mdへ。判定不能項目なし。
