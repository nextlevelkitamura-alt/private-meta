親計画: ../program.md ／ 分類: skill ／ 種別: 既存改善 ／ 規模: ライト（例外2点のみ事前承認: 基盤mainマージ・実マップへのprogctl初適用）

# 07 plan-ops残機能（旧・計画親子層 子03）

## 現状（2026-07-02 調査済み・保留条件「痛点の再確認」は充足）

1. **マップ手動更新が最大の痛点（実測）**: 当日の~/Private 28コミット中19件がprogram.md子計画マップへの手動Edit+コミット。規約（program.md「子の状態変更と同じコミットでマップ更新」）が手作業を強制している。子04がこのマップを機械parseして描画予定のため、手書きのままだとparse破損リスクも恒常化。
2. **2repo束ねが手作業**: 基盤マージ↔~/Privateマップ更新のコミットペアが当日最低5組。毎回相手hashを本文で言及する束ね規約を手で実施。
3. **静的整合チェックが無い**: 「マップactiveなのに実ファイル無し」「完了なのに完了条件未チェック」「ダッシュボード/README追従漏れ」が当日も再発（手動追従は必ずドリフトする実証）。plan-ops SKILL.md自身も旧レター呼称・モードA前提のままドリフト。
4. **scaffold正本が無い**: テンプレ正本がareas/AGENTS.md §3の埋め込みテキスト。子06トリアージが計画起案するため、単一正本にしないと二重管理の芽（デイリーテンプレ正本化607caa9と同型の解決が可能）。
5. 卒業手順（areas/AGENTS.md §5）は実行実績0回・`移行済み/`不存在。**卒業の自動化は痛点未確認のため保留継続**。

## 方針

1. **progctl**（マップ書き込みの機械手続き）: `progctl.sh set <program.md> <NN> --state/--next/--ref <repo>@<hash>`。該当NNブロックのみ冪等書換え（マップ外の人間記述はバイト不変）。既定dry-run・`--commit`で定型コミット。--refで2repo束ねを定型化。**何を書くかは指揮官の判断のまま＝手だけを機械化**。
2. **scaffold**: `new-plan.sh`/`new-child.sh`。テンプレ本文の正本を `skills/plan-ops/templates/` へ移し、areas/AGENTS.md §3はポインタ化（契約§8: 決定ログ+grep追従）。子06がテンプレ内蔵なら呼び出しに置換（07スコープ）。
3. **program-lint**（静的整合チェック）: マップNN↔実ファイル・backlink解決・状態語彙・完了×未チェックを検査し違反一覧+非0 exit。**ライブなレーン状態は見ない**（watch.sh/子04の領分）。
4. **SKILL.md現行化**: モードB既定・ai-jobs休眠を明記、旧レター参照を除去、新3機能を追加。
5. **非スコープ**: 卒業自動実行・バケットmv・レーン実況・WAKE判断・計画内容の判断（重複排除の分界をSKILL.mdに残す）。

## 完了条件（レビュー項目）

- [x] progctl: fixtureへの`set`2回連続で差分ゼロ（冪等）・マップ外セクションがバイト不変のテストが全PASS
- [x] scaffold: 生成物がareas/AGENTS.md §3必須構成を満たし、テンプレ本文の正本が`skills/plan-ops/templates/`の1箇所のみ（§3は本文複製ゼロのポインタ・grep確認）
- [x] program-lint: 違反合成fixtureで非0 exit+違反行指摘、現行統合programの実ファイルで0 exit
- [x] SKILL.md: 「program A」等の旧レター参照がgrep 0件、モードB/ai-jobs休眠が運用契約§1と矛盾しない
- [x] 実運用スモーク: 実programへの子状態変更1件をprogctl経由（dry-run→承認→commit）で実施できる

## 着手時期

子06マージ後 かつ 子04a＋ダッシュボードポインタ化完了後、子09より前（子09はscaffoldを使う側のため07が先行）。実行はレーン既定2ペイン。
