親計画: ../program.md ／ 分類: skill ／ 種別: 新規作成

# 06 vocab完全一致崩しの子

## 目的

合成データ。親program.md側の状態「実装破綻」が語彙集合に完全一致しない違反fixture
（前方一致だと「実装」に誤ってマッチしてしまう抜け穴の回帰テスト用）。backlink・完了条件は
正しく整えて、この子からはvocab違反以外が出ないようにする。

## 現状

合成データ。

## 方針

合成データ。

## 完了条件

- [x] 全部やった（状態が「完了」ではないため未チェックでも違反にならないが、揃えておく）
