対象計画: plan.md ／ ラウンド: 01
diff範囲: e82deab..4298d9e ／ 規模: ライト ／ 評価者: impl-reviewer(sonnet)

# 評価01: 計画入口一本化

## 項目別採点

- [PASS] skills/plan-create-review/ が存在し、SKILL.md の name と見出しが plan-create-review で、説明に「作る・合流・評価・終了（archiveまで）」の受け口が含まれる
  根拠: `skills/plan-create-review/SKILL.md` をRead。2行目`name: plan-create-review`、6行目見出し`# plan-create-review`、3行目descriptionに「計画を作る・既存計画へ合流する・programの子計画を管理する・評価（レビュー）と遷移を進める・終了を伝えてdone/archiveへ閉じる」が明記され4受け口すべて含む。

- [PASS] skills/plan-management/ が存在せず、git上 rename として追跡されている
  根拠: `ls -la .../skills/plan-management` は`No such file or directory`。`git show --summary -M 4298d9e`でSKILL.html(90%)・SKILL.md(64%)・workflows 3本(100%)の計5件が`rename ... plan-management/... => plan-create-review/...`として検出。`git log --follow --oneline -- .../plan-create-review/SKILL.md`は改名前コミット23e9791まで履歴が連続。

- [FAIL] 過去記録（plans/ 配下・logs/created/）を除き、personal-os 内の plan-management 参照が0件（plan-registry/AGENTS.md・catalog/meta.md・common.py・tests・SKILL.html・workflows を含む）
  根拠: `grep -rn "plan-management\|plan_management" personal-os --include=*.md --include=*.py --include=*.sh --include=*.json --include=*.html`で、`plan-registry/AGENTS.md`本体・`catalog/meta.md`・`common.py`・`test_common.py`は0件（正しく追従済み）だが、対になる`plan-registry/AGENTS.html`（33/39/80行目）に3件残存。80行目は「runtime露出は保留」という今回のrunnerで解消されたはずの記述がそのまま残り、AGENTS.mdとAGENTS.htmlが不整合。加えて`skills/plan-create-review/SKILL.html`30行目にも「plan-managementから改名」という残存文言が1件。両ファイルとも`plans/配下`・`logs/created/`のいずれの除外にも該当しない（`git diff e82deab 4298d9e --stat -- ".../plan-registry/AGENTS.html"`は無出力＝このコミットで未更新と確認）。

- [PASS] logs/migrated/2026-07/07-16-plan-create-review.md が規約（日付時刻 HH:mm JST・旧正本・新正本・理由・露出結果）を満たす
  根拠: Readで確認。3行目`日付時刻: 2026-07-16 19:53 JST`、4行目旧正本、5行目新正本、7行目移行理由、9行目検証、10行目runtime露出結果のすべてが記載済み。

- [PASS] link-global-skill.sh により5 runtime全てで symlink が正本 skills/plan-create-review を指す（verify通過）
  根拠: `ls -la`で5件全て`-> .../skills/plan-create-review`。旧名symlinkは5箇所とも`No such file`。さらに`link-global-skill.sh plan-create-review`を再実行し5件とも`Already linked`（verify_link内蔵チェック込み・exit 0）で機械確認。

- [PASS] session-board テストが全緑（common.py 案内文の改名追従を含む）
  根拠: `tests/test_*.py`を全7本実行し`builders 14・common 13・events 43・events_sql 26・output_layers 7・reconcile 16・spool 16`（計135）で全てFAIL=0。`common.py`は`plan_management_guide_candidate()`→`plan_create_review_guide_candidate()`に改名され、`test_common.py`の(i)(i1)(i2)が新関数名・新文字列`plan-create-review`で検証し通過。

- [PASS] 入口Skillの責務境界の記述（規模基準・route・script・合否・runtime露出を所有しない）が改名後も維持されている
  根拠: `plan-registry/AGENTS.md`責務地図の行で明記（改名前行と文言一致、スキル名だけ差し替え）。`SKILL.md`の「委譲と境界」「絶対ルール」節は`git show e82deab:.../plan-management/SKILL.md`と比較し該当4項目が文言レベルで同一。

## 総合判定
FAILあり（修正01.mdへ）

## 修正指示ドラフト

- `plan-registry/AGENTS.html`（33・39・80行目）: `plan-management`のまま、80行目は「runtime露出は保留」という解消済みの古い記述が残り、対の`AGENTS.md`と不整合。→ 現行内容（`plan-create-review`・露出済み）に合わせて更新。
- `skills/plan-create-review/SKILL.html`（30行目）: 「（plan-managementから改名）」の残存文言がgrep完了条件と食い違う。→ 注記を削除するか完了条件を明確化。
- 修正を1作業単位で追加コミットし、同じgrepを再実行して0件を確認する。

## 評価メタ所見（指揮官追記）

実装者（指揮官）の自己検証grepは、直前コマンドの `cd` でcwdがsession-boardに移ったまま相対path `personal-os` を走査しており空振りしていた（存在しないディレクトリへのgrep→0件と誤読）。独立レビュアーが正しいcwdで再実測して検出。検証コマンドは絶対pathで打つこと。
