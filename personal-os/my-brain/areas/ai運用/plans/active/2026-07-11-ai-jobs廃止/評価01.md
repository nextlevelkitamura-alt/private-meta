対象計画: personal-os/my-brain/areas/ai運用/plans/active/2026-07-11-ai-jobs廃止/plan.md ／ ラウンド: 01
diff範囲: staged全部（ai-jobs本体・ai-jobs-dispatcher・references/worker-prompt.md・plan-ops/scripts/jobctl.sh・new-run-card.sh の削除＋計画フォルダrename）＋ unstagedのうち本計画分12ファイル ／ 規模: フル ／ 評価者: impl-reviewer（Claude, ラウンド1）／ 実装: Codex thread 019f4f0e-1231-7c81-a56e-7e40eac8a673

# 評価01: ai-jobs 完全廃止（モードA実行レーンの撤去）

## 項目別採点

- [PASS] 現役の正本ルール8ファイルで、ai-jobsへの生きた導線が0件（廃止注記・README§4経緯記述は許容。loop-types.mdは対象外）
  根拠: `rg -n -i "ai-jobs"` を運用契約.md／README.md／areas/AGENTS.md／loops-registry/AGENTS.md／基盤AGENTS.md／plan-ops SKILL.md／基盤.gitignore／実行一覧/personal-os.mdの8ファイルに実行。ヒットは運用契約.md:11、README.md:20,26,28、areas/AGENTS.md:141の4箇所のみで、全て「旧ai-jobs＝モードAは2026-07-11廃止・決定ログ#14」形式のtombstoneかREADME§4「以前の前提は…」の経緯記述。パス参照・使用手順・投入導線は0件。`run-card|jobctl|new-run-card` も同8ファイルで0件（`rg`終了コード1）。

- [PASS] `loops-registry/ai-jobs/`・`loops-registry/loops/ai-jobs-dispatcher/`・`references/worker-prompt.md`・`plan-ops/scripts/jobctl.sh`・`plan-ops/scripts/new-run-card.sh` が存在しない
  根拠: 5パスを個別に`[ -e "$p" ]`で実存在確認、全て`absent`。`git diff --cached --name-status -M`でも全てstaged `D`（削除）を確認済み。

- [PASS] 運用契約§1が1モード（テキスト状態）で、「モードA」「モードB」の対比が契約から消えている
  根拠: 運用契約.mdを全文Read。§1見出しは「状態の持ち方（テキスト状態・単一指揮官）」に置換済み、本文はモードB/A記述を撤去し「無人・並行実行を将来必要とする場合は再設計する（旧ai-jobs＝モードAは2026-07-11廃止・決定ログ#14）」に一本化。`rg -i "モードa|モードb"`で契約全文をスキャンし、ヒットは11行目の廃止注記1件のみ、「モードB」は0件。

- [PASS] 決定ログに廃止エントリが1件あり、doneカード exec-audit-20260702.md の要点が転記されている
  根拠: 決定ログ.mdに`## 2026-07-11 #14 ai-jobs（モードA＝run-cardフォルダキュー）の完全廃止（ユーザー裁定）`エントリを確認。要点「launchdドリフト8件を読み取り監査で再確認し、実解消は人間ゲート待ち」を、削除前原本のCodexセッションログ実出力（rollout-2026-07-11T11-42-30-…jsonl line22）と突き合わせ、数字・結論とも正確に一致（壊れplist1件＋orphan plist5件＋二重稼働候補2件＝8件・読み取り専用・人間ゲート）。番号は当初#13採番→末尾衝突を自己検知し#14へ全箇所訂正済み（同ログline83）。6ファイルの追従記述も現物と一致。

- [PASS] `AIJOBS_RUN` が board-sweep と session-board hook に残っている
  根拠: `rg -n "AIJOBS_RUN"` で sweep.py/sweep.sh/llm-judge.sh/loop.md/plist/test_sweep.py、common.py/session-start.md/README.md/test-shims.sh の計17箇所にヒット。機能・名前とも改変なし。

- [PASS] board-sweep・session-boardの各テストが緑（exec-audit・rendererは並行セッション削除により対象外）
  根拠: board-sweep test_sweep.py → PASS=31 FAIL=0。session-board test-session-board.sh → PASS=80 FAIL=0、test-shims.sh → PASS=55 FAIL=0、python系6本（builders/common/events/events_sql/reconcile/spool）全緑（PASS計121 FAIL=0）。

- [PASS] plan-opsの残機能が動く
  根拠: program-lint.sh をマルチ指揮官体制program.mdへ実行→クラッシュせず既存違反9件を正しく列挙しexit1。2026-07-11-画像スキル簡素化/program.mdへ実行→「違反なし」exit0。jobctl/new-run-card削除後もクラッシュなし。

- [PASS] `git branch -a | grep -i ai-jobs` が0件
  根拠: 実行し出力なし（grep終了コード1）。

- [PASS] launchdにai-jobs系がない
  根拠: `launchctl list | grep -i ai-jobs` 0件。`find ~/Library/LaunchAgents -iname "*ai-jobs*"`（_retired退避フォルダ含む）0件。`launchctl print gui/<uid>/com.kitamura.ai-jobs-dispatcher`は「Could not find service」。

- [PASS] secret混入なし・`git add -A`不使用
  根拠: staged は本計画スコープの20ファイルのみ（並行セッションの大量変更はunstagedのまま＝add -A不使用の裏付け）。staged/unstaged diff双方をsecretパターンでscanし実値の混入なし。

## 総合判定

全10項目PASS。完了可（doneゲート条件を満たす）。修正指示ドラフト無し。

## 追加指摘（項目外・FAILではない）

1. README.md §3見出しが「状態の持ち方：2モード」のまま（本文は単一モード化済み・見出しと本文の矛盾）→ 指揮官が統合修正（2026-07-11・本評価直後）。
2. 運用契約.md 23行目付近が参照する `loops-registry/references/loop-types.md` は並行セッションの大掃除で消滅済み＝dangling reference。本計画スコープ外（並行作業側の追従漏れ）として別途拾う。
3. マルチ指揮官体制program.md 45行目「契約§1モードBの強化」表現は2026-07-03裁定の歴史的記録につき残置（計画どおり指示範囲外）。
