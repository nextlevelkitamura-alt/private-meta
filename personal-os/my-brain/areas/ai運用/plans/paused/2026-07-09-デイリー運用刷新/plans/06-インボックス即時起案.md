親計画: ../program.md ／ 分類: skill ／ 種別: 既存改善
規模: ライト
並列: 可（検証のみ） ／ レビュー: 一括（Checkpoint A / B / Final Gate）

# inbox-triage互換確認

## 目的

すでに実装済みの「人間が指定した依頼インボックス1行をplanningへ起案する」経路が、8節Dailyと新しいDaily運用でも壊れないことを確認する。廃止済みの自動巡回・Notion pull・launchdを復活させない。

## 現状

- `skills/inbox-triage/` と `skills/plan-ops/scripts/new-plan.sh` による手動指定経路は存在する。
- 自動巡回loop、行claim、Notion自動pull、headless起案は2026-07-11方針で廃止済み・範囲外。
- 対象repoの `plans/planning/` へドラフトを置き、active化・commit・pushはしない契約。
- 旧programの「tick plistをロード」は誤り。Global Skill内にpausedへ移ったprogramの旧 `plans/active/...` 参照が残る可能性がある。

## 検証パケット

### 担当

- 新規Terra workerは立てない。Plan 09の統合・テスト担当がCheckpoint A / B / Final Gateで読む。

### 最初に読むもの

1. この `plans/06-インボックス即時起案.md`
2. `personal-os/AIエージェント基盤/global-skill-registry/AGENTS.md`
3. `personal-os/AIエージェント基盤/skills/inbox-triage/SKILL.md`
4. `personal-os/AIエージェント基盤/skills/plan-triage/SKILL.md`
5. `personal-os/AIエージェント基盤/skills/plan-ops/SKILL.md`

### 触る場所

- 原則なし。回帰FAIL時だけ `skills/inbox-triage/` の最小修正を所有planへ切り出す。
- 旧計画backlink、catalog、logsはPlan 09だけが更新する。

### 触らない場所

- 自動巡回、Notion PATCH/pull、headless起動、launchd。
- 指定されていないインボックス行、plans/active、git commit/push。
- program、他plan、実Dailyの他の節。

### 確認すること

- 8節Dailyの `## 依頼インボックス` から、人間指定の1行だけを読む。
- 重複planがあれば新規フォルダを増やさない。
- 対象repoのplanningへ原文・出所・日付を持つドラフトを作る。
- active・commit・push・他行を変更しない。
- 削除済みhelper/loop/Notion自動pull/launchdを参照しない。

## 方針

- 手動指定の1行起案だけを現行正本とし、自動巡回・Notion・headless・launchdを復活させない。
- 回帰FAIL時だけ最小の所有planへ切り出し、planning以外の状態を変更しない。
- CheckpointとFinal Gateで8節Dailyとの互換をまとめて検証し、Plan 06単独の正式レビューは行わない。

## 完了条件（レビュー項目）

- [ ] 8節Daily fixtureで指定1行だけが処理され、他行・他節が不変
- [ ] 対象repoのplanningへ正しいドラフトが作られ、重複時は増えない
- [ ] active・commit・push・自動巡回・Notion・launchdが実行されない
- [ ] 削除済みhelper/loopと旧 `plans/active/...デイリー運用刷新` 参照が現役導線にない
- [ ] Checkpoint A / B / Final Gateの互換testがPASSする

## 完了報告

Plan 09が `対象fixture / planning結果 / 非変更範囲 / stale参照 / 判定` をCheckpoint結果へまとめる。
