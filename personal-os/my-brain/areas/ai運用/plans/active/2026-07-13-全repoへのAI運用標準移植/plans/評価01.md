# 評価01 — 中央正本とrepo標準契約

判定: PASS

## 確認結果

- 全repo移植の計画正本は中央program一式だけで、仕事repoへ複製していない。
- `repo-registry → 対象repo AGENTS.md → 既存plan検索 → repo固有計画箱` の二段ルーティングを中央契約とsession-boardへ反映した。
- Private→対象repoは新しい可視sessionへ引き継ぎ、session IDの移管・reparentを行わない契約になった。
- session-boardはsession状態とDaily実行ログだけを所有し、plan本文・plan状態を所有しない。
- root `plans/` 固定、存在しない節、廃止rendererの肯定的前提を解消した。
- 既存の白背景、WIP、Daily 2区画、ユーザー差分を維持した。

## 証拠

- `test_common.py`: 10/10 PASS
- `test-session-board.sh`: 80/80 PASS
- `test-shims.sh`: 59/59 PASS
- Python個別suite: 全PASS
- `program-lint.sh`: 違反なし
- `git diff --check`: PASS
- 独立reviewer: PASS、修正必須点なし

## 後続へ分けた責務

- `plan-triage` のSkill実装・HTML・runtime整合は子04で行う。
- `repo-create` のcriteria・監査・scaffoldは子08で行う。
- 同じ手順を子01へ複製しない。
