分類: loop ／ 種別: 既存改善（loops/daily-digest拡張） ／ 優先: ○ ／ 規模: ライト（1レーン）
次: 完了（2026-07-03 レビュー3巡目PASS・mainマージa7a8a1d・launchd3本化切替済み・実運用snapshot成功=repo別要約+全体1行+レーン段階所要時間統計が当日デイリーに生成・Notion当日ページへも反映）。運用フェーズ。※切替直後の実走でrun.shの空配列展開バグ(bash3.2・launchd経路全滅級)を発見→指揮官がサクッと修正b699398（テスト13/13）

# デイリーダイジェスト（1日の記録をrepo/フォルダ単位で自動要約）

## 目的

1日分の膨大なログ（コミット・レーン稼働・セッション）を、「このrepo/フォルダでは今日こういうことを何回やった」が数行で分かる形に自動要約し、当日デイリー末尾に「今日のダイジェスト」区画として残す（2026-07-02ユーザー要望）。Notionへは追加工事ゼロで載る（N1が当日ページ全文をpushするため）。

## データ源（すべて既存・新しい記録は作らない）

- 各repoの `git log --since=今日` （コミット件数と要旨。ユーザー言: コミットコメントを見れば大体分かる）
- cockpit 段階イベントJSONL（レーン何本・どの段階まで行ったか・他チャットのレーンも含む）
- 当日デイリーの done 区画・実況区画
- 対象repoは固定リスト（AIエージェント基盤・Private(my-brain)・仕事・focusmap）＋当日イベントに現れたworktree

## 方針（2026-07-03裁定: 案2採用）

1. 案1・夜締め型1回: 不採用（中間経過が見えない。ユーザーは3回を選択）。
2. **案2・3回スナップ（採用）**: 12:30／18:30／23:30 に digest を実行。スクリプトは冪等（`auto:digest` 区画の全置換）なので回数はlaunchd設定の問題。
3. 案3・コミット毎hook型: 不採用（断片化・リアルタイム性は実況＋Notionで充足）。
- **実装方式（既存転用）**: `loops/daily-digest`（=23:30締めレンダの薄いラッパ・稼働中）を拡張する。digest.sh は生成ロジック正本の `renderer/scripts/` に新設し、daily-digest の run.sh に1ステップ追加＋昼夕用 `--snapshot` モード追加＋plistを3回化。renderer/render.sh 本体と renderer/loop.md は触らない（並行レーンとの衝突回避）。マーカーI/Oは daily-digest/scripts/ の既存正本（set-marker-block.sh等）を再利用。
- **要約モデル**: claude -p --model claude-sonnet-5（軽量運用・ユーザー裁定）。呼び出しは env `DIGEST_LLM_CMD` で差替可能にし、テストはstub。
- **repo概要の読み合わせ**: `repo-registry/repo概要.md` を新設（各repo 1〜2行・詳細は各repoのAGENTS.mdが正本というポインタ明記・二重管理しない）。digestはこれを文脈として渡し「このrepoは何をしている場所か」を踏まえた要約にする。
- **レーン所要時間の統計（2026-07-03ユーザー要望）**: cockpit段階イベントJSONL（`skills/orca-cockpit/state/events.jsonl`・実行時は基盤main checkoutの絶対パス・テストはfixture）の当日行から、レーンごとの段階遷移と所要時間（ts差分）を集計しダイジェストに含める。「この規模×このモデル＝この時間」の統計蓄積の種にする。
- 出力様式（例）: 「基盤: コミット12件（watch.sh v3・plan-ops新3機能・Notion盤面）／レーン3本（2完了・1実装中）」のようにrepoごと1〜2行＋全体1行＋レーン所要時間の短い箇条書き。
- secret禁止・Notion側の追加工事なし（当日ページ全文pushに乗る）・launchd変更は人間ゲート（3回スケジュールは2026-07-03ユーザー裁定で承認済み・再登録の実施は指揮官）。

## 完了条件（レビュー項目）

- [x] 方式決定（2026-07-03人間裁定: 案2=3回スナップ・sonnet5・repo概要読み合わせ・所要時間統計）
- [x] digest.sh: 固定リスト＋当日worktreeを走査し、`auto:digest` 区画へ冪等書き込み（テスト13本・差し戻し2回で当日worktree集計と検証力を補強）
- [x] plistが12:30/18:30/23:30の3本になり再登録済み（2026-07-03 bootout→symlink→bootstrap・launchctlで3本確認）
- [x] repo-registry/repo概要.md がある（各repo1〜2行・ポインタ明記）
- [x] ダイジェストにrepoごとの要約とレーン段階所要時間が含まれる（実物: 2026-07-03デイリー「今日のダイジェスト」）
- [x] 実運用1件: 2026-07-03 snapshot実行で当日デイリーに載り、Notion当日ページにも反映（push成功確認）
