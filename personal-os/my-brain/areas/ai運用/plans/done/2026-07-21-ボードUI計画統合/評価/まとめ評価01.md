対象計画: plans/01-ボード純UI.md（修正02後）＋plans/05-計画直結ボード.md＋plans/06-理想形モック駆動の仕上げ.md ／ ラウンド: まとめ評価01（人間指示「まとめられるレビューはまとめる」初適用）
diff範囲: focusmap main 2dcd7420・2ab2d0f4・e86bdaf4・21825e61（5f520df7..21825e61＝14ファイル・+1331/-398）／ 規模: フル ／ 評価者: 品質レビュー（実物コード照合＋tsc --noEmit実走＋git差分・read-only。ブラウザ実機検証はしない。reviewer subagentをライブ派遣し段階3表示の実走テストを兼ねた）

# まとめ評価01: 修正02＋子05＋子06（3段階ドリルダウンv6）

## 項目別採点

### 子01 ボード純UI（修正02で改訂された条件）
- [PASS] 独立3区画が消え、きみの番レーン廃止で残存コードなし
  根拠: ask-lane.tsx はdiffで削除・src全体で AskLane/AskItem/asks のimport残存0（「きみの番」文字列は説明コメント3件のみ）。board/page.tsx は planCards＋StrayBox のみ描画。
- [PASS] 通常状態＝1行サマリのデフォルト折りたたみ、計画本文をボードに出さない
  根拠: theme-card.tsx PlanCardV2 useState(false) 初期折りたたみ・open時のみ工程/セッション展開。計画本文は非描画で PlanChip→/dashboard/plans/[slug] の2段導線。stray-box.tsx も useState(false)。
- [PASS] steps未登録todoが沈黙しない
  根拠: 工程0件で「工程未登録（計画待ち）」（plan-steps.tsx:352）／「ステップ未登録（計画待ち）」（theme-card.tsx:285）を明示。※原文の「バッジ」ではなくテキスト表現（沈黙させない意図は充足）。
- [保留・実機] 375pxでテーマカード内「1✔斜線→2実行中(明滅)」がライブで見え、全カードが1画面に収まる
  根拠: コードは done=薄緑バッジ+取り消し線・doing=緑バッジ+「実装中」ピル点滅で表現。1画面収まりは実機375px目視が必要＝人間ゲート。
- [対象外] 質問回答・2層チェック機能維持／後続部品のprops契約明確／PC幅・PCサイドバー同一部品
  根拠: 評価02で全PASS済み。今回diffでも配線維持。PCサイドバーは board-summary-panel.tsx が PlanCardV2/StrayBox を同一部品で描画（個別実装なし）。

### 子05 計画直結ボード
- [PASS] plan_slug付きtodoの見出し行が消え工程タイムラインがカード直下に直接出る（同文二重なし）
  根拠: PlanCardV2 は !isThemeOnly && assignee==='ai' の todo を PlanTaskSteps（見出し行なし・番号工程直下）で描画。TaskRow の planSlug 分岐もタイトル・チェック丸を出さない。
- [PASS/保留] plans/active の全計画がカード化・動きなしは1行カードで沈黙しない
  根拠(コード): getActivePlans() が plan_docs kind IN(program,single) AND bucket='active' を返し build.ts が計画ごとにカード生成。hasActivity=false は静かな1行カード。保留: 「フォルダ実体6本と一致」はplan_docsミラー実データ確認が必要。
- [PASS] 単発todo（plan_slugなし）は従来どおりタスク行
  根拠: plan_slugもthemeも無いtodoは strayTasks→StrayBox、theme所属のみは home card→TaskRow。build.ts:194-204。
- [PASS/保留] 質問回答・2層チェック・セッション行・サブ表示の既存機能維持
  根拠(コード): PlanTaskSteps に TaskQuestion+QuestionAnswer・completeHeadingAction(allDone時)・FixReattach・carryOverAction・AiLane を全配線。保留: 実操作1往復は実機。
- [PASS/保留] tsc --noEmit エラー0・/api/board/summary 200
  根拠: tsc --noEmit を実走。既存エラー49件はすべて対象外ファイル、変更14ファイルの新規型エラーは0。API 200は認証付き実走が必要＝保留（route.tsは soft() フェイルソフトで500化しない設計）。

### 子06 理想形モック駆動の仕上げ（v6確定仕様）
- [PASS/保留] 段階1=計画名1行→段階2=指揮官バー+番号工程(1行)→段階3=工程タップでAIレーン
  根拠(コード): PlanCardV2 open で段階2（CommanderBar＋PlanTaskSteps番号工程・工程行は経過非表示の1行）、StepRow expandable(=aiStepIdx) の open で段階3 AiLane 展開。3段の深化は実機目視で最終確認。
- [PASS] 実装中ピルだけ点滅・番号バッジは色のみ・経過はAIレーンでリアルタイム
  根拠: 番号バッジ BADGE_CLASS はアニメ無し。点滅は doing ピルの内側ドットのみ animate-pulse motion-reduce:animate-none。経過は段階3 SubChip の LiveElapsed が setInterval 1秒更新。工程行に経過なし＝v6準拠。
- [PASS/保留] 確認待ちは指揮官/独立メインのみ、サブには立たない
  根拠(コード): CommanderBar/MainSessionMini は state==='wait' で「確認待ち」表示。SubChip は確認待ち非表示・ドットは running緑/静的のみ。工程ピルの琥珀は doing工程に未回答質問がある時だけ。保留: wait状態の実データ確認。
- [PASS/保留] 工程の📄からその工程の文書が表示専用で開く
  根拠(コード): StepDocButton が /api/board/plan-doc で plan_docs.body を遅延取得し MarkdownDoc を Sheet 表示。getPlanStepDoc は review→eval md(NN一致)・impl/fix→child md(NN一致)・不能時 program/single へフォールバック。保留: 文書対応の実データ確認。
- [PASS/保留] active計画カードがフォルダ実体active6本と一致
  根拠: getActivePlans の bucket='active' 抽出＝コードは正。実体一致はplan_docsミラー実データ確認（子06前段でplansync整合済み）＝保留。
- [PASS] tsc --noEmit エラー0（コードPASS・API実走保留）
- [PASS] 子03表示側（サブ詳細5列）を子06が吸収
  根拠: SessionSubagent 型に runtime/model/agent_type/launch_via/prompt の5列読み、SubChip が5列すべてを表示。board側migrationはsession-board所掌で適用済み・focusmap側migration追加なし。

## 総合判定

コードレベルで全完了条件がPASS（または実機/実データの人間ゲートで保留）。FAIL 0・憲法4条の違反0。修正ラウンドは不要。done移動には下記「保留」の実機確認クリアが条件。

憲法（正本境界）確認:
- 憲法1 DB→md書き戻し無し: src全体で plan_docs/session_subagents への INSERT/UPDATE/DELETE 0件（SELECTのみ）。plan-doc APIは read-only。PASS。
- 新テーブル/migration追加なし: 5f520df7..21825e61 の差分にmigrationファイル0。PASS。
- 主観値/導出値をDB保存しない: 進捗%・稼働N・経過・指揮官識別いずれも非保存。PASS。
- 計画リンクはslug単位: planSlugBase で slug/slug#NN を正規化、plan_docs.program_slug と突合（path非依存）。PASS。

セキュリティ: /api/board/{summary,plan-doc} とも supabase.auth.getUser() で401ガード。SQLは全て名前付きパラメータでinjection無し。サブprompt全文表示は board.py 側マスキング前提（本diff外の依存・指摘2参照）。

## 保留（実機の人間ゲート・reviewerは越えられない）

1. 375px実機での3段階ドリルダウン挙動・全カード1画面収まり・折りたたみ目視
2. 実装中ピル点滅／番号色のみ／AIレーン毎秒経過の実機目視
3. 確認待ちが指揮官/独立メインに立ちサブに立たない、のwait実データ確認
4. 工程📄が正しい子計画/評価mdを開く、のplan_docs実データ確認
5. active計画カード＝フォルダ実体6本一致（plan_docsミラー実データ確認）
6. /api/board/summary の認証付き200実走
7. 質問回答・2層チェック・セッション/サブ表示の実操作1往復
8. push→本番デプロイ反映（済/人間の最終目視）

## 重要な指摘（非ブロッキング・任意改善）

1. SessionRow のライブドットが run/sub で animate-pulse（session-row.tsx:35）。v6ドリルダウン経路外（テーマのみカード・単発todo・カード直下 cardSessions）だが、「点滅は実装中ピルだけ」を徹底するならドット点滅を外す検討を。
2. SubChip の prompt 全文表示は board.py 側マスキング前提。マスキング実効性は本diff外＝実機で機密断片が出ないか1件目視。
3. StepDocButton は h-7（約28px）でモバイル最低44pxを下回る。誤タップ確認を。

## 指揮官の追加所見（このレビュー実走で判明）

- **capture hookのライブ実証**: 本レビューを reviewer subagent としてライブ派遣し、session_subagents に status=running・agent_type=reviewer・launch_via=agent-tool・prompt全文が即時記録されることを実データで確認（＝段階3 AIレーンが実データで動く）。
- **model列の未捕捉**: runtime=claude は取れるが model（opus等）が NULL。capture-subagent-detail.py が model を拾っていない小穴。表示は runtime フォールバックで機能するが、hook側で1行補う改善余地（別途起票候補）。
