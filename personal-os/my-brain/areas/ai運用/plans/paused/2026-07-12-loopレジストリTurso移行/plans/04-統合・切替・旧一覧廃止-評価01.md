対象計画: plans/04-統合・切替・旧一覧廃止.md ／ ラウンド: 01
diff範囲: Focusmap `codex/loop-registry-integration`（`ac3bfcea..d2062929`）／ 規模: フル ／ 評価者: 独立監査

# 評価01: 統合・切替・旧一覧廃止

## 項目別採点

- [FAIL] 01〜03の最終評価が全PASSで、shared type/schema/API responseの重複定義がない。
  根拠: このprogram配下には01〜03の評価MDが存在せず、統合前提を証明できない。
- [FAIL] revision=1の7loopが旧一覧・plist・launchctlと全項目一致し、55stepの欠落・順序差がない。
  根拠: Turso migration・初期importは未実施であり、実DBのrevision=1は作成していない。
- [FAIL] shadow期間に無変更writeが抑制され、offline/spool/replay/重複eventが期待どおり動く。
  根拠: Mac observerの実機shadow稼働は未実施。専用テストは通過したが、実機の代替にはならない。
- [FAIL] Focusmap read切替後もfeature flagで旧表示へ戻せる。
  根拠: `FOCUSMAP_LOOP_REGISTRY_READ_ENABLED` 等のread切替flagと、Cloud Runで到達できる旧一覧fallbackは未実装。旧MD/HTMLはPersonal OS側に残っているだけである。
- [FAIL] MCPがcontract→list/get/runs/drift/update/request applyの順で動き、approveを持たない。
  根拠: MCP実装は統合branchにあるが、接続した実API/Tursoを使うend-to-end検証は未実施。
- [FAIL] canary apply、失敗時rollback、再適用no-opを実証し、対象外6loopへ影響がない。
  根拠: applyは人間承認待ちであり、canaryを開始していない。
- [FAIL] 全7loopがsynced、Mac online、desired==applied==observed revision/hashになっている。
  根拠: import・shadow・applyをまだ行っていない。
- [FAIL] MCP/Focusmapだけで旧一覧と同じ定義・内部処理・実機状態・履歴へ到達できる。
  根拠: DBへの初期import、実機観測、read切替が未実施である。
- [FAIL] `daily-notion-sync` は停止・attentionの実機状態を正しく表示し、独立レビューと人間確認まではNotion実書き込み・launchd復帰・canary適用の対象外である。
  根拠: 計画には停止・対象外の安全条件を追記済みだが、DB/UIでの表示は未検証。launchd復帰・Notion書込み・canaryは行っていない。
- [FAIL] 旧一覧削除は人間の明示承認後で、AGENTSから削除済みファイルへの参照が残らない。
  根拠: 旧一覧は安全なfallbackとして保持中で、削除承認もない。
- [PASS] Focusmap local main、origin/main、本番、Private main、仕事repoの反映状態を分けて報告できる。
  根拠: Focusmap local main=`e3980f72`（origin/mainより1 commit先行）、統合branch=`d2062929`（main未統合）、origin/main/本番は未反映、Privateと仕事repoは別管理として識別済み。
- [PASS] secret scanと対象範囲のdiff確認でtoken・credential・個人情報・生ログ混入がない。
  根拠: `npm run security:secrets` は通過、統合branchで `git diff --check` は通過した。

## 補足検証

- loop専用テスト: 4ファイル・24テスト PASS。
- lint: PASS。
- production build: PASS（middleware conventionの非推奨warningのみ）。
- 全体Vitest: FAIL（16件）。local mainでも同一16件を再現したため、統合差分の回帰とは判定しない。
- Browser: ログイン済みセッションがないため、設定画面の実画面確認は未完了。
- Turso migrationは、`--confirm-human-gate=loop-registry-turso-migration` なしでは接続前に終了するよう補強済み。migration自体は未実行。

## 総合判定

FAILあり。統合branchの静的実装・専用検証は前進しているが、04の完了には至っていない。次の修正は、まず01〜03の評価根拠とread切替/fallbackの設計を揃え、その後に人間ゲートを通してTurso migration・初期import・shadowへ進める。

## 修正指示ドラフト

1. Focusmapが切替前後どちらの一覧を表示するのか、Cloud Runでも到達できる旧一覧fallbackの正本とfeature flagを明文化・実装する。
2. 01〜03の最終評価を先に作成し、shared contractの重複がないことを確認する。
3. 人間承認後だけ、migration → import → shadow → read切替 → canary の順に実機検証する。`daily-notion-sync` は独立レビュー・人間確認まで停止/attentionかつ対象外を維持する。
