分類: 調査ドラフト（phase1・読み取り専用） ／ 担当: 調査担当（本worktree） ／ 監督: 全体管理者A
正本計画: `~/Private/personal-os/my-brain/areas/ai運用/plans/active/2026-07-03-orca-cockpit評価レーンの型と簡素化/plan.md`

# orca-cockpit 再編ドラフト

## 0. 重要な裏取り事実（読む前に）

調査中に、**子09（spawn高速化）が既にmain実体（`~/Private/personal-os/AIエージェント基盤`）へ未コミットで実装済み**であることを確認した（本worktreeのbranchは子09マージ前の古い状態）。

- 本worktree（`cockpit-skill-reorg` branch, base=ec2ead4）の `skills/orca-cockpit/SKILL.md` は **72行・9節**、レシピ節なし。
- 一方、mainの実working tree（未コミット）は `git status` で `M skills/orca-cockpit/SKILL.md` `M skills/orca-cockpit/scripts/cockpit.sh` `?? skills/orca-cockpit/scripts/spawn.test.sh` を確認。**現物は88行・10節**で、既に「6. レシピ（この時はこれを実行する）」節が追加済み（中身は本ドラフト§4で引用）。
- plan.mdの直列前提（「子09のmainマージ後に本再編を実施」）どおり、**本再編（phase2）は子09がコミット・マージされてから着手する**。行数目標（§1）は子09マージ後の実測値で再計算すること（下記は現時点88行を仮基準にした目安）。
- 副次確認: `cockpit-supervisor/SKILL.md` §3.4 のモデル既定（実装=claude sonnet5／レビュー=codex xhigh）と、orca-cockpit `SKILL.md` §2.1 のモデル既定（実装=codex gpt-5.5 medium／レビュー=codex gpt-5.5 medium）が**食い違っている**。個別の指示ではなく型の欠如が原因（plan.mdの構造原因と同根）。本再編で「役割型」節にモデル既定を再掲せず cockpit-supervisor を正とする形に寄せれば、この矛盾も同時に解消できる（§2参照）。

---

## 1. 現状マップ

### 1.1 ファイル一覧（行数・役割）

| ファイル | 行数 | 役割 |
|---|---|---|
| `SKILL.md`（本worktree版） | 72 | 入口。9節構成（下記1.2） |
| `SKILL.md`（main実working tree・未コミット） | 88 | 同上＋子09の「6. レシピ」節・spawn言及済み（10節） |
| `references/role-prompts.md` | 63 | 各ペイン（実装/レビュー/監督）投入プロンプト定型・状態マーカー標準・共通制約・指示スペック定型 |
| `scripts/cockpit.sh` | 421 | 決定的処理本体。`new/split/agent/send/title/status/down/perm/plan/up` の9サブコマンド（＋main未コミット版は`spawn`追加） |
| `scripts/watch.sh` | 237 | 見張り番本体。複数worktree監視・5種のWAKE検知（error/人間確認待ち/権限待ち/idle/timeout） |
| `scripts/worktree-sweep.sh` | 124 | 夜間worktree回収の検知（子07・検知のみ、実行は人間ゲート） |
| `scripts/events.test.sh` | 170 | `cockpit.sh` のイベント関連テスト |
| `scripts/watch.test.sh` | 150 | `watch.sh` のテスト |
| `scripts/worktree-sweep.test.sh` | 48 | `worktree-sweep.sh` のテスト |
| `scripts/__tests__/fixtures/*.json`（13ファイル） | 各9〜22 | `orca terminal ps` 出力を模した合成fixture（alldone/busy/done_marker/err/false_positive_*/malformed/marker/multi_second_err/review_marker/terms_clean/terms_dialog/waiting）。watch.shの各検知パターン用 |

`workflows/` ディレクトリは存在しない（新設ゼロを確認する上での前提）。

### 1.2 SKILL.md（本worktree版・72行）の節構成と評価

| 節 | 行目安 | 入口に必要か | 判定 |
|---|---|---|---|
| 1. 役割 | 7 | ○ 核 | 目的節へ統合 |
| 2. 既定ルール | 9 | △ 一部詳細 | item1(2ペイン既定)のみ役割型節へ、item2〜6（display-name規則・ペインタイトル挙動・4ペインtemplate配置・起動秒数目安・codex update自動skip挙動）は**入口に不要な詳細→references/agent-invocation.md（新設）へ** |
| 3. 手順 | 8 | ○ 核 | レシピ節へ統合 |
| 4. 読み込み方針 | 6 | △ | 1行に圧縮して目的節へ |
| 5. エージェント指定 | 6 | △ 詳細 | 役割種別の要約だけ役割型節に残し、`--model`/`--effort`の引数詳細は**references/agent-invocation.mdへ**（cockpit.sh helpと二重化しない） |
| 6. 安全方針 | 8 | ○ 核 | 安全節へ統合 |
| 7. 出力 | 4 | △ | 1行に圧縮してレシピ節末尾へ |
| 8. 関連 | 8 | △ | 3行に圧縮して目的節末尾へ |
| 9. 見張り番・権限配布（機構） | 6 | △ 詳細 | 判断根拠として安全節に1行ポインタのみ残し、**タイムアウト値・`.claude/settings.json`配布内容の詳細はreferences/watch-and-permissions.md（新設）へ** |

→ 「入口に不要な詳細」＝ §2 item2-6、§5の引数詳細、§9の数値・ファイル内容詳細。これらは判断に必要な短い要約だけ本文に残し、実行時にしか要らない細部をreferencesへ落とす。

main実物のみに存在する「6. レシピ」節（子09追加・14行）は§4で扱う。

---

## 2. 裏取り: 評価レーンの型の不在確認

- `SKILL.md`（本worktree版・main未コミット版とも）に「評価」「評価者」「第三者評価」の文言は**一切ない**（`grep -n "評価" skills/orca-cockpit/` → 0件）。
- `references/role-prompts.md` にも評価ペイン定型は存在しない（1〜5節は実装/レビュー/監督の3役のみ）。
- `cockpit-supervisor/SKILL.md`（判断層の正本）にも評価レーンの言及なし。モデル選定基準（§3.4）は実装/レビューの2役のみを扱う。
- 結論: 「評価レーン（評価者=codex既定）」の型はorca-cockpit/cockpit-supervisorのどこにも存在しない。plan.mdの構造原因（「型が無いため個別判断に落ちた」）と一致。

**入れるべき節（2026-07-03 16時ユーザー裁定反映）**: 「評価レーン」という別役割は作らない。役割型は「実装=claude／レビュー=codex」の2役のままとし、評価（第三者実測・判定）は**レビュー役の定義に統合**する。orca-cockpit `SKILL.md` の**役割型**節（新設予定・目標構成§3）のレビュー項目へ「評価＝第三者実測・判定を含む」を明記し、評価を他へ委ねる場合の宣言ルール（分担明示＋全体管理者宣言）を同じレビュー項目内に1行で書く。独立の役割項目・独立のレシピ項目は追加しない。

---

## 3. 目標構成（SKILL.md）

4節のみ: **目的／役割型／レシピ／安全**。

- 現行行数の基準は§0参照（子09マージ後の実測88行を仮基準）。
- **目標行数: 仮基準の30%減＝61行以下**（子09コミット後に実測し直し、実測値×0.7以下を正式目標とする）。
- 節ごとの目安（仮基準88行→合計約54〜60行に収める設計）:

### 3.1 ## 1. 目的（目安8〜9行）
- 現行§1（役割1-4）を2〜3行に圧縮。
- 現行§4（読み込み方針）を1行に圧縮：「まずSKILL.mdだけ読む。細部は`cockpit.sh help`／references」。
- 現行§8（関連）を2〜3行に圧縮：上位連携（coding-task-orchestrator）・判断層（cockpit-supervisor）・正本plan群へのリンクのみ。

### 3.2 ## 2. 役割型（目安9〜11行・2026-07-03 16時裁定反映＝2役のまま）
- **実装**: claude（既定）。位置=右上。
- **レビュー**: codex（既定。**評価＝第三者実測・判定を含む**）。位置=右下。モデル既定は本文に再掲せず**cockpit-supervisor §3.4を正とする**旨を1行明記し、二重管理・矛盾を解消。評価（実測・判定）をレビュー以外の担当へ委ねる場合は、実測担当と判定担当を明示したうえで全体管理者が宣言してから既定を外す（黙って変えない）。← 完了条件の核はここに1行で収める。独立の「評価」役割項目は作らない。
- 計画+監督（任意3ペイン目、要否判断は`cockpit-supervisor`）。
- opencode（将来対応枠、追加方法1行）。
- 現行§5「エージェント指定」の`--model`/`--effort`引数詳細と現行§2 item2-6（display-name規則・ペインタイトル挙動・4ペインtemplate・起動秒数目安・codex update挙動）はここから`references/agent-invocation.md`（新設）へ委譲。role-prompts.mdへの参照もここに1行集約。

### 3.3 ## 3. レシピ（目安13〜15行・2026-07-03 16時裁定反映）
- 現行§3「手順」（構成確認→構築→指示注入→監督→片付け）を4行に圧縮。
- **子09が追加した「この時はこれを実行する」4項目（中間指揮官を立てる/実装+レビュー並行/MCPが要る作業/緊急1ペイン）をそのまま移設**（言い換えのみ、内容追加・削除はしない）。
- 評価は独立の役割型を作らずレビュー役の定義（§3.2）に1行で統合したため、**レシピ節に評価専用の項目は追加しない**（評価タスクも既存の`up`/`spawn`呼び出しで足りる）。
- 現行§8「出力」（JSON形式）を1行でレシピ末尾に統合。

### 3.4 ## 4. 安全（目安8〜9行）
- 現行§6「安全方針」5項目はほぼそのまま。
- 現行§9「見張り番・権限配布（機構）」は判断根拠として要点1〜2行のみ残し（何が起きたら止まるか）、**タイムアウト値（2分/4分/25分/3時間）と`.claude/settings.json`の配布内容詳細は`references/watch-and-permissions.md`（新設）へ**移設し、1行ポインタで参照。

---

## 4. references/ 移動リスト

| 移動元 | 移動先 | 内容 | SKILL.md側の参照方法 |
|---|---|---|---|
| 現行§2 item2-6（display-name規則／ペインタイトル挙動／4ペインtemplate配置／起動秒数目安／codex update自動skip挙動）＋現行§5の`--model`/`--effort`引数詳細 | `references/agent-invocation.md`（新設） | エージェント起動・命名の実行時詳細 | 「役割型」節末尾に1行「起動引数の細部は`references/agent-invocation.md`」 |
| 現行§9「見張り番・権限配布（機構）」のタイムアウト値・`.claude/settings.json`配布内容の詳細 | `references/watch-and-permissions.md`（新設） | 見張り番の5種検知閾値・権限ファイルの中身方針 | 「安全」節末尾に1行「見張り番の閾値・権限配布の詳細は`references/watch-and-permissions.md`」 |
| 現行`references/role-prompts.md` | 変更なし（既存のまま） | 各ペイン投入プロンプト定型 | 参照元を現行§9.3（見張り番機構）から新設「役割型」節へ移動（内容は無編集） |
| モデル既定表（実装=claude sonnet5／レビュー=codex xhigh等） | 新設ファイルは作らない | — | `cockpit-supervisor/SKILL.md` §3.4を正とし、orca-cockpit側は「モデル既定はcockpit-supervisorを見よ」の1行のみ（重複記載の解消） |

新設は2ファイルのみ（`agent-invocation.md`, `watch-and-permissions.md`）。リンクは全て絶対パスではなく同skill内相対参照（`references/xxx.md`）で統一する。

---

## 5. workflows/ 新設ゼロの確認

- 現状 `workflows/` ディレクトリは存在しない（§1.1で確認済み）。
- 本再編でも新設しない。判断根拠（review-rules.md §6「別workflowにしない条件」）:
  1. レシピ節の分岐は4〜5パターンの浅いif-then（中間指揮官/実装+レビュー/MCP要否/緊急1ペイン/評価）で、手順が数行ずつしかない。
  2. 各パターンは単体でユーザー発話の目的にならず、`cockpit.sh`のサブコマンド1回呼び出しで完結する（別workflowにする条件「それ単体で成果物・判断・実装計画まで出せる」を満たさない）。
  3. 「必要そうに見える」箇所（評価レーンの構成カード提示・分担明示の宣言手順）は、既存Step（現行§3手順のStep0「構成確認」）へ短い1文を足すだけで足りる。独立workflow化すると、review-rules.md §6「親workflowの完了条件を別workflowへ逃がして見落としやすくする」の禁則に抵触する。

---

## 6. 子09（spawnサブコマンド＋レシピ節）との統合位置

- **前提**: 子09は現時点でmain実working treeに未コミット。plan.mdの直列前提により、子09のコミット・マージが完了してからphase2に着手する。
- **統合方法**: 子09が`SKILL.md`へ追加した「6. レシピ（この時はこれを実行する）」節（4項目＋補足）を、新設「## 3. レシピ」節へ**そのまま移設**する。文言の言い換え・節番号の付け替えのみ行い、項目の追加・削除・要約による情報欠落はしない（子09の完了条件「orca-cockpit SKILL.mdにレシピ節がある」を壊さないため）。
- **重複させない点（2026-07-03 16時裁定反映）**: 評価は独立役割にせずレビュー役の定義（役割型節）へ1行で統合するため、子09のレシピ節（4項目）とは重ならない。レシピ節へ評価専用の項目を足さないことで、判断基準（分担明示＋全体管理者宣言）を役割型節の1箇所だけに保つ。
- **spawnサブコマンド自体**（`scripts/cockpit.sh`の実装）は機構であり、本phase1・phase2とも変更対象外（scripts変更禁止の制約どおり）。SKILL.md側は子09が書いたレシピ節の参照先として`spawn`コマンド名をそのまま使う。

---

## 7. phase2実装手順とレビュー計画（枠回復後）

### 実装手順
1. 前提確認: `~/Private/personal-os/AIエージェント基盤` で子09（spawn関連の変更）がコミット・main反映済みであることを`git log`/`git status`で確認する。未反映なら着手しない。
2. 子09マージ後の実際の`SKILL.md`を読み直し、行数を再計測（§3の「61行以下」目標を実測値×0.7で再設定）。
3. 本ドラフト§3の目標構成に従い、`SKILL.md`を「目的／役割型／レシピ／安全」の4節へ再構成。評価はレビュー役の定義に1行統合（独立項目は作らない）。子09のレシピ節内容を無編集でレシピ節へ移設。
4. `references/agent-invocation.md`・`references/watch-and-permissions.md`を新設し、§4の移動リストどおり詳細を移す。`references/role-prompts.md`は無編集のまま参照元だけ更新。
5. 行数確認（30%以上減）。未達なら圧縮を継続。
6. `rg`で他Skill（`cockpit-supervisor`・`coding-task-orchestrator`等）が`orca-cockpit SKILL.md`の行番号や旧節番号（例:「§9見張り番」）を参照していないか確認し、参照があれば節見出し名ベースの参照に置き換える（review-rules.md §6）。
7. `global-skill-registry/catalog/meta.md`のorca-cockpit概要行が新構成と乖離していないか確認・追従（子09完了条件にも同項目あり、二重作業にしない＝子09側の追従が先に済んでいれば流用）。
8. `AGENTS.md`/`CLAUDE.md`symlinkの過不足を確認（review-rules.md §9.6）。

### レビュー計画
- codex 1パス（枠に余裕あり・遅延設計は不要=通常フローでよい）。観点: (a) 9→4節統合で内容欠落がないか（特に見張り番の検知種別・権限配布内容が安全な形でreferencesに移っているか） (b) references新設2ファイルへのリンク切れなし (c) 子09のレシピ節が改変されず移設されているか (d) cockpit-supervisorとのモデル既定重複が解消されているか (e) 役割型節のレビュー項目に「評価＝第三者実測・判定を含む」＋分担明示・全体管理者宣言ルールが1行で入っており、独立の評価役割・独立レシピ項目が増えていないか (f) SKILL.md行数が30%以上減か（数値で明記） (g) `AGENTS.md`/`CLAUDE.md`symlink過不足。
- PASS後、全体管理者へ差分提示→人間ゲートでマージ（scripts本体・role-prompts.md role定型自体は無変更のためリスクは低いが、削除・移動を伴うためマージは人間承認）。
- plan.md完了条件5項目（評価レーン1項存在／30%以上減＋リンク健全／workflows新設ゼロ／子09との重複なし統合／codexレビューPASS＋catalog追従）を1件ずつ確認してから`active/`→`done/`判定は全体管理者が行う。

---

## 判断メモ（迷った点と選んだ理由）

1. **行数基準の二重性**: 本worktree branchは子09マージ前（72行）だが、main実working treeは子09未コミット反映済み（88行）。保守的に「実測は子09マージ後に再計測」を明記し、仮の目標値（61行）を出す形にした（自走・停止しない方針に従う）。
2. **モデル既定の矛盾発見**: orca-cockpitとcockpit-supervisorのモデル既定が食い違っていた（実装=codex medium vs claude sonnet5等）。修正は本phase1のスコープ外（scripts/SKILL.md変更禁止）だが、簡素化の設計そのもの（役割型節でモデル既定を再掲しない）で自然に解消できるため、目標構成に組み込んだ。判断が必要なら全体管理者が最終確認すること。
3. **子09の扱い**: plan.mdの直列前提を尊重し、子09の内容を先取りして書き換える提案はせず「そのまま移設する」方針に限定した。
4. **2026-07-03 16時ユーザー裁定の反映**: (a) 「評価レーン」を独立役割にせず、役割型は「実装=claude／レビュー=codex（評価=第三者実測・判定を含む）」の2役のまま。外す時の宣言ルール（分担明示＋全体管理者宣言）はレビュー役の1行に統合し、独立の役割項目・レシピ項目は作らない形へ§2/§3.2/§3.3/§6/§7を修正した。(b) codex枠温存の前提を撤回（枠は余裕）したため、「計測実行=claude＋判定=codex後段」のような遅延設計は削除し、レビュー役がそのまま評価も担う通常フローに統一した。

CKREORG_PLAN_DONE
