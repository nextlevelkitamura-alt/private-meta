# 評価01

## 判定

全PASS。`images-generate` は、組み込み `image_gen` を直接使う一枚Skillだけになった。

## 採点

- [PASS] `skills/images-generate/` は `SKILL.md` だけで、削除した6ファイルと空ディレクトリは残っていない。
- [PASS] 本文に英語化、モックアップ分岐、CLI委任、フォールバック手順がなく、ユーザー指定を優先する。
- [PASS] catalogと `sns-post` の連携文が、汎用Skillと業務固有パイプラインの境界を明示する。
- [PASS] 現役範囲に削除済みの `workflows`、`references`、`agents`、`evals` への参照がない。
- [PASS] Global 4窓と仕事repo 2窓のsymlinkから同じ `SKILL.md` を読める。
