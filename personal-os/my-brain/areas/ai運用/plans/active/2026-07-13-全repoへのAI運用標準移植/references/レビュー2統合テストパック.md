# Review 2 統合テストパック

親計画: `../program.md`  
正式レビュー仕様: `統合レビュー仕様.md`  
作成者: Terra test-author（実装者・Integration担当とは別）  
状態: 設計完了・全12項目 `NOT-RUN`

この12項目をfocusmapカナリアとfleet分類の完了後、Review 2へ一括投入する。Review 1と同じく、子計画ごとの正式レビューは作らない。

## テスト項目

| ID | 状態 | 期待結果 | 必須証拠 | rollback / 停止条件 |
|---|---|---|---|---|
| R2-01 | NOT-RUN | Review 1最終roundが20項目全PASSで、security/hook回帰後もtracked secret・自動stage/commit/pushが0 | Review 1評価md・redacted scan件数・hook manifest hash | FAILならReview 2を止めReview 1次roundへ差戻す。credentialは旧値へ戻さない |
| R2-02 | NOT-RUN | Private→registry→repo AGENTS→既存plan/計画箱→focusmap sessionが一意。曖昧時exit 3 | route JSON・handoff payload・session-board時系列・index/parser hash | fixture破棄・session個別finish・reparent禁止 |
| R2-03 | NOT-RUN | canonical base・専用worktree・計画箱・allowed paths・許可test commandがF02で人間承認済み | 承認記録・開始snapshot・path manifest・command allowlist | 未承認ならF03以降を開始せず変更0 |
| R2-04 | NOT-RUN | `docs/ai/plans/active`=実行計画、`docs/plans`=product、`docs/specs`=spec、root plans=legacy候補を維持し、src/DB/package/deploy変更0 | 分類表・changed path・各path hash | F03/F04 commitだけrevertし既存文書を移動・昇格しない |
| R2-05 | NOT-RUN | 外部副作用なしのfocusmap計画が承認箱でE2Eを通り、既存plan合流と箱不明停止を再現 | route結果・canary plan・task-board・exit・index link | fixture commitだけrevertし開始snapshot・計画箱・indexへ復帰 |
| R2-06 | NOT-RUN | CLAUDE固有指示をAGENTSへlossless統合し、`CLAUDE.md -> AGENTS.md` の同一repo相対symlinkになる | 移行前hash・固有条項ID対応表・移行後hash・file kind/readlink | 契約commitをrevertしfile kind・target・hashをF01へ戻す |
| R2-07 | NOT-RUN | 変更path/commandが人間承認manifest内だけで、未承認npm test/lint/build・browser・curl・deployが0 | 実行log・exact path diff・allowlist照合・secret値出力0 | 逸脱時即停止・当該commitだけrevert。外部副作用は個別復旧 |
| R2-08 | NOT-RUN | repo-createがfocusmapを値なしread-only監査し、計画箱・CLAUDE形状・境界を検出。2回出力が決定的 | JSON envelope・2回のhash・finding/exit・secret値出力0 | 出力fixture破棄。実repo apply・上書き・移動0 |
| R2-09 | NOT-RUN | Git common dirでlinked worktreeを1repoへdedupeし、worktree containerと未mount deferredを区別 | canonical key・identity/mount-state表・重複前後件数 | 分類出力だけ破棄。worktree削除・branch整理・mount操作禁止 |
| R2-10 | NOT-RUN | active実repoを導入済み・保留・対象外へ重複なく分類し、仕事/focusmapの参照commitと再開条件が一意 | fleet matrix・registry/実体照合・commit hash・理由/再開条件 | repo単位で差戻し、他repoのPASSを巻き戻さない |
| R2-11 | NOT-RUN | focusmap契約waveとcanary waveを個別rollbackでき、AGENTS/CLAUDE・計画箱・index・Git状態が開始snapshotへ一致 | before/after hash・revert hash・dirty count・rollback結果 | 専用commit単位revert。worktree削除・branch改名禁止 |
| R2-12 | NOT-RUN | local commit・origin push・本番deployを別状態表示し、未承認push/deploy 0。active fleet完了と全履歴repo完了も別判定 | HEAD/origin参照・push/deploy state・人間判断記録 | local revertとpush/deploy取消を分離。未mountを導入済みにしない |

## Review 2への引渡し条件

1. Review 1の最終roundが全PASS。
2. 12項目すべてPASSで、`NOT-RUN` が0件。
3. focusmapのF02人間決定、F03契約、F04 E2E/rollback、F05証拠統合が完了。
4. fleet matrixがactive実repoと未mount/paused/archiveを分離し、container/worktreeを重複repoとして数えていない。
5. reviewerは `../plans/レビュー2-全体完了-評価NN.md` の1系列だけに採点し、同じroundで修正しない。

