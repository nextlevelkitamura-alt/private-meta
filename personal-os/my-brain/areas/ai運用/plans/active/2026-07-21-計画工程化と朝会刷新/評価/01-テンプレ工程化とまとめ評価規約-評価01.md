対象計画: plans/01-テンプレ工程化とまとめ評価規約.md ／ ラウンド: 01
diff範囲: working tree未コミット（skills/plan-ops/* ＋ plan-registry/AGENTS.md ＋ 新規 templates/まとめ評価.md） ／ 規模: フル ／ 評価者: reviewer（独立・実装者と別）

# 評価01: テンプレ工程化とまとめ評価規約

都度評価。理由: 子03・子04が子01の「## 工程」節仕様に直接依存するため、まとめ評価既定の例外として先に確定させる（plan-registry §2の都度評価条件に該当）。

## 項目別採点   ※ plan.md「完了条件」と同順・全項目

- [PASS] new-plan.sh生成のplan.md/program子に「## 工程」節がある
  根拠: reviewerが `new-plan.sh` / `new-child.sh` を実生成し、plan.mdに `## 工程`＋step行、子に `## 工程`＋step行、両方に `テンプレ: v2` を実在確認。
- [PASS] plan-lintが「工程節なし」「実装/評価混在」を検出しexit非0になる
  根拠: v2マーカー有り・工程節なしの独立クラフト → exit=1「工程節が無い」。`## 項目別採点`＋`- [PASS]`行の混在plan → exit=1「評価本文が計画に混在」。対照（採点行なし）は違反なし。既存テストとは別入力で再現（非トートロジー）。
- [PASS] plan-registryに「まとめ評価既定」の正文とまとめ評価テンプレがある
  根拠: plan-registry/AGENTS.md §評価に「評価はまとめが既定…」「まとめ評価は…テンプレは plan-ops/templates/まとめ評価.md」の2行を実見。templates/まとめ評価.md が実在し `## 項目別採点`／`## 総合判定`（「全PASS」ゲート説明つき）を持つ。
- [PASS] 既存active/done計画がlint新規則で誤FAILしない
  根拠: active/done配下 全38ファイル＋進行中の本program（program.md＋子01-04）へ変更後lintを実行し、新検査メッセージのヒット0件。既存に `テンプレ: v2` マーカー0件（マーカーゲートで構造的に非発火）。

## 総合判定

全PASS。テスト全走 240 pass / 0 fail（plan-lint (m)-(q)＝43件、bucketctl (j)＝46件を含む）。4完了条件すべて実物照合でPASS。回帰・後方互換の破れなし。子01は完了（評価済み）。

## 申告リスクの判定と指揮官対応

- リスク1（工程節ガイドコメント `<!-- -->` がPLACEHOLDER_REに一致）: reviewer判定=許容。テンプレは元々placeholder 21件で「実行前に全placeholder除去」設計と一貫。→ 指揮官対応: 保留（後続の軽微改善。文言の紛らわしさのみ・機能影響なし）。
- リスク2（`sorted(評価*.md + まとめ評価*.md)[-1]` の様式混在時ラウンド取り違え＝done誤通過の失敗モード）: reviewer判定=許容だが失敗モードは安全側でない。→ 指揮官対応: **規約ガードで即閉じた**。plan-registry §2に「1計画の評価/では 評価RR.md と まとめ評価RR.md を混在させない」を追記済み（本評価と同commit）。

## 指揮官の後処理（本評価後・同commit）

- program.mdテンプレの不活性 `テンプレ: v2` マーカーを除去（reviewer指摘の軽微不整合。program.mdは工程検査対象外＝マーカーはinert）。
- 方針#2「単発plan.mdでも評価の分離配置を生成」: 未実装だが4完了条件外。文書分離は lint_eval_mixing で達成済み。単発は既存規約どおり隣接 `評価NN.md` で足り、評価/フォルダ生成は不要（over-specだったと確定）。親programの統合評価では、完了条件「評価/分離があり」をこの解釈（＝文書分離であってフォルダ生成ではない）で採点する。

## 追加所見（reviewer）

- 追加テスト(m)-(q)・(j)は都度入力をクラフトし厳密なメッセージ・exit codeを検査＝非トートロジー。新ロジック（is_template_v2 / lint_steps / lint_eval_mixing / evaluation_filesのまとめ評価glob）を実際に叩いている。
- 生成program.md単体lintの「子計画マップに子ブロックが無い」FAILは変更前からのscaffold既存挙動で子01スコープ外（回帰ではない）。
